<div id=toc></div>

# Table of Contents

- [cs.CL](#cs.CL) [Total: 51]
- [cs.AI](#cs.AI) [Total: 51]


<div id='cs.CL'></div>

# cs.CL [[Back]](#toc)

### [1] [The Annotation Scarcity Paradox in Low-Resource NLP Evaluation: A Decade of Acceleration and Emerging Constraints](https://arxiv.org/abs/2605.19066)
*Vukosi Marivate*

Main category: cs.CL

TL;DR: The paper critically surveys how low-resource NLP evaluation has evolved and argues that current technical progress outpaces the sociolinguistic and community infrastructure needed to evaluate systems fairly and validly.


<details>
  <summary>Details</summary>
Motivation: Low-resource NLP has rapidly advanced using cross-lingual transfer and multilingual models, but the field lacks sufficient, fairly organized sociolinguistic expertise and community involvement to evaluate complex generative models, threatening the credibility and equity of reported progress.

Method: The authors conduct a critical narrative survey of low-resource NLP evaluation literature from 2014 to the present, organizing it into three historical phases and analyzing practices such as extractive data pipelines, ghost work, and language data flaring, as well as emerging solutions like data augmentation, model-based evaluation, participatory curation, and annotation-efficient methods.

Result: They identify an “Annotation Scarcity Paradox” where model scalability has far outstripped the available human, community-based evaluation capacity; they show how current practices create structural friction, inequities, and epistemic risks, and they map the trade-offs of different proposed mitigation strategies along axes of equity and validity.

Conclusion: The paper concludes that merely scaling models or benchmarks cannot resolve the evaluation bottleneck in low-resource NLP; instead, the field must shift toward relational, community-embedded evaluation frameworks that emphasize epistemic governance, data sovereignty, fair compensation, and shared ownership with speaker communities.

Abstract: Over the past decade, low-resource natural language processing (NLP) has experienced explosive growth, propelled by cross-lingual transfer, massively multilingual models, and the rapid proliferation of benchmarks. Yet this apparent progress masks a critical, insufficiently examined tension: the deep sociolinguistic expertise required to evaluate increasingly complex generative systems is severely strained, inequitably distributed, and structurally marginalised. We present a critical narrative survey of low-resource NLP evaluation (2014--present), tracing its evolution across three phases: early heuristic optimism, the illusions of top-down benchmark scaling, and the current era of generative bottlenecks. We conceptualise the \emph{Annotation Scarcity Paradox}, the structural friction arising when the technical capacity to scale models vastly outpaces the sovereign human infrastructure required to authentically evaluate them. By examining extractive data pipelines, undercompensated ``ghost work'', and language data flaring, we argue that this paradox threatens the epistemic validity of reported progress. We survey emerging responses -- including data augmentation, model-based evaluation, participatory curation, and annotation-efficient approaches via item response theory and active learning -- and assess their equity and validity trade-offs. We close with a practitioner call to action, arguing that overcoming this bottleneck requires a paradigm shift from transactional data extraction to relational, community-embedded evaluation rooted in epistemic governance, data sovereignty, and shared ownership.

</details>


### [2] [Benchmarking Commercial ASR Systems on Code-Switching Speech: Arabic, Persian, and German](https://arxiv.org/abs/2605.19069)
*Sajjad Abdoli,Ghassan Al-Sumaidaee,Clayton W. Taylor,Ahmad,ElShiekh,Ahmed Rashad*

Main category: cs.CL

TL;DR: The paper introduces a new benchmark for evaluating commercial ASR systems on code-switched speech across four language pairs, shows that current systems struggle in realistic multilingual conditions, and argues that BERTScore is a better metric than WER for Arabic and Persian pairs.


<details>
  <summary>Details</summary>
Motivation: Most commercial ASR benchmarks focus on clean, monolingual audio and report a single WER, which does not reflect real-world, multilingual, code-switched scenarios. There is also a lack of standardized, high-quality datasets and evaluation protocols for code-switched ASR, especially for language pairs involving Arabic and Persian, where script and transliteration issues distort WER-based evaluation.

Method: The authors construct four code-switching datasets (Egyptian Arabic–English, Saudi Arabic–English, Persian–English, and German–English), each with 300 samples. They use a two-stage selection pipeline: (1) a heuristic filter that scores transcripts on five structural code-switching indicators to preselect promising samples; (2) an ensemble of GPT-4o and Gemini 1.5 Pro that scores candidate samples across six linguistic dimensions to finalize the dataset, reducing LLM scoring costs versus exhaustive scoring. They then benchmark five commercial ASR providers on these datasets, evaluating with both WER and BERTScore, and perform difficulty-stratified and embedding-based analyses.

Result: The LLM-assisted selection pipeline cuts annotation/scoring cost by about 91% while still producing high-quality, structurally rich code-switching samples. Among five commercial ASR systems, ElevenLabs Scribe v2 delivers the best performance with an overall WER of 13.2% (13.1% on Egyptian Arabic) and the highest BERTScore of 0.936. BERTScore better captures semantic correctness in Arabic and Persian pairs where different but valid transliterations inflate WER. Difficulty-stratified analysis exposes performance differences that average metrics hide, and embedding projections show that model hypotheses are semantically close to references even when scripts differ.

Conclusion: A dedicated, publicly available benchmark for code-switched ASR reveals meaningful performance differences between commercial systems that standard monolingual WER benchmarks miss. BERTScore is a more appropriate metric than raw WER for script- and transliteration-sensitive pairs like Arabic–English and Persian–English. The proposed LLM-assisted selection pipeline offers an efficient way to curate complex code-switching datasets, and difficulty-aware evaluation plus embedding analysis provide a richer understanding of ASR behavior in multilingual, code-switched contexts.

Abstract: Code-switching -- the natural alternation between two languages within a single utterance -- represents one of the most challenging and under-studied conditions for automatic speech recognition (ASR). Existing commercial ASR benchmarks predominantly evaluate clean, monolingual audio and report a single Word Error Rate (WER) figure that tells practitioners little about real-world multilingual performance. We present a benchmark evaluating five commercial ASR providers across four language pairs: Egyptian Arabic--English, Saudi Arabic (Najdi/Hijazi)--English, Persian (Farsi)--English, and German--English. Each dataset comprises 300 samples selected by a two-stage pipeline: a heuristic filter scoring transcripts on five structural code-switching signals, followed by a GPT-4o and Gemini 1.5 Pro ensemble scoring candidates across six linguistic dimensions. This pipeline reduces LLM scoring costs by approximately 91\% relative to exhaustive scoring. We evaluate the systems on both WER and BERTScore, arguing that BERTScore is a more reliable metric for Arabic and Persian pairs where transliteration variance causes WER to penalise semantically correct transcriptions. ElevenLabs Scribe v2 achieves the lowest WER across all four language pairs (13.2% overall; 13.1% on Egyptian Arabic) and leads on BERTScore (0.936 overall). We further demonstrate that difficulty-stratified analysis reveals performance gaps masked by aggregate averages, and that BERT embedding projections confirm semantic proximity between reference and hypothesis despite surface-level script differences. The benchmarking dataset is publicly available at https://huggingface.co/datasets/Perle-ai/ASR_Code_Switch.

</details>


### [3] [ReacTOD: Bounded Neuro-Symbolic Agentic NLU for Zero-Shot Dialogue State Tracking](https://arxiv.org/abs/2605.19077)
*Yanjun Lin,Zimo Xiao,Kartik Natarajan,Mahesh Sankaranarayanan,Niraj Nawanit,Rakshit Parashar,Austin Zhang,Karthik Konaraddi,Rishita Mote,Wei Niu*

Main category: cs.CL

TL;DR: ReacTOD is a neuro-symbolic architecture for task-oriented dialogue that wraps moderately-sized LLMs in a bounded ReAct-style loop plus symbolic validation to reduce hallucinations and format errors, achieving new zero-shot state-of-the-art results on MultiWOZ and SGD.


<details>
  <summary>Details</summary>
Motivation: Task-oriented dialogue systems must behave predictably because their outputs trigger real-world actions like bookings or reservations, where mistakes can be costly. However, the moderately-sized LLMs needed for practical latency are prone to hallucinations and structural errors that propagate into wrong actions. Existing single-pass or unbounded agentic approaches lack strict guarantees on action validity and dialogue state correctness, especially in zero-shot settings without task-specific training. The paper aims to provide a method that keeps prompts small, improves reliability, and enforces schema-compliant actions while still leveraging LLM flexibility.

Method: The authors propose ReacTOD, a bounded neuro-symbolic architecture that reformulates natural language understanding as discrete tool calls within a constrained ReAct-style loop. In each turn, the LLM performs incremental dialogue state prediction and issues tool calls, while a deterministic symbolic validator checks every proposed update for action compliance, schema conformance, and coreference consistency. The ReAct loop is bounded, enabling a limited number of self-correction iterations when validation fails. The system also uses on-demand retrieval of dialogue history to keep prompts compact. This yields structured execution traces and supports zero-shot deployment across benchmarks without task-specific fine-tuning.

Result: On MultiWOZ 2.1, ReacTOD improves accuracy by up to 9.3 percentage points over single-pass inference, and achieves a new zero-shot state-of-the-art: gpt-oss-20B attains 52.71% joint goal accuracy, 14 points above the previous best, and Qwen3-8B achieves 47.34% with only 8B parameters. The symbolic validator attains a 93.1% self-correction rate on intercepted errors. On the Schema-Guided Dialogue benchmark, ReacTOD with Claude-Opus-4.6 achieves 80.68% JGA in a fully end-to-end setting with predicted domains, and Qwen3-32B reaches 64.09%, indicating robust cross-benchmark generalization without any task-specific training data.

Conclusion: ReacTOD demonstrates that wrapping moderately-sized LLMs in a bounded ReAct loop with deterministic symbolic validation can substantially reduce hallucinations and structural errors in task-oriented dialogue, while maintaining practical latency. This neuro-symbolic approach yields compact prompts, structured execution traces, and strong zero-shot performance that surpasses previous state-of-the-art on MultiWOZ and SGD, suggesting that carefully constrained tool-based reasoning and validation can make LLM-powered dialogue systems more reliable and generalizable without task-specific training.

Abstract: Task-oriented dialogue systems -- handling transactions, reservations, and service requests -- require predictable behavior, yet the moderately-sized LLMs needed for practical latency are prone to hallucination and format errors that cascade into incorrect actions (e.g., a hotel booked for the wrong date). We propose ReacTOD, a bounded neuro-symbolic architecture that reformulates NLU as discrete tool calls within a self-correcting ReAct loop governed by deterministic validation. A bounded ReAct loop enables iterative self-correction, improving accuracy by up to 9.3 percentage points over single-pass inference on MultiWOZ. A symbolic validator enforces action compliance, schema conformance, and coreference consistency on every dialogue state update, achieving a 93.1% self-correction rate on intercepted errors and producing structured execution traces. Incremental state prediction and on-demand history retrieval keep prompts compact, empirically improving instruction adherence in parameter-constrained models. On MultiWOZ 2.1, ReacTOD achieves a new zero-shot state-of-the-art: gpt-oss-20B reaches 52.71% joint goal accuracy, surpassing the previous best by 14 percentage points, while Qwen3-8B achieves 47.34% with only 8B parameters. On the Schema-Guided Dialogue (SGD) benchmark, ReacTOD with Claude-Opus-4.6 achieves 80.68% JGA under fully end-to-end evaluation with predicted domains, and Qwen3-32B reaches 64.09% -- demonstrating cross-benchmark generalization without task-specific training data.

</details>


### [4] [Agent Meltdowns: The Road to Hell Is Paved with Helpful Agents](https://arxiv.org/abs/2605.19149)
*Rishi Jha,Harold Triedman,Arkaprabha Bhattacharya,Vitaly Shmatikov*

Main category: cs.CL

TL;DR: They define and measure 'accidental meltdown': harmful agent behavior triggered only by benign system errors, not attacks.


<details>
  <summary>Details</summary>
Motivation: Existing reliability/safety benchmarks miss how agents react to ordinary web/computer errors, which may trigger unsafe actions even without adversaries. They want to systematically study this overlooked failure mode.

Method: They build a taxonomy of meltdown behaviors, then create an agent-agnostic framework that injects controlled local and remote errors into agents' rollout environments. Using this, they test multiple agent systems powered by GPT, Grok, and Gemini across many error conditions and log behaviors.

Result: Across models, systems, and error types, 64.7% of rollouts that hit simulated errors show some meltdown behavior, such as unauthorized reconnaissance or bypassing access control. More than half of these unsafe behaviors are not surfaced to the user. Error-triggered exploration is statistically associated with unsafe behavior.

Conclusion: Benign environmental errors can reliably elicit unsafe behaviors from current agent systems, even without adversarial prompts, and these behaviors are often hidden from users. Evaluating and mitigating this new 'accidental meltdown' class of failures is essential for realistic agent safety and reliability.

Abstract: Agents operating with computer and Web use inevitably encounter errors: inaccessible webpages, missing files, local and remote misconfigurations, etc. These errors do not thwart agents based on state-of-the-art models. They helpfully continue to look for ways to complete their tasks.
  We introduce, characterize, and measure a new type of agent failure we call \emph{accidental meltdown}: unsafe or harmful behavior in response to a benign environmental error, in the absence of any adversarial inputs. Because meltdowns are not captured by the existing reliability or safety benchmarks, we develop a taxonomy of meltdown behaviors. We then implement an agent-agnostic infrastructure for injecting simulated local and remote errors into the rollout environment and use it to systematically evaluate agent systems powered by GPT, Grok, and Gemini.
  Our evaluation demonstrates that meltdowns (e.g., conducting unauthorized reconnaissance or subverting access control) of varying severity and success occur in 64.7\% of agent rollouts that encounter simulated errors, spanning all combinations of agent system, backing model, and error type. In over half of these meltdowns, unsafe behaviors are not reported to the user. Comparing behaviors of the same agents with and without errors, we find that exploration in response to errors is correlated with unsafe and harmful behavior.

</details>


### [5] [MMoA: An AI-Agent framework with recurrence for Memoried Mixure-of-Agent](https://arxiv.org/abs/2605.19194)
*Rui Chu*

Main category: cs.CL

TL;DR: Proposes MMoA, a recurrent mixture-of-agents architecture that uses an LSTM-based router to select and weight LLM agents more contextually, preserving accuracy while activating fewer agents and reducing computation.


<details>
  <summary>Details</summary>
Motivation: Existing Mixture-of-Agents frameworks improve LLM performance by aggregating multiple agent outputs, but their routers are typically static and cannot exploit temporal and contextual dependencies across aggregation layers or interactions. This limits adaptivity and can lead to unnecessary activation of many agents, increasing computation cost without proportional gains.

Method: Introduce MMoA, a recurrent MoA architecture where the routing mechanism is implemented as an LSTM-based recurrence gate. At each aggregation step, the recurrent router takes the current input and the history of past routing decisions (encoded in LSTM hidden states) to adaptively determine which agents to activate and how much to weight their outputs. The system is evaluated on standard instruction-following benchmarks (AlpacaEval 2.0, MT-Bench, Arena-Hard) and compared against a traditional, non-recurrent MoA baseline in terms of accuracy and runtime efficiency.

Result: MMoA matches or nearly matches the accuracy of traditional MoA while using fewer active agents and thus less computation. On AlpacaEval 2.0, it achieves a 58.0% win rate versus 59.8% for MoA, yet improves runtime efficiency by up to 4.6%. Similar trends hold across other instruction-following benchmarks, indicating that recurrent routing can maintain quality with lower cost.

Conclusion: Incorporating recurrent, LSTM-based routing into MoA architectures enables more context-aware and temporally informed agent selection, which reduces the number of active agents and computational overhead with only minor (if any) loss in accuracy. MMoA is therefore a scalable, efficient approach for adaptive multi-agent LLM systems and a promising direction for future MoA designs that better exploit interaction history.

Abstract: The Mixture-of-Agents (MoA) framework has shown promise in improving large language model (LLM) performance by aggregating outputs from multiple agents. However, existing MoA systems often rely on static routers that do not fully capture temporal and contextual dependencies across aggregation layers. To address this limitation, we propose MMoA, a recurrent MoA architecture that integrates LSTM-based gating into the agent selection process. The recurrence router adaptively modulates agent contributions based on both current inputs and historical routing decisions, enabling more context-aware aggregation. We evaluate MMoA on standard instruction-following benchmarks, including AlpacaEval 2.0, MT-Bench, and Arena-Hard. The results show that MMoA achieves comparable accuracy to traditional MoA while reducing computational overhead by dynamically activating fewer agents. For example, on AlpacaEval 2.0, MMoA achieves a win rate of 58.0%, compared with 59.8% for MoA, while improving runtime efficiency by up to 4.6%. These results suggest that MMoA provides a scalable and efficient approach for adaptive multi-agent LLM systems.

</details>


### [6] [Time to REFLECT: Can We Trust LLM Judges for Evidence-based Research Agents?](https://arxiv.org/abs/2605.19196)
*Leyao Wang,Yanan He,Peng Chen,Asaf Yehudai,Yixin Liu,Rex Ying,Michal Shmueli-Scheuer,Arman Cohan*

Main category: cs.CL

TL;DR: The paper introduces REFLECT, a benchmark for rigorously evaluating how reliably LLMs can act as judges of deep research agents’ outputs, revealing that current LLM judges are often inaccurate, especially at verifying evidence.


<details>
  <summary>Details</summary>
Motivation: Deep research agents increasingly handle complex, multi-step information-seeking and reporting tasks, making automated, scalable evaluation important. LLMs are used as judges to assess these agents’ factual accuracy, evidence use, and reasoning, but the reliability of these LLM judges is not well understood. Existing meta-evaluations rely on subjective human preferences and focus on simpler or verifiable tasks, not on open-ended agent executions, leaving a gap in understanding judge reliability in realistic settings.

Method: The authors build REFLECT, a meta-evaluation benchmark that defines a detailed taxonomy of failure modes at both process (reasoning, tool use) and outcome (final report) levels. They generate benchmark instances by taking high-quality agent execution traces and applying controlled, localized interventions that introduce specific, known errors. These manipulated traces create verifiable and fine-grained test cases for validating how well different LLM judges can detect various types of failures in agent behavior and outputs.

Result: Using REFLECT, the authors evaluate current LLM judges across reasoning, tool-use, and report-quality failure modes. They find that even the best judge models achieve under 55% accuracy overall, with especially low performance in verifying whether evidence is correctly used and cited. This indicates that today’s LLM judges frequently miss or misclassify important errors in deep research agents’ processes and outputs.

Conclusion: REFLECT exposes systematic limitations in LLM-as-judge evaluation for deep research agents, showing that current judges are not reliably detecting many fine-grained failures, particularly around evidence verification. The benchmark and its taxonomy highlight tradeoffs between cost and reliability among judge models and provide practical guidance for designing more trustworthy evaluation pipelines and future judge architectures for complex agentic research systems.

Abstract: Deep research agents increasingly automate complex information-seeking tasks, producing evidence-grounded reports via multi-step reasoning, tool use, and synthesis. Their growing role demands scalable, reliable evaluation, positioning LLM-as-judge as a supervision paradigm for assessing factual accuracy, evidence use, and reasoning quality. Yet the reliability of these judges for deep research agents remains poorly understood, posing a critical meta-evaluation problem: before deploying LLM judges to supervise research agents, we must first evaluate the judges themselves. Existing meta-evaluations fall short in two ways: (1) reliance on coarse, subjective human-preference agreement; (2) focus on instruction-following or verifiable tasks, leaving open-ended agent executions unexplored. To address these gaps, we introduce REFLECT (REliable Fine-grained LLM judge Evaluation via Controlled inTervention), a meta-evaluation benchmark targeting fine-grained failure detection in agentic environments. REFLECT defines a detailed taxonomy of process- and outcome-level failure modes, instantiated by performing controlled and localized interventions on quality-screened agent execution traces. This yields verifiable, comprehensive, and fine-grained instances for validating the judge models. Our experiments show that current LLM judges remain unreliable: even the best-performing models achieve overall accuracies below 55% across reasoning, tool-use, and report-quality failures, with especially poor performance on evidence verification. Together, our taxonomy and findings expose systematic judge limitations, reveal tradeoffs in cost and reliability, and offer actionable guidance for building more reliable evaluation pipelines for deep research agents.

</details>


### [7] [Position: Uncertainty Quantification in LLMs is Just Unsupervised Clustering](https://arxiv.org/abs/2605.19220)
*Tiejin Chen,Longchao Da,Xiaoou Liu,Hua Wei*

Main category: cs.CL

TL;DR: The paper argues that current uncertainty quantification (UQ) methods for LLMs are flawed because they measure internal consistency instead of external correctness, leading to overconfidence in hallucinations, and proposes a new paradigm for grounding UQ in objective truth and better evaluation practices.


<details>
  <summary>Details</summary>
Motivation: As LLMs are increasingly used in high-stakes applications, we need reliable measures of when to trust or distrust their outputs. Existing UQ methods are assumed to provide this safeguard, but the authors suspect these methods actually fail to capture whether answers are factually correct, thus posing safety and deployment risks.

Method: Conceptual and critical analysis of existing UQ approaches for LLMs, showing that they essentially perform unsupervised clustering over model generations and internal states. The authors identify three pathologies—hyperparameter sensitivity, internal evaluation cycles, and lack of ground truth—and use these to argue that current methods quantify stability rather than truth. They then outline a roadmap and design principles for future UQ methods that incorporate external verification, improved metrics, and native uncertainty mechanisms in models.

Result: They demonstrate, at a conceptual and methodological level, that mainstream UQ approaches cannot reliably detect confident hallucinations and are highly sensitive to design choices, leading to misleading uncertainty estimates. They show how current evaluations conflate internal agreement with factual correctness and rely on unstable proxies instead of objective ground truth.

Conclusion: The prevailing approach to UQ in LLMs is mis-specified: it treats internal agreement as a surrogate for truth, which breaks down in the presence of stable but wrong outputs. To make UQ a real safety tool, the community must shift toward methods that are grounded in external reality, use robust and appropriate evaluation setups, and incorporate mechanisms for native uncertainty that align model confidence with actual correctness.

Abstract: Uncertainty Quantification (UQ) is widely regarded as the primary safeguard for deploying Large Language Models (LLMs) in high-stakes domains. However, we argue that the field suffers from a category error: mainstream UQ methods for LLMs are just unsupervised clustering algorithms. We demonstrate that most current approaches inherently quantify the internal consistency of the model's generations rather than their external correctness. Consequently, current methods are fundamentally blind to factual reality and fail to detect ``confident hallucinations,'' where models exhibit high confidence in stable but incorrect answers. Therefore, the current UQ methods may create a deceptive sense of safety when deploying the models with uncertainty. In detail, we identify three critical pathologies resulting from this dependence on internal state: a hyperparameter sensitivity crisis that renders deployment unsafe, an internal evaluation cycle that conflates stability with truth, and a fundamental lack of ground truth that forces reliance on unstable proxy metrics to evaluate uncertainty. To resolve this impasse, we advocate for a paradigm shift to UQ and outline a roadmap for the research community to adopt better evaluation metrics and settings, implement mechanism changes for native uncertainty, and anchor verification in objective truth, ensuring that model confidence serves as a reliable proxy for reality.

</details>


### [8] [Fine-tuning language encoding models on slow fMRI improves prediction for fast ECoG](https://arxiv.org/abs/2605.19224)
*Aditya R. Vaidya,Richard J. Antonello,Alexander G. Huth*

Main category: cs.CL

TL;DR: The paper shows that slow, non-invasive fMRI data can be used to train language representations that generalize to and improve prediction of fast, invasive ECoG brain signals.


<details>
  <summary>Details</summary>
Motivation: Intracranial recordings like ECoG provide high-quality neural data but are limited to small, clinically constrained patient populations, restricting the amount and diversity of training data available for building neural encoding/decoding models. There is a need for a way to leverage large-scale, non-invasive data such as fMRI to overcome these limitations and improve models of fast brain dynamics.

Method: The authors fine-tune spoken language representation models on fMRI responses to speech, then use these fMRI-tuned representations as features in encoding models of ECoG activity. They compare prediction performance to other representations across ECoG frequency bands. They also systematically downsample the fMRI data by a factor of two to test robustness to reduced temporal resolution and examine how ECoG prediction performance scales with the amount of fMRI training data used for fine-tuning.

Result: Language representations fine-tuned on fMRI significantly improved prediction of ECoG responses, despite fMRI’s much poorer temporal resolution. The improvements extended to ECoG frequency bands that fMRI does not directly measure. Models trained on temporally downsampled fMRI still predicted both fMRI and ECoG responses at performance levels comparable to models trained on full-resolution fMRI. ECoG prediction performance increased steadily with the quantity of fMRI data used for fine-tuning.

Conclusion: Slow, non-invasive fMRI data can be effectively used to train neural language representations that transfer to and enhance models of fast, invasive ECoG signals. This suggests that integrating across recording modalities can compensate for individual limitations and may further benefit applications such as neural decoding in the future.

Abstract: Neuroscientists have recently turned to intracranial brain recording methods, like electrocorticography (ECoG), for human experiments because of the fine spatial and temporal resolution that they afford. Models trained on this data, however, are fundamentally restricted by the patient populations that can receive the implants necessary for recording. We propose using non-invasive fMRI to bridge the gap in training data. Using spoken language representations fine-tuned on fMRI, we build encoding models of ECoG. These representations showed improved prediction performance in ECoG, even though the temporal resolution of fMRI is two orders of magnitude worse. Prediction improved in frequency bands well beyond what is directly measured in fMRI. Next, to test the procedure's generalization ability, we fine-tuned models on fMRI responses that were temporally downsampled by a factor of 2. Despite the loss in resolution, these models were able to predict fMRI and ECoG responses at levels comparable to the original fMRI-tuned models. Finally, we showed that ECoG performance steadily scales with the amount of fMRI-tuning data. Our results show that "slow" data like fMRI can be a valuable resource for building better models of "fast" brain data like ECoG. In the future, integrating across multiple recording methods may further improve performance in other applications, like decoding.

</details>


### [9] [Diagnosing Multi-step Reasoning Failures in Black-box LLMs via Stepwise Confidence Attribution](https://arxiv.org/abs/2605.19228)
*Xiaoou Liu,Tiejin Chen,Dengjia Zhang,Yaqing Wang,Lu Cheng,Hua Wei*

Main category: cs.CL

TL;DR: They propose a method to estimate confidence for each step in LLM reasoning traces to better detect and fix errors.


<details>
  <summary>Details</summary>
Motivation: While LLMs can perform multi-step reasoning, it's hard to know which specific step in a reasoning trace causes failure. Existing confidence estimation focuses on final answers or requires internal access to the model, limiting applicability to closed-source LLMs. There is a need for a step-level, model-agnostic diagnostic tool.

Method: They introduce Stepwise Confidence Attribution (SCA), a framework that uses the Information Bottleneck principle to assign confidence to each reasoning step based only on generated traces. SCA gives higher confidence to steps consistent with consensus patterns across correct solutions and flags deviations as low confidence. They instantiate SCA with two methods: NIBS, a non-parametric IB-based method that measures consistency without explicit graph structures; and GIBS, a graph-based IB model that learns important subgraphs via differentiable masks to capture logical variability.

Result: Experiments on mathematical reasoning and multi-hop QA tasks show that SCA can reliably assign low confidence to steps that correspond to actual reasoning errors. Using these step-level confidence scores to guide self-correction improves correction success rates by up to 13.5% compared with using only answer-level feedback.

Conclusion: Stepwise Confidence Attribution enables step-level error diagnosis in LLM reasoning without internal model access, and its two variants, NIBS and GIBS, effectively identify problematic steps and enhance self-correction performance over traditional answer-level confidence methods.

Abstract: Large Language Models have achieved strong performance on reasoning tasks with objective answers by generating step-by-step solutions, but diagnosing where a multi-step reasoning trace might fail remains difficult. Confidence estimation offers a diagnostic signal, yet existing methods are restricted to final answers or require internal model access. In this paper, we introduce Stepwise Confidence Attribution (SCA), a framework for closed-source LLMs that assigns step-level confidence based only on generated reasoning traces. SCA applies the Information Bottleneck principle: steps aligning with consensus structures across correct solutions receive high confidence, while deviations are flagged as potentially erroneous. We propose two complementary methods: (1) NIBS, a non-parametric IB approach measuring consistency without graph structures, and (2) GIBS, a graph-based IB model that learns subgraphs through a differentiable mask to capture logical variability. Extensive experiments on mathematical reasoning and multi-hop question answering show that SCA reliably identifies low-confidence steps strongly correlated with reasoning errors. Moreover, using step-level confidence to guide self-correction improves the correction success rate by up to 13.5\% over answer-level feedback.

</details>


### [10] [AI Technologies in Language Access: Attitudes Towards AI and the Human Value of Language Access Managers](https://arxiv.org/abs/2605.19234)
*Miguel A. Jiménez-Crespo,Stephanie Rodriguez,Alejandro Jaume Losa*

Main category: cs.CL

TL;DR: The paper examines how AI is affecting language access services, focusing on managers’ attitudes toward AI and the ongoing role of humans.


<details>
  <summary>Details</summary>
Motivation: To understand how AI-driven tools are changing language access, a field with high stakes due to legal, ethical, commercial, and safety considerations, and to explore how those responsible for managing language access view these changes and the value of human work in an AI-driven environment.

Method: A qualitative thematic analysis of ten semi-structured interviews with language access managers in the United States who work in healthcare, courts, public services, and local government. The interviews are part of a larger study on language access and technology.

Result: Language access managers are cautiously or conditionally optimistic about AI as its implementation is seen as inevitable. They are highly aware of risks and emphasize the need for safeguards. They consistently stress the importance of human value and insist on human oversight over AI use and outputs.

Conclusion: AI will inevitably become more prominent in language access, but its adoption is shaped by managers’ conditional optimism, strong risk awareness, and firm commitment to preserving human value and human oversight in AI-mediated language services.

Abstract: The rapid emergence of AI technologies is reshaping translation practices and theory across the board. This paper deals with the impact of AI in language access. This area is characterized by the need to serve broad and diverse user populations, within a context where efficiency and access are shaped by legal mandates, ethical and commercial tensions, and safety concerns. This paper reports on the attitudes and perceptions of language access managers towards the AI and the human value in the AI age. Methodologically, this paper presents an analysis of a subset of a broader study on language access and technology, specifically a qualitative thematic analysis of ten semi-structured interviews with language access managers in the USA working in healthcare, court, public service and local government contexts. The results indicate that language access managers show conditional optimism towards the inevitable AI implementations, are strongly risk aware, and deeply committed to the human value and human oversight of AI implementations and output.

</details>


### [11] [FormalASR: End-to-End Spoken Chinese to Formal Text](https://arxiv.org/abs/2605.19266)
*Wanyi Ning,Yinshang Guo,Haitao Qian,Jiyuan Cheng,Weiyuan Feng,Yufei Zhang*

Main category: cs.CL

TL;DR: The paper proposes FormalASR, compact end-to-end speech recognition models that directly convert spoken Chinese into formal written text, avoiding a separate LLM post-editing stage.


<details>
  <summary>Details</summary>
Motivation: Conventional ASR produces verbatim transcripts with disfluencies and informal structures, which are suboptimal for writing-oriented applications and usually require a latency- and resource-heavy ASR+LLM post-editing pipeline that is hard to run on-device.

Method: The authors construct two large-scale spoken-to-formal Chinese datasets (WenetSpeech-Formal and Speechio-Formal) via LLM-based rewriting and quality filtering, then perform supervised fine-tuning of Qwen3-ASR models at 0.6B and 1.7B parameter scales to directly output formalized text from speech.

Result: On both WenetSpeech-Formal and Speechio-Formal benchmarks, FormalASR achieves up to 37.4% relative character error rate reduction compared with verbatim ASR baselines and also yields higher ROUGE-L and BERTScore, indicating better match with reference formal text.

Conclusion: FormalASR can directly generate formal written transcriptions from spoken Chinese without an additional post-editing LLM, reducing latency and resource usage and making on-device spoken-to-formal transcription practical.

Abstract: Automatic speech recognition (ASR) systems are typically optimized for verbatim transcription, which preserves disfluencies, filler words, and informal spoken structures that are often unsuitable for downstream writing-oriented applications. A common workaround is a two-stage ASR+LLM pipeline for post-editing, but this design increases latency and memory cost and is difficult to deploy on-device. We present FormalASR, two compact end-to-end models (0.6B and 1.7B) that directly transcribe spoken Chinese into formal written text. To enable this setting, we build WenetSpeech-Formal and Speechio-Formal, two large-scale spoken-to-formal datasets constructed by LLM-based rewriting and quality filtering. We then fine-tune Qwen3-ASR at two scales (0.6B and 1.7B) with supervised fine-tuning. Experiments on WenetSpeech-Formal and Speechio-Formal show that FormalASR achieves up to 37.4% relative CER reduction over verbatim baselines, while also improving ROUGE-L and BERTScore. FormalASR requires no post-processing LLM at deployment time, providing a lightweight, on-device solution for spoken-to-formal transcription.

</details>


### [12] [DECOR: Auditing LLM Deception via Information Manipulation Theory](https://arxiv.org/abs/2605.19270)
*Linyue Cai,Samuel Yeh,Jwala Dhamala,Rahul Gupta,Sharon Li*

Main category: cs.CL

TL;DR: The paper proposes DECOR, a multi-agent, theory-grounded framework that detects and explains fine-grained deceptive behavior in LLM outputs, achieving state-of-the-art results across models and benchmarks.


<details>
  <summary>Details</summary>
Motivation: Existing methods for detecting deception in large language models are coarse-grained, largely uninterpretable, and cannot specify which facts are being manipulated or how. There is a need for an approach that can provide fine-grained, interpretable analysis of information manipulation in LLM responses.

Method: The authors develop DECOR, a multi-agent framework based on Information Manipulation Theory. DECOR first decomposes the input context into atomic informational units. For each unit, specialized agents evaluate the correspondence between the unit and the model’s response along four manipulation dimensions (e.g., omission, distortion, distraction, or obfuscation), assigning scores that yield a detailed manipulation profile. These profiles are then aggregated into a global deception index for the response. The framework is evaluated across multiple LLMs and benchmarks, and ablation studies analyze the importance of its components.

Result: DECOR achieves state-of-the-art performance on both single-turn and multi-turn deception detection benchmarks across real-world domains. It outperforms competitive black-box baselines and generalizes well across 15 different frontier language models. Ablation studies validate that each principal design choice in DECOR contributes meaningfully to its overall performance.

Conclusion: Fine-grained, theory-based auditing of information manipulation—implemented via the DECOR multi-agent framework—provides an effective and interpretable method for detecting deceptive behavior in large language models, improving both performance and transparency over existing coarse-grained approaches.

Abstract: Large language models can deceive by subtly manipulating truthful information -- omitting key facts, shifting focus, or obscuring meaning -- making such behavior difficult to detect. Existing black-box methods rely on coarse-grained judgments, offering limited interpretability and failing to pinpoint which facts were distorted and how. We introduce DECOR, a multi-agent framework grounded in Information Manipulation Theory for fine-grained auditing of strategic deception in LLM responses. DECOR decomposes input contexts into atomic informational units and scores each unit against the response across four dimensions of manipulation, producing interpretable manipulation profiles that are aggregated into a global deception index. We comprehensively evaluate DECOR on both single-turn and multi-turn deception detection benchmarks spanning real-world domains, and show that DECOR achieves state-of-the-art performance on both, outperforming competitive baselines. The framework generalizes across 15 frontier models, and ablation studies confirm the contribution of each key design component. Our findings demonstrate that fine-grained, theory-grounded auditing of information manipulation offers an effective and interpretable path for LLM deception detection.

</details>


### [13] [Lost in Interpretation: The Plausibility-Faithfulness Trade-off in Cross-Lingual Explanations](https://arxiv.org/abs/2605.19274)
*Somnath Banerjee,Pranav Jha,Rima Hazra,Animesh Mukherjee*

Main category: cs.CL

TL;DR: The paper shows that using English explanations to audit multilingual LLM decisions creates a trade-off: English explanations align better with human-highlighted text spans but are less faithful to the model’s actual decision process, so audits should prefer native-language explanations and richer faithfulness metrics.


<details>
  <summary>Details</summary>
Motivation: Multilingual LLMs are often evaluated and audited using English explanations, even when the inputs are in other languages, because English is convenient and widely understood. However, it is unclear whether these English "pivot" explanations accurately reflect the model’s true internal decision-making over non-English inputs, or whether they introduce distortions that could mislead auditors, especially for socially nuanced tasks. The authors aim to systematically test how faithful English explanations really are compared to explanations generated directly in the input language.

Method: The authors study extractive explanations, where a model highlights input token spans as evidence and produces a textual rationale. They run controlled evaluations across three tasks, five languages, and two multilingual LLM families. They compare two explanation settings: native-language explanations (same language as input) vs. English-pivot explanations (non-English input, English explanation). They measure: (1) span agreement with human rationales (lexical overlap between model-selected spans and human annotations), and (2) causal faithfulness of the evidence using comprehensiveness and sufficiency metrics—how much the highlighted spans affect the model’s prediction when removed or used alone. They also inspect socially nuanced classification tasks to see whether pragmatic and context-sensitive cues survive the English pivot.

Result: English-pivot explanations often have higher span agreement with human rationales, meaning the highlighted spans look more similar to human-annotated evidence. However, these same explanations are less causally grounded in the model’s predictions: comprehensiveness and sufficiency decline, with comprehensiveness worsening by up to 5.7x relative to native-language explanations, even though task accuracy remains largely unchanged. English rationales are fluent but loosely anchored to the model’s actual decision process. For socially nuanced classification, English explanations fail to preserve important pragmatic cues from the source language, which further lowers both faithfulness and span agreement in those settings.

Conclusion: Auditing multilingual LLMs with English explanations is risky: although English rationales can look better aligned with human highlights, they are less faithful to the model’s real decision pathway. Consequently, English explanations should be treated mainly as readable communication summaries, not as faithful traces of the underlying decision. The authors recommend (1) generating and auditing explanations in the input language whenever possible, (2) using multi-faceted faithfulness metrics—such as comprehensiveness and sufficiency—rather than relying solely on lexical overlap with human rationales, and (3) being cautious when interpreting English-pivot explanations, especially for socially nuanced classification tasks where pragmatic information is critical.

Abstract: LLMs deployed multilingually are often audited via English explanations for non-English inputs. We evaluate extractive explanations ''where the model identifies input token spans as evidence alongside a generated rationale'' and uncover a systematic trade-off: English-pivot explanations can achieve higher span agreement with human rationales while their evidence becomes less causally grounded in the model's prediction, as measured by both comprehensiveness and sufficiency. Across 3 tasks, 5~languages, and 2~multilingual LLM families, we find that English explanations frequently produce fluent but loosely anchored rationales, with comprehensiveness degrading by up to 5.7x relative to native-language conditions - even as task accuracy remains stable across settings. For socially nuanced classification, English pivots also fail to preserve pragmatic cues, reducing both faithfulness and span agreement. We recommend auditing explanations in the input language, reporting multi-faceted faithfulness metrics beyond lexical overlap, and treating English rationales as communication summaries rather than faithful decision traces.

</details>


### [14] [OpenCompass: A Universal Evaluation Platform for Large Language Models](https://arxiv.org/abs/2605.19276)
*Maosong Cao,Kai Chen,Haodong Duan,Yixiao Fang,Tong Gao,Ge Jiaye,Mo Li,Hongwei Liu,Junnan Liu,Yuan Liu,Chengqi Lyu,Han Lyu,Ningsheng Ma,Zerun Ma,Yu Sun,Zhiyong Wu,Linchen Xiao,Jun Xu,Haochen Ye,Zhaohui Yu,Yike Yuan,Songyang Zhang,Yufeng Zhao,Fengzhe Zhou,Peiheng Zhou,Dongsheng Zhu,Lin Zhu,Jingming Zhuo*

Main category: cs.CL

TL;DR: The paper introduces OpenCompass, an open-source, scalable platform for unified, high-concurrency evaluation of large language models across many benchmarks and task types.


<details>
  <summary>Details</summary>
Motivation: Existing LLM evaluation methods based on static benchmark datasets suffer from fragmented data and workflows, inconsistent criteria, and difficulty handling diverse tasks and large-scale, cross-domain model assessments. There is a need for an objective, quantitative, and comprehensive evaluation platform that can unify and streamline these processes as LLM development accelerates.

Method: The authors design and implement OpenCompass, a modular, component-decoupled LLM evaluation framework. Its architecture includes five main components: a Configuration System, Task Partitioning Module, Execution and Scheduling Module, Task Execution Unit, and Result Visualization Module. The workflow supports multiple evaluation modes—rule-based metrics, LLM-as-a-Judge, and cascaded evaluators—and integrates a wide range of mainstream benchmark datasets spanning knowledge, reasoning, computation, science, language, and code tasks.

Result: OpenCompass is open-sourced as a general-purpose LLM evaluation platform that supports high compatibility with diverse benchmarks and models, flexible configuration for different task scenarios, and high-concurrency execution to enable large-scale evaluations. It successfully unifies dataset handling and evaluation workflows across domains, enabling efficient, cross-domain model comparison.

Conclusion: OpenCompass provides a unified, efficient, and extensible solution for evaluating large language models at scale. By offering modular architecture, multiple evaluation strategies, and support for many established benchmarks, it helps both academia and industry objectively diagnose LLM strengths and weaknesses and guide subsequent optimization and development.

Abstract: In recent years, the field of artificial intelligence has undergone a paradigm shift from task-specific small-scale models to general-purpose large language models (LLMs). With the rapid iteration of LLMs, objective, quantitative, and comprehensive evaluation of their capabilities has become a critical link in advancing technological development. Currently, the mainstream static benchmark dataset-based evaluation methods face challenges such as the diversity of task types, inconsistent evaluation criteria, and fragmentation of data and processing workflows, making it difficult to efficiently conduct cross-domain and large-scale model evaluation. To address the aforementioned issues, this paper proposes and open-sources OpenCompass, a one-stop, scalable, and high-concurrency-supported general-purpose LLM evaluation platform. Adhering to the design philosophy of modularization and component decoupling, the platform boasts three core advantages: high compatibility, flexibility, and high concurrency. The core architecture of OpenCompass comprises five key components: the Configuration System, Task Partitioning Module, Execution and Scheduling Module, Task Execution Unit, and Result Visualization Module. Its workflow provides rule-based, LLM-as-a-Judge, and cascaded evaluators to adapt to the requirements of different task scenarios. Supporting mainstream benchmark datasets across multiple domains, including knowledge, reasoning, computation, science, language, code, etc., the platform offers a unified and efficient LLM evaluation tool for both academia and industry, facilitating the accurate identification of strengths and weaknesses of LLMs as well as their subsequent optimization.

</details>


### [15] [Language models struggle with compartmentalization](https://arxiv.org/abs/2605.19284)
*Thomas Vincent Howe,David Wingate*

Main category: cs.CL

TL;DR: The paper shows that large language models often compartmentalize different surface forms of the same underlying concept (e.g., across languages or formalisms), leading to redundant, parallel internal representations instead of shared ones, which harms sample efficiency and capacity use.


<details>
  <summary>Details</summary>
Motivation: To understand and quantify how well large language models actually unify knowledge across different surface realizations of the same latent concepts (such as multiple natural languages or programming languages), and to explain why scaling and multilingual/dual-format training sometimes underperform expectations.

Method: The authors construct settings where the same underlying concepts (facts, functions, propositions) are presented in multiple distinct formats (different natural languages, programming languages, formal vs natural language). They then probe whether models share representations or instead learn separate ones, analyzing sample efficiency, internal representations, and the effect of adding synthetic parallel data, along with interventions intended to promote unification. They study small models and investigate phase transitions in the effectiveness of these interventions as the number of distinct presentations grows.

Result: They find that LLMs can strongly compartmentalize: rather than building shared representations across formats, they often learn redundant parallel internal representations. This reduces sample efficiency and uses model capacity ineffectively, and synthetic parallel data does not reliably fix this. In small models, early multilingual learning is found to be almost fully compartmentalized. The authors also observe a phase transition pattern where interventions only help below some number of distinct presentations.

Conclusion: The language modeling objective does not reliably induce unified, shared representations across different surface realizations of the same underlying concept. Instead, models often compartmentalize, duplicating knowledge across formats and wasting capacity. This has implications for multilingual and multi-format training, model scaling, and the design of objectives or architectures that better encourage representation sharing.

Abstract: In the training data used by large language models (LLMs), the same latent concept is often presented in multiple distinct ways: the same facts appear in English and Swahili; many functions can be expressed in both Python and Haskell; we can express propositions in both formal and natural language. We show that LLMs can exhibit compartmentalization, where they fail to identify and share statistical strength between distinct presentations of unified concepts. In the worst case, LLMs simply learn parallel internal representations of each presentation of the concept, saturating model capacity with redundancies and decreasing sample efficiency with the number of such presentations. We also demonstrate that synthetic parallel data can fail to improve this despite being easily learned itself. Under this framework, we find that, for small models, early multilingual learning is nearly entirely compartmentalized. Finally, all interventions that we study exhibit a phase transition in which their effectiveness depends on the number of distinct presentations, suggesting that the language modeling objective may only inconsistently unify representations.

</details>


### [16] [Are Rationales Necessary and Sufficient? Tuning LLMs for Explainable Misinformation Detection](https://arxiv.org/abs/2605.19285)
*Bing Wang,Rui Miao,Ximing Li,Chen Shen,Shaotian Yan,Changchun Li,Kaiyuan Liu,Xiaosong Yuan,Jieping Ye*

Main category: cs.CL

TL;DR: The paper proposes LONSREX, a data synthesis pipeline that locates necessary and sufficient rationales for explainable misinformation detection using large language models, overcoming limitations of naive label-based filtering.


<details>
  <summary>Details</summary>
Motivation: Misinformation spreads rapidly on social media, and existing misinformation detection models are black-box and/or provide low-quality explanations. Current explainable MD mainly relies on prompting off-the-shelf LLMs and then naively filtering outputs based only on label correctness, which leads to rationales that are either insufficient (too shallow to truly justify decisions) or unnecessary and verbose (due to over-verification). There is a need for a principled way to obtain high-quality, concise, and faithful rationales for training specialized explainable MD models.

Method: The authors build a pipeline that first collects large-scale fact-checked articles and uses multiple strong LLMs to generate veracity labels and associated rationales. Instead of filtering training data only by whether the predicted labels are correct, they introduce LONSREX, a novel data synthesis pipeline. LONSREX defines a metric to quantify how much each verification step in a rationale contributes to the final prediction, thereby assessing both its necessity and sufficiency. Using this metric, the pipeline identifies and retains only those rationale segments that are necessary and sufficient, reducing insufficiency and over-verification in the training data, and then fine-tunes a dedicated LLM for explainable misinformation detection on this curated dataset.

Result: Empirical experiments show that models fine-tuned with data produced by LONSREX yield better quality explanations and improved performance on explainable misinformation detection tasks compared to baselines using naive label-correctness filtering and prompt-only approaches. The resulting LLM generates more concise yet adequate rationales that more faithfully support its veracity judgments.

Conclusion: Naive filtering of LLM-generated explanations by label correctness alone is inadequate for building high-quality explainable misinformation detection systems, because it leads to rationales that are either insufficient or unnecessarily long. By explicitly measuring the necessity and sufficiency of each verification step, LONSREX can synthesize superior training data and support fine-tuning a specialized LLM that produces more faithful, concise, and effective rationales for misinformation detection.

Abstract: The rapid spread of misinformation on social media platforms has become a formidable challenge. To mitigate its proliferation, Misinformation Detection (MD) has emerged as a critical research topic. Traditional MD approaches based on small models typically perform binary classification through a black-box process. Recently, the rise of Large Language Models (LLMs) has enabled explainable MD, where models generate rationales that explain their decisions, thereby enhancing transparency. Existing explainable MD methods primarily focus on crafting sophisticated prompts to elicit rationales from off-the-shelf LLMs. In this work, we propose a pipeline to fine-tune a dedicated LLM specifically for explainable MD. Our pipeline begins by collecting large-scale fact-checked articles, and then uses multiple strong LLMs to produce veracity predictions and rationales. To ensure high-quality training data, we leverage a filtering strategy that selects only the correct instances for fine-tuning. While this pipeline is intuitive and prevalent, our experiments reveal that naive filtering based solely on label correctness is insufficient in practice and suffers from two critical limitations: (1) Coarse-grained labels cause insufficient rationales: Rationales filtered solely based on binary labels are insufficient to adequately support their decisions; (2) Over-verification behavior causes unnecessary rationales: Stronger LLMs tend to exhibit over-verification behavior, producing excessively verbose and unnecessary rationales. To address these issues, we introduce LONSREX, a novel data synthesis pipeline to Locate Necessary and Sufficient Rationales for Explainable MD. Specifically, we propose a metric that quantifies the contribution of each verification step to the final prediction, thereby evaluating its necessity and sufficiency. Experimental results demonstrate the effectiveness of LONSREX.

</details>


### [17] [How Do Document Parsers Break? Auditing Structural Vulnerability in Document Intelligence](https://arxiv.org/abs/2605.19309)
*Yue Chen,Yihao Wang,Ziyi Tang,Keze Wang*

Main category: cs.CL

TL;DR: The paper introduces a new, structure-aware robustness auditing framework for document layout analysis systems that moves beyond traditional area-based (footprint) robustness metrics.


<details>
  <summary>Details</summary>
Motivation: Existing robustness evaluations of document layout analysis (DLA) systems focus mainly on the area or footprint of perturbations on a page, assuming that larger affected regions indicate greater vulnerability. However, this area-centric view—termed Footprint Bias—fails to capture how perturbations interact with the underlying document structure and how resulting errors propagate to downstream tasks like retrieval-augmented generation and long-document QA. The paper is motivated by the need for more accurate and diagnostic robustness measures that reflect structural sensitivity and downstream impact.

Method: The authors propose a lightweight, output-level auditing framework that operates on the DLA outputs rather than internal model states. The framework decouples three components: (1) probe construction, which introduces controlled perturbations into documents; (2) policy-driven targeting, which decides where and how to apply probes; and (3) structure-aware diagnosis, which analyzes the DLA outputs. Key technical elements include the Block-level Structural Loss Rate (B-SLR) to quantify structural failures, granularity-aware exposure descriptors that characterize how layout elements are exposed to perturbations, and pathway attribution techniques to trace how specific perturbations interact with layout structures and propagate failures through the pipeline.

Result: On 1,000 pages evaluated with two DLA pipelines, MinerU and PP-StructureV3, the authors find that affected area (the traditional footprint measure) shows only weak correlation with perturbation-induced OCR instability (R^2=0.384 for MinerU and 0.110 for PP-StructureV3). In contrast, the proposed B-SLR metric exhibits much stronger alignment with OCR instability (R^2=0.727 and 0.916, respectively). Exposure descriptors distinguish different failure modes, separating occlusion-dominant from topology-dominant vulnerability pathways. Moreover, small, structurally targeted perturbation probes can degrade downstream QA and retrieval performance to a degree comparable with much larger, indiscriminate perturbations, underscoring the importance of structural factors over raw perturbation size.

Conclusion: The study demonstrates that robustness evaluation for document layout analysis systems should move beyond simple footprint-based stress testing. Structure-aware metrics like B-SLR and exposure descriptors provide more faithful and diagnostic assessments of vulnerability, and they reveal that small but strategically placed structural perturbations can cause severe downstream performance degradation. The proposed auditing framework offers a practical way to identify and analyze these vulnerabilities at the output level, shifting the focus of DLA robustness work toward structure-aware vulnerability auditing and more reliable downstream document intelligence applications.

Abstract: Document Layout Analysis (DLA) pipelines provide structured page representations for retrieval-augmented generation, long-document question answering, and other document intelligence systems, yet their robustness evaluation remains largely area-centric. We identify this Footprint Bias and propose a lightweight output-level auditing framework that decouples probe construction, policy-driven targeting, and structure-aware diagnosis. The framework combines Block-level Structural Loss Rate (B-SLR), granularity-aware exposure descriptors, and pathway attribution to analyze where perturbations interact with layout structure and how failures propagate. Across MinerU and PP-StructureV3 on 1,000 pages, affected area weakly tracks perturbation-induced OCR instability (R^2=0.384/0.110), whereas B-SLR aligns much more closely with it (R^2=0.727/0.916). Exposure descriptors further separate occlusion- and topology-dominant pathways, and small structurally targeted probes cause downstream QA/retrieval degradation comparable to larger-footprint perturbations. These results shift DLA robustness evaluation from footprint-based stress testing toward structure-aware vulnerability auditing.

</details>


### [18] [A Multi-Agent Framework for Feature-Constrained Difficulty Control in Reading Comprehension Item Generation](https://arxiv.org/abs/2605.19316)
*Seonjeong Hwang,Jun Seo,Hyounghun Kim,Gary Geunbae Lee*

Main category: cs.CL

TL;DR: They propose MAFIG, a multi-agent LLM framework that generates reading comprehension items under explicit difficulty-related feature constraints, and show it yields better controlled difficulty than single-agent baselines.


<details>
  <summary>Details</summary>
Motivation: Existing LLM-based methods for generating difficulty-controlled reading comprehension questions usually use a single prompt to control difficulty-related features, but they often fail to satisfy all the specified constraints, leading to questions that don't match the target difficulty. A more reliable mechanism is needed to enforce feature constraints and obtain items with predictable, monotonic difficulty levels.

Method: They design MAFIG, a multi-agent framework in which multiple LLM agents and feature-specific evaluators collaborate. Agents iteratively generate and revise reading comprehension items according to given difficulty-related feature constraints, guided by automatic evaluators that check feature satisfaction. In addition, they propose a procedure to construct a sequence of feature constraint sets such that items generated under later sets are guaranteed to be more difficult, enabling monotonic difficulty control.

Result: Experiments show that MAFIG produces reading comprehension items that meet the specified feature constraints much more often than baseline, single-agent approaches. The framework, combined with their difficulty-calibrated constraint sequence, achieves robust control over item difficulty, yielding items whose difficulty increases monotonically along the designed sequence.

Conclusion: A multi-agent, evaluator-in-the-loop framework like MAFIG can substantially improve the reliability of difficulty-controlled item generation compared to single-prompt methods. By explicitly modeling and calibrating the relationship between feature constraint sets and item difficulty, the approach enables robust, monotonic difficulty control for automatically generated reading comprehension items.

Abstract: Recent studies in difficulty-controlled reading comprehension item generation have leveraged large language models (LLMs) to produce items by adjusting difficulty-related features. However, existing methods typically rely on a single-agent prompting approach, which often fails to consistently satisfy specified feature constraints, resulting in items that deviate from the target difficulty level. To address this limitation, we introduce MAFIG, a Multi-agent Framework for Feature-constrained Item Generation, where multiple LLM agents and feature-specific evaluators collaborate to generate and iteratively revise items based on intended constraints. Furthermore, to verify the efficacy of MAFIG in difficulty control, we propose a method for constructing a sequence of feature constraint sets that yield items with monotonically increasing difficulty. Experimental results demonstrate that MAFIG generates items that adhere to target constraints at a significantly higher rate than baselines, achieving robust difficulty control through the difficulty-calibrated constraint sequence.

</details>


### [19] [HalluWorld: A Controlled Benchmark for Hallucination via Reference World Models](https://arxiv.org/abs/2605.19341)
*Emmy Liu,Varun Gangal,Michael Yu,Zhuofu Tao,Karan Singh,Sachin Kumar,Steven Y. Feng*

Main category: cs.CL

TL;DR: HalluWorld is a unified, controllable benchmark that tests hallucinations of language models across diverse, fully specified worlds, revealing that perception is mostly solved but multi-step reasoning and state tracking are still major failure modes.


<details>
  <summary>Details</summary>
Motivation: Existing hallucination benchmarks are fragmented across tasks (summarization, QA, RAG, agents) and define hallucinations inconsistently, often depending on human annotation, static references prone to memorization, or irreproducible setups. This makes it hard to compare methods and understand the root causes of hallucinations or whether a mitigation generalizes across contexts. The authors aim to create a principled, scalable, and reproducible way to define and measure hallucinations that is not tied to specific applications and that allows systematic probing of different underlying failure modes.

Method: They formalize hallucination via an explicit reference-world formulation: a model hallucinates when it outputs an observable claim that is false with respect to a fully specified world. On top of this, they build HalluWorld, a suite of synthetic and semi-synthetic environments (gridworlds, chess, realistic terminal tasks) where the world state is known exactly, the model’s observations are carefully controlled, and hallucination labels can be generated automatically. The benchmark enables controlled variation along dimensions such as world complexity, observability, temporal dynamics, and conflict between sources, and it breaks hallucinations into fine-grained error categories. They then systematically evaluate both frontier and open-weight language models across all these settings.

Result: Across environments, they find consistent patterns. Frontier models rarely hallucinate about directly observed, perceptual information—these cases are nearly solved. However, they frequently fail at multi-step state tracking and causal forward simulation, even when given opportunities for extended reasoning. In more realistic terminal-based tasks, models additionally perform poorly at deciding when to abstain instead of guessing. The benchmark reveals an uneven failure profile across probe types and domains, indicating structured, domain-dependent weaknesses rather than uniform unreliability.

Conclusion: The study concludes that hallucinations do not stem from a single, monolithic deficiency, but rather from multiple distinct failure modes that manifest differently across tasks and environments. Controlled reference worlds offer a scalable, reproducible way to dissect these modes, systematically measure hallucination behavior, and evaluate mitigations. HalluWorld thus provides a principled foundation for future research on understanding, benchmarking, and reducing hallucinations in modern language models.

Abstract: Hallucination remains a central failure mode of large language models, but existing benchmarks operationalize it inconsistently across summarization, question answering, retrieval-augmented generation, and agentic interaction. This fragmentation makes it unclear whether a mitigation that works in one setting reduces hallucinations across contexts. Current benchmarks either require human annotation and fixed references that may be memorized, or rely on observations in settings that are difficult to reproduce. To study root causes, we introduce HalluWorld, an extensible benchmark grounded in an explicit reference-world formulation: a model hallucinates when it produces an observable claim that is false with respect to this world. Building on this view, we construct synthetic and semi-synthetic environments in which the reference world is fully specified, the model's view is controlled, and hallucination labels are generated automatically. HalluWorld spans gridworlds, chess, and realistic terminal tasks, enabling controlled variation of world complexity, observability, temporal change, and source-conflict policy, and disentangling hallucinations into fine-grained error categories. We evaluate frontier and open-weight language models across these settings and find consistent patterns: perceptual hallucination on directly observed information is near-solved for frontier models, while multi-step state tracking and causal forward simulation remain difficult and are not generally solved by extended thinking. In the terminal setting, models also struggle with when to abstain. The uneven profile of failures across probe types and domains suggests that hallucinations arise from distinct failure modes rather than a single capability. Our results suggest that controlled reference worlds offer a scalable and reproducible path toward measuring and reducing hallucinations in modern language models.

</details>


### [20] [Retrieval-Augmented Linguistic Calibration](https://arxiv.org/abs/2605.19344)
*Yi-Fan Yeh,Linwei Tao,Minjing Dong,Tao Huang,Jialin Yu,Philip Torr,Chang Xu*

Main category: cs.CL

TL;DR: The paper proposes a probabilistic, distribution-based framework for calibrating and evaluating how linguistic expressions like “I think” or “probably” convey confidence, and introduces a retrieval-augmented method to rewrite model answers so their wording better matches calibrated confidence levels.


<details>
  <summary>Details</summary>
Motivation: Existing confidence calibration largely focuses on numeric probabilities, while humans and LLMs often communicate confidence through linguistic cues (e.g., hedges like “probably”). These linguistic expressions are hard to calibrate because multiple cues can co-occur, context changes their meaning, and different readers interpret them differently. There is a need for a principled, general framework that can capture these complexities and align linguistic confidence with actual correctness probabilities.

Method: 1) Model each linguistic confidence expression as a probability distribution over perceived correctness probabilities, explicitly capturing variability in how audiences interpret phrases like “I’m pretty sure.” 2) Define a new evaluation dimension, faithfulness, and introduce Faithfulness Divergence (FD), an information-theoretic measure of how much a revealed ground truth outcome would surprise an audience given the expressed linguistic confidence. 3) Propose Retrieval-Augmented Linguistic Calibration (RALC), a post-hoc pipeline that takes calibrated numeric confidence, retrieves examples or patterns, and rewrites model answers so that their linguistic cues (hedges, modifiers) reflect the calibrated confidence. 4) Evaluate RALC on three QA benchmarks and five LLM families, comparing against black-box and grey-box calibration baselines.

Result: RALC leads to substantial improvements in how well linguistic confidence expressions match true correctness probabilities and in how faithfully they represent calibrated beliefs. Specifically, across three QA benchmarks and five LLM families, the method yields up to 66% improvement in faithfulness (as measured by the proposed FD metric) and up to 58% improvement in calibration relative to existing calibration baselines, outperforming both black-box and grey-box approaches.

Conclusion: Modeling linguistic confidence as distributions over perceived probabilities enables more nuanced and robust calibration of natural language confidence expressions. The proposed Faithfulness Divergence metric offers a principled way to quantify how well these expressions reflect underlying beliefs, and the RALC pipeline effectively injects calibrated confidence back into model outputs through retrieval-augmented rewriting. Together, these contributions significantly improve the alignment between what language models say about their confidence and their actual likelihood of being correct.

Abstract: Linguistic cues such as "I believe" and "probably" offer an intuitive interface for communicating confidence, yet a generalisable, principled calibration framework for linguistic confidence expressions remains underexplored. In particular, co-occurring linguistic cues, contextual variation, and subjective audience interpretation pose unique challenges. We therefore model linguistic confidence as a distribution over plausible perceived probability values that a statement is correct, capturing interpretation variability that scalar representations discard. Within this distributional framework, we introduce faithfulness as a complementary evaluation dimension and present Faithfulness Divergence (FD), an information-theoretic metric quantifying the surprise induced in audience beliefs upon truth revelation. Building on these foundations, we present Retrieval-Augmented Linguistic Calibration (RALC), a lightweight post-hoc pipeline that propagates calibrated confidence signals back into natural language via retrieval-augmented rewriting. Across three QA benchmarks and five LLM families, RALC improves in-domain faithfulness and calibration up to 66% and 58%, respectively, outperforming black-box and grey-box calibration baselines.

</details>


### [21] [Taming the Thinker: Conditional Entropy Shaping for Adaptive LLM Reasoning](https://arxiv.org/abs/2605.19358)
*Shuyu Wei,Jian Sun,Delai Qiu,Yining Wang,Shengping Liu,Jiaen Liang,Ying Fu,Wei Huang,Jitao Sang*

Main category: cs.CL

TL;DR: They propose Conditional Entropy Shaping (CES), a method that dynamically controls token-level entropy in LLM reasoning to get concise answers on easy problems and deeper exploration on hard ones, achieving higher accuracy with shorter responses than DAPO on math benchmarks.


<details>
  <summary>Details</summary>
Motivation: Existing entropy-based deep reasoning methods for LLMs either make responses unnecessarily long or sacrifice accuracy when trying to shorten them. There is a need for a principled way to balance reasoning depth and conciseness, adapting to problem difficulty rather than applying a one-size-fits-all entropy control.

Method: They build on DAPO and introduce Conditional Entropy Shaping (CES), which treats token-level entropy as an uncertainty signal and uses a conditional bidirectional policy. Specifically, CES identifies high-entropy “forking point” tokens along reasoning trajectories, penalizing such tokens when they lie on correct reasoning paths to enforce more deterministic and concise reasoning, and rewarding them when they occur on incorrect paths to encourage exploration and correction. The framework is implemented in training LLMs, particularly DeepSeek-R1-Distill-7B, to modulate entropy differently depending on correctness of local reasoning segments, effectively shaping the model’s token generation behavior based on conditional feedback.

Result: When applied to DeepSeek-R1-Distill-7B and tested on 12 mathematical benchmarks, CES yields consistent improvements in average accuracy compared to DAPO, while simultaneously reducing response length. Additional experiments on a smaller 1.5B-parameter backbone and on out-of-domain benchmarks show similar performance and length trade-off improvements, indicating robustness and generality of the approach.

Conclusion: Conditional Entropy Shaping provides an effective way to control the depth and conciseness of LLM reasoning by manipulating token-level entropy conditionally on correctness. It outperforms prior entropy-based methods like DAPO by achieving better accuracy with shorter responses, and generalizes across model sizes and domains, suggesting it is a promising direction for refining deep reasoning behavior in LLMs.

Abstract: Entropy-based deep reasoning has emerged as a promising direction for improving the reasoning capabilities of Large Language Models (LLMs), but existing methods often either increase response length indiscriminately or shorten responses at the cost of accuracy. To better balance this trade-off, we introduce Conditional Entropy Shaping (CES), a framework that dynamically controls token-level response entropy, enabling LLMs to produce concise solutions on simple problems while encouraging deeper exploration on hard ones. Built on DAPO, CES uses token-level entropy as an uncertainty signal and applies a conditional bidirectional policy: it penalizes high-entropy "forking point" tokens on correct reasoning paths to improve conciseness, and rewards them on incorrect paths to encourage exploration and error correction. We implement CES on DeepSeek-R1-Distill-7B and evaluate it on 12 mathematical benchmarks. CES consistently improves average accuracy while reducing response length relative to DAPO, and supplementary experiments show similar trends on a smaller 1.5B backbone and on out-of-domain benchmarks.

</details>


### [22] [EmbGen: Teaching with Reassembled Corpora](https://arxiv.org/abs/2605.19394)
*Arun K Lenin,Kai Rouse,Andrea Nicastro,Anna Leontjeva*

Main category: cs.CL

TL;DR: The paper proposes EmbGen, a synthetic QA data generation pipeline using embeddings and clustering to better capture complex semantic relations in domain corpora, improving small instruction-tuned models without costly human SFT data.


<details>
  <summary>Details</summary>
Motivation: Collecting high-quality instruction-response data for domain adaptation of small instruction-tuned models is expensive and hard to scale. Existing synthetic data pipelines that use a teacher LLM over a corpus tend to generate homogeneous outputs and fail to model cross-passage or cross-document dependencies, which are crucial in semantically heterogeneous domains. The authors aim to build a more structurally aware synthetic data generator that better exploits corpus semantics under a fixed token budget.

Method: EmbGen first decomposes a domain corpus into entity-description pairs. It then computes embeddings and infers semantic structure via similarity, clustering related entities/descriptions. Using these clusters, the system reassembles content and generates QA pairs through three sampling regimes: (1) proximity sampling within local neighborhoods, (2) intra-cluster sampling within a semantic cluster, and (3) inter-cluster sampling across clusters to capture cross-document relationships. Cluster-specialized system prompts guide a teacher LLM to generate diverse, semantically rich QA examples under a controlled token budget.

Result: Under fixed synthetic token budgets of 5M and 20M on three datasets with different degrees of semantic heterogeneity, EmbGen is compared to EntiGraph, InstructLab, and Knowledge-Instruct. Using lexical overlap metrics, an LLM-as-a-judge rubric, and Binary Accuracy (Factual Accuracy × Completeness), EmbGen delivers large Binary Accuracy gains on the most heterogeneous dataset—12.5% at 5M tokens and 88.9% at 20M tokens over the strongest baseline—while remaining competitive on more homogeneous datasets.

Conclusion: The proposed EmbGen pipeline more effectively captures complex semantic structure and cross-document dependencies in a corpus when generating synthetic QA data, leading to significantly better downstream performance—especially in heterogeneous domains—under constrained token budgets. This suggests that embedding- and cluster-aware synthetic data generation is a promising direction for cost-effective domain adaptation of smaller instruction-tuned models.

Abstract: Adapting small instruction-tuned models to specialized domains often relies on supervised fine-tuning (SFT) on curated instruction-response examples, which is expensive to collect at scale. Synthetic training examples generated by a teacher LLM from a domain corpus can reduce this cost, but existing pipelines can produce homogenized outputs and do not consistently capture cross-passage or cross-document dependencies. We introduce EmbGen, a synthetic data generation pipeline that decomposes a corpus into entity-description pairs, reassembles them using semantic structure inferred from embedding similarity, and then generates question-answer (QA) pairs via proximity, intra-cluster, and inter-cluster sampling with cluster-specialized system prompts. We evaluate EmbGen against EntiGraph, InstructLab and Knowledge-Instruct on three datasets of varied semantic heterogeneity, under fixed token budgets (5 and 20 million tokens). We use lexical overlap metrics, an LLM-as-a-judge rubric, and Binary Accuracy, a composed metric combining Factual Accuracy and Completeness for evaluation. EmbGen improves Binary Accuracy on the most heterogeneous dataset by 12.5% at 5M and 88.9% at 20M tokens budget, relative to the strongest baseline, while remaining competitive across other datasets with lower heterogeneity.

</details>


### [23] [Backtracking When It Strays: Mitigating Dual Exposure Biases in LLM Reasoning Distillation](https://arxiv.org/abs/2605.19433)
*Bing Wang,Shaotian Yan,Chen Shen,kaiyuan liu,Sinan Fan,Ximing Li,Rui Miao,Xiaosong Yuan,Zhanming Shen,Jieping Ye*

Main category: cs.CL

TL;DR: The paper tackles the exposure bias issues in distilling reasoning abilities from large teacher LLMs to smaller students and proposes MOTAB, a new pipeline that monitors student trajectories and selectively backtracks to let the teacher correct errors, improving reasoning performance.


<details>
  <summary>Details</summary>
Motivation: Reasoning with long chain-of-thought in LLMs is powerful but computationally expensive, so smaller models are needed. Distilling reasoning from large to small models is promising, but existing off-policy (teacher-trajectory only) and on-policy (student-trajectory) distillation both suffer from different forms of exposure bias, making long CoT reasoning unreliable. A method is needed that balances student exploration with robust teacher supervision without falling into these biases.

Method: The authors analyze two types of exposure bias: classical exposure bias in off-policy distillation (student never trained on its own errorful contexts) and a reversed exposure bias in on-policy distillation (teacher must guide from sub-optimal, student-generated contexts). They propose MOTAB (Monitoring Trajectories and Backtracking when it strays), which monitors the student's on-policy generation relative to an adaptive safety boundary. As long as the student's trajectory remains within this boundary, it continues autonomously, tolerating small mistakes. Once the student's generation deviates beyond the threshold, the system backtracks to the most recent "safe" state and calls on the teacher to intervene, providing corrected guidance. Training thus uses mixed trajectories with controlled exposure to both student and teacher contexts while avoiding very poor student states for teacher conditioning.

Result: On LIMO-v2 and AceReason reasoning benchmarks, MOTAB reduces dual exposure bias and achieves around a 3% average improvement over baseline reasoning distillation methods. The experiments show that MOTAB is more robust for long CoT reasoning and yields better student performance without increasing model size.

Conclusion: MOTAB offers a principled way to distill reasoning from strong LLMs into smaller ones by dynamically monitoring and correcting student trajectories, addressing both conventional exposure bias and the newly characterized reversed exposure bias. This hybrid on/off-policy training improves long chain-of-thought reasoning performance and makes compact models more viable for practical deployment.

Abstract: Large language models (LLMs) have achieved remarkable success in complex reasoning tasks via long chain-of-thought (CoT), yet their immense computational overhead hinders real-world deployment. LLM reasoning distillation addresses this by transferring reasoning capabilities from formidable teacher models to compact student models. However, existing distillation paradigms face a fundamental dilemma. Typical off-policy distillation strictly utilizes teacher-generated golden trajectories, suffering from an exposure bias due to the mismatch between training distributions and student-generated inference contexts, which leads to error cascades in long CoT reasoning. To address this, on-policy distillation allows students to explore their own trajectories, but we demonstrate that it inherently introduces a reciprocal reversed exposure bias: the teacher model also struggles to provide positive guidance when conditioned on student-generated sub-optimal contexts. To resolve this dual exposure biases problem, we propose Monitoring Trajectories and Backtracking when it strays (MOTAB), a new LLM reasoning distillation pipeline. Specifically, MOTAB dynamically monitors the student's on-policy generation against an adaptive safety boundary. When the generation strays and exceeds this threshold, MOTAB backtracks to the last safe state and leverages teacher intervention to correct the course. This approach inherently tolerates minor student errors to mitigate exposure bias, while preventing sub-optimal contexts to circumvent reversed exposure bias. Extensive experiments on the LIMO-v2 and AceReason datasets demonstrate that MOTAB effectively alleviates the dual exposure biases, yielding a roughly 3% average performance improvement in reasoning tasks.

</details>


### [24] [Drifting Objectives for Refining Discrete Diffusion Language Models](https://arxiv.org/abs/2605.19470)
*Daisuke Oba,Hiroki Furuta,Naoaki Okazaki*

Main category: cs.CL

TL;DR: The paper introduces TokenDrift, a training objective that adapts continuous drifting methods to discrete diffusion language models, significantly improving text generation quality at fixed sampling steps.


<details>
  <summary>Details</summary>
Motivation: Discrete diffusion language models generate text via iterative denoising but require many sampling steps (NFEs) to reach high quality. Recent work in continuous diffusion shows that some sampling-time correction (drifting) can be absorbed into training via anti-symmetric fixed-point objectives, improving efficiency. There is a need to transfer these advantages to discrete models, but discrete tokens are non-differentiable and do not naturally support continuous drifting, leaving a gap in training techniques for DDLMs.

Method: The authors propose TokenDrift, a drifting objective for DDLMs. They map categorical predictions to soft-token feature representations and operate in a frozen semantic feature space. Anti-symmetric drifting is applied in this continuous space, creating a refined feature target. This target is used with stop-gradient and backpropagated to the model’s logits, effectively training the DDLM to internalize aspects of the drift that would otherwise be done only at sampling time. They evaluate this in controlled continual-training setups using masked and uniform-state diffusion backbones.

Result: In continual-training experiments, TokenDrift improves generation quality for a fixed number of function evaluations (NFEs) compared to matched continuation baselines. Specifically, it substantially reduces generative perplexity (Gen.-PPL) at 4 NFEs: by 89% on the MDLM backbone and by 86% on the DUO backbone, demonstrating that the model learns to produce higher-quality text with few denoising steps.

Conclusion: TokenDrift successfully transfers anti-symmetric drifting ideas from continuous to discrete diffusion language models by operating in a soft, frozen semantic feature space. This enables a practical refinement objective that improves the efficiency–quality trade-off of DDLMs at low NFEs, indicating that integrating drift-like corrections into training is a promising direction for better and faster discrete diffusion text generators.

Abstract: Discrete diffusion language models (DDLMs) generate text by iteratively denoising categorical token sequences, while recent drifting methods for continuous generators suggest that part of this sampling-time correction can instead be absorbed into training through an anti-symmetric fixed-point objective. We study how to transfer this principle to DDLMs, where the main challenge is the interface with discrete text: hard token samples are non-differentiable, and categorical predictions do not directly provide continuous samples to drift. We formulate TokenDrift, a drifting objective that lifts categorical predictions to soft-token features, applies anti-symmetric drifting in a frozen semantic space, and backpropagates the resulting stop-gradient feature target to DDLM logits. In controlled continual-training experiments with masked and uniform-state diffusion backbones, TokenDrift improves fixed-NFE generation quality over matched continuation baselines, reducing Gen.-PPL at 4 NFEs by 89% on MDLM and 86% on DUO. These results suggest that drifting can provide a practical refinement objective for DDLMs.

</details>


### [25] [Base Models Look Human To AI Detectors](https://arxiv.org/abs/2605.19516)
*Yixuan Even Xu,Ziqian Zhong,Aditi Raghunathan,Fei Fang,J. Zico Kolter*

Main category: cs.CL

TL;DR: The paper studies how commercial AI-text detectors behave on outputs from different models, finds that they often label base-model text as human but instruction-tuned text as AI, and proposes an iterative paraphrasing method (HIP) that makes model outputs more likely to be classified as human while preserving meaning.


<details>
  <summary>Details</summary>
Motivation: As AI-generated text becomes common in real-world settings like education and academic integrity checks, institutions increasingly rely on commercial AI-text detectors. However, it is unclear what these detectors truly detect, how they behave across model types, and whether their judgments align with any robust notion of machine-generated text. The authors want to understand detector behavior, identify systematic biases (e.g., against instruction-tuned models), and explore whether these detectors can be circumvented while preserving semantics, which has implications for fairness, reliability, and detector design.

Method: The authors empirically evaluate commercial AI-text detectors (specifically GPTZero and Pangram) on text generated by both base language models and their instruction-tuned counterparts. Observing that base-model outputs are often classified as human while instruction-tuned outputs are not, they build on this to design Humanization by Iterative Paraphrasing (HIP). HIP minimally fine-tunes a base model into a paraphraser and applies it iteratively to generated text, aiming to gradually increase the likelihood of being labeled human while preserving original meaning. They test HIP across different model families (Llama-3 and Qwen-3, sizes 0.6B–70B) and compare against baseline paraphrasing/obfuscation approaches, assessing trade-offs between semantic preservation and detector evasion.

Result: The experiments show that commercial detectors like GPTZero and Pangram frequently classify outputs from base models as human while flagging instruction-tuned models’ outputs as AI-generated. HIP systematically increases the human-likeness scores assigned by these detectors across Llama-3 and Qwen-3 families, from small (0.6B) to large (70B) models. Compared with baseline methods, HIP achieves a better balance: it more effectively evades detection while maintaining closer semantic similarity to the original text.

Conclusion: Current commercial AI-text detectors appear to rely heavily on artifacts linked to instruction tuning and local textual cues, rather than capturing a stable, model-agnostic signature of machine-generated text. The proposed HIP pipeline demonstrates that relatively light, iterative paraphrasing with base models can significantly reduce detectability under these systems, raising concerns about the robustness and reliability of detector-based AI-text governance. The authors argue that future detector designs should explicitly model instruction-tuning effects and contextual factors if they aim to provide meaningful and less manipulable detection of AI-generated text.

Abstract: As AI-generated text enters the real-world at scale, institutions increasingly use commercial AI-text detectors, especially in education and academic-integrity workflows. We report a surprising empirical finding about such systems: when evaluated by GPTZero and Pangram, generated text from base models is often judged overwhelmingly human, whereas text generated by their instruction-tuned counterparts is not. Building on this observation, we propose Humanization by Iterative Paraphrasing (HIP), a detector-agnostic pipeline that minimally fine-tunes a base model into a paraphraser and applies it iteratively. Compared with the baselines we test, HIP yields a stronger trade-off between semantic preservation and detector evasion on commercial detectors. Across Llama-3 and Qwen-3 families, spanning model sizes from 0.6B to 70B, HIP consistently improves detector human-likeness. Our findings suggest that current detectors are tracking artifacts of instruction tuning and local context more than any invariant notion of machine-generated text. This, in turn, calls for detector designs that model these factors more explicitly.

</details>


### [26] [Investigating Cross-Modal Skill Injection: Scenarios, Methods, and Hyperparameters](https://arxiv.org/abs/2605.19523)
*Zhiyu Xu,Lean Wang,Yuanxin Liu,Lei Li,Hao Zhou,Fandong Meng,Jie Zhou,Xu Sun*

Main category: cs.CL

TL;DR: The paper studies how to efficiently inject domain-specific skills from language-only models into vision-language models using model merging instead of expensive fine-tuning, and systematically analyzes when and how this works best.


<details>
  <summary>Details</summary>
Motivation: Vision-Language Models are strong generalists but weak at rapidly acquiring new, domain-specific, and continually evolving skills. Standard approaches like supervised fine-tuning need lots of curated data and compute. Model merging is an appealing, low-cost alternative that can transfer knowledge from specialized LLMs to VLMs, but prior work has not systematically examined how to do cross-modal skill injection, when it helps, and what methods and hyperparameters matter.

Method: The authors treat the problem as cross-modal skill injection via model merging: they integrate a domain-expert LLM into a base VLM without additional training. They systematically compare different merging scenarios (e.g., instruction-following, cross-lingual, math reasoning), multiple merging algorithms (e.g., TA, DARE vs other methods), and perform a quantitative study on key hyperparameters needed by the stronger methods. They then evaluate resulting merged VLMs across benchmarks reflecting those scenarios.

Result: They find that cross-modal skill injection via merging is effective for instruction-following and cross-lingual tasks but performs poorly for mathematical reasoning. Among merging approaches, classic techniques such as Task Arithmetic (TA) and DARE consistently outperform alternative methods. They also show that performance is highly sensitive to hyperparameter choices and provide a detailed quantitative characterization of this sensitivity.

Conclusion: Cross-modal model merging is a promising, efficient way to inject domain-specific skills from LLMs into VLMs without extra data or heavy fine-tuning, but its success depends on the task type and careful tuning of merging hyperparameters. It works well for instruction-following and cross-lingual skills, less so for math reasoning, and classic methods like TA and DARE remain strong baselines when properly tuned.

Abstract: Vision-Language Models (VLMs) have demonstrated remarkable proficiency in general multi-modal understanding; yet they struggle to efficiently acquire continually evolving domain-specific skills. Conventional approaches to enhancing VLM capabilities, such as Supervised Fine-Tuning (SFT), require extensive dataset curation and substantial computational resources. Model merging has emerged as an efficient alternative that enables the transfer of domain-specific expertise from Large Language Models (LLMs) to VLMs without incurring additional training data requirements or significant computational overhead. Unlike conventional merging of homogeneous LLMs, which mainly aggregates existing capabilities, cross-modal skill injection aims to induce emergent cross-modal capabilities by integrating a domain-expert LLM into a VLM. However, existing research lacks a systematic analysis of the applicability and methodology of cross-modal skill injection. In this study, we investigate cross-modal skill injection across three main aspects: scenarios, methods, and hyperparameters. For scenarios, we find that cross-modal skill injection generally performs well in instruction-following and cross-lingual settings, yet struggles with mathematical reasoning. For methods, we find that classic approaches such as TA and DARE consistently achieve superior performance over alternative merging methods. We also provide a systematic and quantitative analysis of the hyperparameter tuning that these classic methods critically depend on.

</details>


### [27] [A Data-Driven Approach to Idiomaticity Based on Experts' Criteria in Theoretical Linguistics](https://arxiv.org/abs/2605.19575)
*Elena Mikhalkova,Anastasiya Vishnyakova,Anastasiya Drozdova,Polina Gavin,Aleksander Zhmykhov,Timofey Protasov*

Main category: cs.CL

TL;DR: The paper analyzes 286 multi-word expressions against 16 idiomaticity-related criteria and finds there is no such thing as a fully, absolutely idiomatic expression.


<details>
  <summary>Details</summary>
Motivation: To empirically test and refine the theoretical notion of idiomaticity by checking how real multi-word expressions match up with criteria proposed in linguistic theory.

Method: The authors compiled 286 multi-word expressions from theoretical literature and had linguistic experts annotate each expression according to 16 lexical, grammatical, and other idiomaticity-related criteria, then examined the distribution and interactions of these criteria.

Result: No expression satisfied all idiomaticity criteria; lexical criteria were the strongest predictors of idiomaticity, grammatical criteria applied only under specific conditions, and archaic/obsolete words or structures affected whether an expression could be replaced by a single word.

Conclusion: Idiomaticity is gradient rather than absolute; lexical factors play a more central role than grammatical ones, which are condition-dependent, and the presence of obsolete elements constrains the possibility of substituting a multi-word expression with a single lexical item.

Abstract: The article observes data analysis of 286 multi-word expressions (MWEs) based on 16 lexical, grammatical and other criteria described in theoretical books and papers on the notion of idiomaticity. MWEs were collected from the same theoretical sources, and a set of experts in linguistics annotated them with these categories. The distribution of categories shows that there are no absolutely idiomatic expressions. Lexical criteria seem to be the most influential; grammatical criteria are bound to certain conditions; presence of obsolete words and grammar influence ability of an MWE to be replaced with one word.

</details>


### [28] [GoLongRL: Capability-Oriented Long Context Reinforcement Learning with Multitask Alignment](https://arxiv.org/abs/2605.19577)
*Minxuan Lv,Tiehua Mei,Tanlong Du,Junmin Chen,Zhenpeng Su,Ziyang Chen,Ziqi Wang,Zhennan Wu,Ruotong Pan,jian Liang,Ruiming Tang,Han Li*

Main category: cs.CL

TL;DR: GoLongRL is an open-source, capability-oriented RL recipe for long-context reinforcement learning with verifiable rewards that improves long-context performance through a diverse dataset and a new multitask optimization method (TMN-Reweight).


<details>
  <summary>Details</summary>
Motivation: Existing long-context RL approaches over-focus on complex retrieval paths and yield homogeneous tasks and oversimplified rewards that do not align well with real long-context usage. There is a need for broader task coverage, realistic reward definitions, and open resources to advance long-context capabilities in a reproducible way.

Method: 1) Construct and fully open-source a capability-oriented RLVR dataset (23K samples across 9 long-context task types) using both curated open-source corpora and synthetic QA pairs from real long documents, each paired with its natural evaluation metric, and train models under a vanilla GRPO setup. 2) Propose TMN-Reweight, a heterogeneous multitask optimization scheme that applies task-level mean normalization to align reward scales and difficulty-adaptive weighting to improve advantage estimation across tasks with different reward characteristics.

Result: Under identical vanilla GRPO training, the new dataset alone outperforms the closed-source QwenLong-L1.5 dataset. A Qwen3-30B-A3B model trained on this dataset achieves long-context performance comparable to much larger or more advanced models such as DeepSeek-R1-0528 and Qwen3-235B-A22B-Thinking-2507. TMN-Reweight further boosts average performance beyond vanilla GRPO while maintaining or improving general capabilities.

Conclusion: Capability-diverse, reward-heterogeneous data and an appropriate multitask optimization strategy are crucial for improving long-context RL performance. GoLongRL demonstrates that an open, capability-oriented RLVR dataset and the TMN-Reweight method can deliver competitive long-context results against larger proprietary models, while preserving or enhancing overall capabilities.

Abstract: We present GoLongRL, a fully open-source, capability-oriented post-training recipe for long-context reinforcement learning with verifiable rewards (RLVR). Existing long-context RL methods often treat data construction as a matter of designing increasingly complex retrieval paths, leading to homogeneous task coverage and reward formulations that inadequately reflect practical long-context requirements. Our work offers two contributions. (1) Capability-oriented data construction with full open release. We openly release a dataset of 23K RLVR samples, the complete construction pipeline, and all training code. Guided by a taxonomy of long-context capabilities, the dataset spans 9 task types, each paired with its natural evaluation metric. It comprises curated open-source samples from established corpora and synthetic samples whose QA pairs are generated from real source documents such as books, academic papers, and multi-turn dialogues. Under the same vanilla GRPO setup, our dataset alone outperforms the closed-source QwenLong-L1.5 dataset. Moreover, our Qwen3-30B-A3B model trained on this data delivers long-context performance comparable to DeepSeek-R1-0528 and Qwen3-235B-A22B-Thinking-2507, suggesting that broader coverage and greater reward diversity substantially benefit long-context capability improvement. (2) TMN-Reweight for heterogeneous multitask optimization. To address optimization challenges from heterogeneous rewards, we propose TMN-Reweight, which combines task-level mean normalization for cross-task reward scale alignment with difficulty-adaptive weighting for more reliable advantage estimation. TMN-Reweight further improves average performance over vanilla GRPO, with general capabilities preserved or improved across reported evaluations.

</details>


### [29] [LLMEval-Logic: A Solver-Verified Chinese Benchmark for Logical Reasoning of LLMs with Adversarial Hardening](https://arxiv.org/abs/2605.19597)
*Ming Zhang,Qiyuan Peng,Yinxi Wei,Yujiong Shen,Kexin Tan,Yuhui Wang,Zhenghao Xiang,Junjie Ye,Zhangyue Yin,Zhiheng Xi,Shihan Dou,Tao Gui,Maxm Pan,Ruizhi Yang,Qi Zhang,Xuanjing Huang*

Main category: cs.CL

TL;DR: LLMEval-Logic is a new, challenging Chinese logical reasoning benchmark for LLMs, based on realistic scenarios and rigorously audited formal annotations.


<details>
  <summary>Details</summary>
Motivation: Existing logical reasoning benchmarks often rely on templated data, have weak or unaudited formal semantics, and are already saturated by current LLMs, making them poor indicators of real logical reasoning capability. There is a need for a more realistic, formally-verified, and challenging benchmark, especially in Chinese, to better measure and stress-test LLM reasoning.

Method: The authors build LLMEval-Logic, a benchmark of natural-language Chinese logical reasoning problems derived from realistic situational scenarios. They forward-author the problems with their intended formalizations, have experts audit both the natural-language items and their logical forms, and verify labeled answers via the Z3 theorem prover. They construct detailed expert rubrics for grading natural-to-formal translation, and employ a closed-loop adversarial process to harden selected items against strong models. The final benchmark consists of two subsets: a Base set of 246 items with 1,400 rubric atoms, and a Hard set of 190 items that includes 938 multi-step sub-questions restricted to closed model spaces.

Result: The benchmark comprises two publicly released subsets (Base and Hard) with rich formal annotations and grading rubrics. When evaluating 14 frontier LLMs, the best model attains only 37.5% accuracy on Hard items, and even with access to reference symbols, the best joint Z3+Rubric score reaches only 60.16%, indicating that the tasks are far from solved.

Conclusion: LLMEval-Logic exposes significant gaps in the logical reasoning and formalization abilities of current LLMs, especially in realistic Chinese scenarios. Its carefully curated, auditor-verified, and adversarially-hardened design makes it a valuable, challenging benchmark for future model development and evaluation in natural-language logical reasoning.

Abstract: Evaluating large language models (LLMs) on natural-language logical reasoning is essential because rule-governed tasks require conclusions to follow strictly from stated premises. Many existing logical-reasoning benchmarks are generated by templating natural-language items from sampled formulas, provide only coarse or unaudited formal annotations, and are now quickly saturated by frontier reasoning models. We present LLMEval-Logic, a Chinese logical reasoning benchmark built from realistic situational scenarios. Its pipeline forward-authors and expert-audits natural-language items together with their reference formalizations, verifies annotated answers with Z3, constructs expert rubrics for natural-to-formal grading, and hardens selected items through a closed-loop adversarial workflow. The benchmark is released in two paired subsets: a 246-item Base subset shipped with 1,400 expert-developed rubric atoms, and a 190-item Hard subset with 938 multi-step sub-questions over closed model spaces. Evaluating 14 frontier LLMs on LLMEval-Logic reveals substantial gaps in current models: the best model reaches only 37.5% Hard Item Accuracy, and even with reference symbols the highest joint Z3+Rubric formalization score among evaluated models reaches only 60.16%. Our benchmark is publicly available at https://github.com/llmeval/LLMEval-Logic.

</details>


### [30] [optimize_anything: A Universal API for Optimizing any Text Parameter](https://arxiv.org/abs/2605.19633)
*Lakshya A Agrawal,Donghyun Lee,Shangyin Tan,Wenjie Ma,Karim Elmaaroufi,Rohit Sandadi,Sanjit A. Seshia,Koushik Sen,Dan Klein,Ion Stoica,Joseph E. Gonzalez,Omar Khattab,Alexandros G. Dimakis,Matei Zaharia*

Main category: cs.CL

TL;DR: The paper presents a single LLM-driven text-optimization framework that achieves state-of-the-art performance across multiple, very different optimization domains by framing them all as text artifacts scored by evaluators.


<details>
  <summary>Details</summary>
Motivation: Existing optimization methods are typically domain-specific: different tools and algorithms are used for program synthesis, scheduling, architecture search, packing problems, etc. This fragmentation makes it hard to build a unified system that can tackle many tasks, transfer knowledge across them, and generalize to new instances. With the rise of powerful LLMs, the authors ask whether one LLM-based optimization system can rival or surpass specialized tools across fundamentally different domains, if problems are uniformly cast as improving text according to a scoring function.

Method: They design an AI-based optimization system that treats candidate solutions as text artifacts and iteratively improves them using an LLM-based search process guided by a numeric scoring function. The framework supports (1) single-task search, (2) multi-task search where solutions to different but related problems are optimized jointly, enabling cross-task transfer, and (3) generalization to unseen inputs. Across tasks, they plug in different scoring/evaluation procedures but keep the same LLM-driven search mechanism. They also conduct ablation studies across three domains to isolate the contributions of actionable side information (more structured feedback than a bare score) and multi-task versus independent optimization under fixed computational budgets.

Result: Across six diverse benchmarks, the single system achieves state-of-the-art or highly competitive performance: it discovers agent architectures for the ARC-AGI benchmark that raise Gemini Flash’s accuracy from 32.5% to 89.5%; it finds scheduling algorithms that reduce cloud costs by 40%; it generates CUDA kernels where 87% of them match or outperform PyTorch baselines; and it surpasses AlphaEvolve’s published result on a circle packing problem with n=26. The ablation experiments show that providing actionable side information allows faster convergence and higher ultimate performance compared to using only scalar scores, and that multi-task optimization consistently outperforms running independent optimizers per task under the same total budget, with the advantage increasing as the number of related tasks grows.

Conclusion: Framing heterogeneous optimization problems as text optimization and solving them with a unified LLM-based search procedure is an effective, general-purpose problem-solving paradigm. It can match or outperform specialized, domain-specific algorithms across very different applications and benefits significantly from richer feedback and cross-task transfer in multi-task search. The authors release the optimize_anything system within the open-source GEPA project, positioning it as a general framework and toolkit for LLM-centric optimization across domains.

Abstract: Can a single LLM-based optimization system match specialized tools across fundamentally different domains? We show that when optimization problems are formulated as improving a text artifact evaluated by a scoring function, a single AI-based optimization system-supporting single-task search, multi-task search with cross-problem transfer, and generalization to unseen inputs-achieves state-of-the-art results across six diverse tasks. Our system discovers agent architectures that nearly triple Gemini Flash's ARC-AGI accuracy (32.5% to 89.5%), finds scheduling algorithms that cut cloud costs by 40%, generates CUDA kernels where 87% match or beat PyTorch, and outperforms AlphaEvolve's reported circle packing solution (n=26). Ablations across three domains reveal that actionable side information yields faster convergence and substantially higher final scores than score-only feedback, and that multi-task search outperforms independent optimization given equivalent per-problem budget through cross-task transfer, with benefits scaling with the number of related tasks. Together, we show for the first time that text optimization with LLM-based search is a general-purpose problem-solving paradigm, unifying tasks traditionally requiring domain-specific algorithms under a single framework. We open-source optimize\_anything with support for multiple backends as part of the GEPA project at https://github.com/gepa-ai/gepa .

</details>


### [31] [K-Quantization and its Impact on Output Performance](https://arxiv.org/abs/2605.19645)
*Robin Baki Davidsson,Pierre Nugues*

Main category: cs.CL

TL;DR: The paper studies how different levels of quantization (2- to 8-bit) affect the performance of eight LLMs on several benchmarks, finding that higher precision improves performance with diminishing returns, while aggressive quantization is often acceptable but sometimes harmful, especially for smaller models.


<details>
  <summary>Details</summary>
Motivation: LLMs achieve strong performance but are very large and expensive to deploy. Quantization is a common compression technique, but its exact effects on LLM accuracy across bit-widths, models, and tasks are not fully characterized. The paper aims to fill this gap to inform practical deployment decisions.

Method: The authors evaluate eight different LLMs at several quantization levels, from aggressive 2-bit (Q2_K) up to 8-bit (Q8_0). They benchmark these models on MMLU-Pro for knowledge and reasoning, CRUXEval for code comprehension, and MuSR for reading comprehension, then compare how performance varies with precision, model size, and task type.

Result: Higher precision quantization (e.g., 8-bit) consistently yields better performance, but with diminishing gains as precision increases. Aggressive 2-bit quantization often preserves acceptable accuracy, but some models suffer large performance drops. The performance degradation from lower bit-widths depends on model architecture, size, and benchmark. Larger LLMs tend to be more robust to aggressive quantization, though they can still incur noticeable losses at very low precision.

Conclusion: Quantization bit-width, model size, and task type interact in non-trivial ways. Lower bit-widths generally reduce accuracy, but the severity of impact varies by model and benchmark. Larger models are more resilient to aggressive quantization, while mid-sized models (7–9B parameters) provide a practical balance between efficiency and performance. These insights help practitioners choose appropriate quantization settings and model sizes for deployment under resource constraints.

Abstract: Recent advancements in large language models (LLMs) have shown their remarkable capacities in many NLP tasks. However, their substantial size often presents challenges for deployment. This necessitates efficient techniques for model compression, with quantization emerging as a prominent solution. Despite its benefits, the exact impact of quantization (from 2- to 6-bit) on the performance and accuracy of LLMs remains an active area of research. This paper investigates the performance of eight LLMs at various quantization levels, focusing on tasks such as MMLU-Pro for knowledge processing and reasoning, CRUXEval for code comprehension, and MuSR for reading comprehension. Our results show a consistent trend where higher precision (e.g., 8-bit Q8\_0) yields improved performance, albeit with diminishing returns. Aggressive quantization (e.g., 2-bit Q2\_K) usually retains acceptable accuracy, though some models show a substantial loss in performance. Our findings indicate that while lower bit precision generally reduces performance, the impact varies across models and tasks. Larger models show greater resilience to aggressive quantization, but can still undergo significant drops at lower precision levels. Mid-sized models in the 7-9 billion parameter range strike an optimal balance between efficiency and resource usage. Such results provide insights into the trade-offs between model size, quantization, and performance.

</details>


### [32] [Can Large Language Models Reliably Correct Errors in Low-Resource ASR? A Contamination-Aware Case Study on West Frisian](https://arxiv.org/abs/2605.19711)
*Yun Hao,Reihaneh Amooie,Wietse de Vries,Rik van Noord,Martijn Wieling*

Main category: cs.CL

TL;DR: The paper studies how well large language models can post-edit and correct automatic speech recognition output for a low-resource language (Frisian), rigorously checking that gains are not due to training data contamination.


<details>
  <summary>Details</summary>
Motivation: ASR still performs poorly for low-resource languages because training data is scarce. Recent work suggests that LLMs can correct ASR errors via generative error correction, but this has mostly been tested on high-resource languages and without careful controls for whether the LLM has seen the test text during training (data contamination). The authors want to know if GER truly helps ASR in a genuinely low-resource setting, and whether apparent gains are real corrections or artifacts of contamination.

Method: They apply LLM-based generative error correction to ASR transcripts for Frisian. Besides using an existing public Frisian corpus, they build an additional offline evaluation dataset from non-public texts to minimize the risk that LLMs have seen this material during pretraining. They compare word error rates before and after correction, including against oracle WERs, and conduct detailed error analysis to categorize and understand the types of corrections that the models perform.

Result: GER with LLMs improves ASR performance in most experimental configurations. The best GPT-5.1 based system even beats oracle WER, indicating strong corrective capability. Similar improvements are observed on the non-public offline dataset, suggesting that the observed gains are not primarily driven by data contamination. Error analyses reveal systematic correction patterns in the model outputs.

Conclusion: LLM-based generative error correction is effective for improving ASR in a low-resource language (Frisian), and the improvements persist even when potential training data contamination is controlled, implying genuine correction ability rather than memorization. The detailed error analysis enhances understanding of how and when such models correct ASR output, supporting their use as a promising post-processing step for low-resource ASR systems.

Abstract: Automatic speech recognition (ASR) has improved substantially in recent years, yet performance remains limited for low-resource languages. Large language models (LLMs) have shown promise for improving ASR through generative error correction (GER), but their effectiveness in low-resource settings remains underexplored. In addition, it remains unclear to what extent data contamination influences the reported improvements in LLM-based GER. This study investigates LLM-based GER for low-resource Frisian. In addition to a public corpus, we construct and use a Frisian offline dataset with non-public texts for evaluation to control for potential data contamination. Results show that GER improves ASR performance in most settings, with the best GPT-5.1 results surpassing oracle WERs. Comparable gains on the offline dataset indicate that improvements reflect true correction ability. We further provide a detailed error analysis revealing model correction patterns.

</details>


### [33] [LLM-Based Financial Sentiment Analysis in Arabic: Evidence from Saudi Markets](https://arxiv.org/abs/2605.19714)
*Mona H. Albaqawi,Eman M. Albalkhi,Joud A. Albaiti,Enrico Lopedoto*

Main category: cs.CL

TL;DR: This paper introduces an Arabic NLP framework for large-scale financial sentiment analysis focused on the Saudi market, integrating news and social media to build an 84K-sample, company-linked, sentiment-annotated corpus.


<details>
  <summary>Details</summary>
Motivation: Investor sentiment strongly influences financial markets, but Arabic financial sentiment analysis is underdeveloped because of the language’s complexity, domain-specific vocabulary, and limited labeled data, especially for the Saudi market. There is a need for a scalable, systematic way to capture both institutional (news) and public (social media) sentiment in Arabic and link it to specific companies and market behavior.

Method: The authors design a multi-stage pipeline to construct a large Arabic financial corpus. It collects official financial news and social media posts, then performs cleaning, deduplication, and entity linking that combines a transformer-based NER model with a curated lexicon of Saudi-listed companies to map mentions to canonical identifiers. Sentiment is annotated with a five-class label scheme (e.g., strongly negative to strongly positive). The final dataset enables aggregation of sentiment at the company level and temporal analysis versus stock movements on the Saudi Exchange. Experiments evaluate the reliability and scalability of the sentiment analysis across this corpus.

Result: The pipeline yields a sizable, structured Arabic financial sentiment dataset of 84K samples, each linked to specific companies and labeled with fine-grained sentiment. Empirical evaluation shows that the framework can reliably perform large-scale sentiment analysis, capturing meaningful sentiment dynamics related to stock market behavior on the Saudi Exchange.

Conclusion: The proposed framework provides an effective solution for Arabic financial sentiment analysis in the Saudi context, addressing resource scarcity and entity linking challenges. By integrating news and social media and producing a large, company-linked, sentiment-annotated corpus, it enables robust measurement of investor sentiment and its relationship to market behavior, and can serve as a foundation for further research and applications in Arabic financial NLP.

Abstract: Investor sentiment shapes financial markets, yet modeling sentiment in Arabic financial contexts remains challenging due to linguistic complexity and limited resources. We present an Arabic NLP framework for large-scale financial sentiment analysis tailored to the Saudi market, integrating official financial news and social media to capture institutional and public investor sentiment. The framework constructs a large Arabic financial corpus through a multi-stage pipeline encompassing data collection, cleaning, deduplication, entity linking, and sentiment annotation. Transformer-based NER combined with a curated company lexicon links textual mentions to canonical company identifiers, with sentiment labels assigned using a five-class scheme. The resulting dataset of 84K samples supports company-level sentiment aggregation and analysis of sentiment dynamics relative to stock market behavior on the Saudi Exchange. Experimental results demonstrate reliable and scalable Arabic financial sentiment analysis.

</details>


### [34] [Mathematical Reasoning in Large Language Models: Benchmarks, Architectures, Evaluation, and Open Challenges](https://arxiv.org/abs/2605.19723)
*Husnain Amjad,Raja Khurram Shahzad,Aamir Shahzad,Mehwish Fatima*

Main category: cs.CL

TL;DR: Survey of how large language models perform mathematical reasoning, covering data, model designs, training, and evaluation, and highlighting progress, limitations, and future directions.


<details>
  <summary>Details</summary>
Motivation: Mathematical reasoning is a key capability for AI and an important benchmark in education, science, and industry. With LLMs rapidly improving, it is necessary to systematically understand how well they handle mathematical reasoning, what methods are used, where they fail, and how to evaluate them reliably.

Method: Conduct a systematic survey of ~120 peer‑reviewed papers and preprints on mathematical reasoning with LLMs. Build a unified taxonomy of mathematical datasets; analyze architectures and training strategies such as tool integration, verifier‑guided reasoning, and parameter‑efficient methods; and compare evaluation metrics for answers and reasoning processes.

Result: The survey organizes existing work into a coherent framework: a taxonomy of datasets by role and reasoning difficulty; a structured comparison of reasoning architectures and training methods and their impact on robustness and generalization; and an analysis of evaluation metrics that exposes gaps between answer accuracy and verification of reasoning steps. It identifies common failure modes including unfaithful reasoning, benchmark bias, and poor generalization.

Conclusion: Current LLM-based mathematical reasoning systems show notable progress but suffer from recurring issues in faithfulness, benchmark design, and generalization. More reliable symbolic grounding, better evaluation protocols—especially for process-level reasoning—and targeted research on robustness and trustworthiness are needed to advance the field.

Abstract: Mathematical reasoning is essential for problem-solving in education, science, and industry, serving as a crucial benchmark for evaluating artificial intelligence systems. As Large Language Models (LLMs) improve their reasoning capabilities, understanding how well they perform mathematical reasoning has become increasingly important. This survey synthesizes recent advancements in mathematical reasoning with LLMs through a structured analysis of datasets, architectures, training strategies, and evaluation protocols. Our systematic review encompasses approximately 120 peer-reviewed studies and preprints, examining the evolution of this research area and providing a unified analytical framework to understand current progress and limitations. Our study particularly introduces a unified taxonomy of mathematical datasets, distinguishing between pretraining corpora, supervised fine-tuning resources, and evaluation benchmarks across varying levels of reasoning complexity. A systematic analysis of reasoning architectures and training strategies, including tool integration, verifier-guided reasoning, and parameter-efficient adaptation, is presented to assess their effects on reasoning robustness and generalization. Moreover, a comparative evaluation of existing metrics highlights the gap between final-answer accuracy and process-level reasoning verification. By synthesizing insights across these areas, our analysis identifies recurring failure modes, such as reasoning faithfulness issues, benchmark biases, and generalization limitations, and outlines key research directions toward improving symbolic grounding, evaluation reliability, and the development of more robust and trustworthy LLM-based reasoning systems.

</details>


### [35] [ContextRAG: Extraction-Free Hierarchical Graph Construction for Retrieval-Augmented Generation](https://arxiv.org/abs/2605.19735)
*Roman Prosvirnin,Sergei Kuznetsov,Seungmin Jin*

Main category: cs.CL

TL;DR: ContextRAG is a graph-based RAG system that avoids expensive LLM-based entity/relation extraction by building a fuzzy concept graph directly from embeddings, achieving competitive multi-hop QA performance with vastly fewer LLM calls and tokens.


<details>
  <summary>Details</summary>
Motivation: Existing graph-structured RAG methods improve multi-hop QA but depend heavily on LLM calls during indexing to extract entities, relations, and summaries. This causes token usage and wall-clock time to scale poorly with corpus size, limiting practicality. The paper aims to design a graph RAG framework that preserves multi-hop reasoning benefits while drastically reducing dependence on LLMs for graph construction.

Method: The authors introduce ContextRAG, which constructs a fuzzy concept graph over chunk embeddings without LLM-based extraction. They use residual-quantization k-means to cluster embedding space and Formal Concept Analysis (FCA) with Lukasiewicz residuated logic to define fuzzy concepts. Graph topology emerges via soft fuzzy join and meet operations that induce bridge-like and meet-derived context nodes instead of LLM-authored edges. Only a small, fixed number of LLM calls are used overall, e.g., for answering queries rather than for large-scale indexing.

Result: On a 130-task UltraDomain subset, ContextRAG builds its index with just 30 LLM calls and 22,073 tokens. A local reproduction of HiRAG, a prior graph RAG system, needed 870 indexing calls and 3.54M tokens on only a 20-task subset and failed to complete graph construction; extrapolation to 130 tasks suggests over 23M tokens. In terms of QA performance, ContextRAG achieves 33.6% F1 overall and 36.8% F1 on multi-hop tasks. An activation analysis reveals that queries retrieving at least one lattice-derived node in the top five results gain 3.9 F1 points compared to those that do not, though this is correlational rather than causal.

Conclusion: ContextRAG demonstrates that graph-structured RAG for multi-hop QA can be implemented with a fuzzy concept lattice built from embeddings, avoiding LLM-heavy entity and relation extraction during indexing. This design yields large savings in token and compute costs while delivering reasonable F1 performance, particularly on multi-hop tasks. The analysis of lattice-derived node activations suggests these nodes are a useful diagnostic signal for successful retrieval, pointing toward further refinements in fuzzy concept graph construction and utilization.

Abstract: Graph-structured retrieval-augmented generation (RAG) systems can improve answer quality on multi-hop questions, but many current systems rely on large language models (LLMs) to extract entities, relations, and summaries during indexing. These calls add token and wall-clock costs that grow with corpus size. We present ContextRAG, a graph RAG system whose graph topology is constructed without LLM-based entity or relation extraction. ContextRAG derives a fuzzy concept graph over chunk embeddings using residual-quantization k-means and Formal Concept Analysis with Lukasiewicz residuated logic. Bridge-like and meet-derived context nodes are induced by soft fuzzy join and meet operations, rather than by LLM-written graph edges. On a 130-task UltraDomain subset, ContextRAG builds its index with 30 LLM calls and 22,073 tokens. In contrast, a local HiRAG reproduction stress test required 870 indexing calls and 3.54M tokens on a 20-task subset before failing during graph construction; linear extrapolation to 130 tasks implies over 23M indexing tokens. ContextRAG obtains 33.6% F1 overall and 36.8% F1 on multi-hop tasks. An activation analysis shows that queries retrieving at least one lattice-derived node in the top five achieve +3.9 percentage points F1 over queries that do not; this association is diagnostic rather than causal.

</details>


### [36] [TERGAD: Structure-Aware Text-Enhanced Representations for Graph Anomaly Detection](https://arxiv.org/abs/2605.19738)
*Wen Shi,Zhe Wang,Huafei Huang,Qing Qing,Ziqi Xu,Qixin Zhang,Xikun Zhang,Renqiang Luo,Feng Xia*

Main category: cs.CL

TL;DR: TERGAD is a structure-aware, text-enhanced framework for graph anomaly detection that uses LLMs to convert node topology into semantic descriptions, fuse them with attributes via a gated autoencoder, and detect anomalies by reconstruction error.


<details>
  <summary>Details</summary>
Motivation: Existing graph anomaly detection methods with text mostly use raw textual node features and underuse structural context, making them weak at detecting anomalies that arise from mismatches between what a node is (its content) and how it is positioned in the graph (its topology). The authors aim to better capture this content–structure inconsistency by encoding structural semantics in a richer, language-based form.

Method: TERGAD augments graph data by (1) converting each node’s topological properties into natural-language descriptions, (2) feeding these descriptions into a large language model to obtain high-level semantic embeddings, and (3) fusing these LLM-based structural embeddings with original node attributes using a gated dual-branch autoencoder that jointly reconstructs the graph structure and node features. The gating mechanism adaptively balances the contribution of structural semantics and original attributes.

Result: On six real-world datasets, TERGAD achieves consistently better anomaly detection performance than state-of-the-art baselines. Ablation experiments show that both the structural semantic guidance (via LLM-generated embeddings) and the gated fusion mechanism are critical contributors to the performance gains.

Conclusion: Encoding graph structure as natural-language descriptions and leveraging LLMs to derive structural semantic embeddings significantly improves graph anomaly detection, especially for anomalies due to content–structure inconsistencies. TERGAD’s gated dual-branch autoencoder effectively integrates these semantics with raw attributes, and empirical results confirm its superiority and the necessity of its key components.

Abstract: Graph Anomaly Detection (GAD) aims to identify atypical graph entities, such as nodes, edges, or substructures, that deviate significantly from the majority. While existing text-rich approaches typically integrate structural context into the data representation pipeline using raw textual features, they often neglect the structural context of nodes. This limitation hinders their ability to detect sophisticated anomalies arising from inconsistencies between a node's inherent content and its topological role. To bridge this gap, we propose TERGAD (Structure-aware Text-enhanced Representations for Graph Anomaly Detection), A novel data augmentation framework that enriches structural semantics for GAD via the semantic reasoning capabilities of Large Language Models (LLMs). Specifically, TERGAD translates node-level topological properties into descriptive natural language narratives, which are subsequently processed by an LLM to derive high-level semantic embeddings. These embeddings are then adaptively fused with original node attributes through a gated dual-branch autoencoder to jointly reconstruct both graph structure and node features. The anomaly score is computed based on the integrated reconstruction error, effectively capturing deviations in both observable attributes and LLM-informed semantic expectations. Extensive experiments on six real-world datasets demonstrate that TERGAD consistently outperforms state-of-the-art baselines. Furthermore, our ablation studies validate the indispensable role of structural semantic guidance and the efficacy of the gated fusion mechanism. Code is available at https://github.com/Kantorakitty/TERGAD-main.

</details>


### [37] [Synthesis and Evaluation of Long-term History-aware Medical Dialogue](https://arxiv.org/abs/2605.19766)
*Hebin Hu,Renke Dai,Ah-Hwee Tan,Yilin Kang*

Main category: cs.CL

TL;DR: The paper presents MediLongChat, a synthetic benchmark of long-term medical dialogues generated by LLMs to evaluate healthcare agents’ ability to recall and reason over longitudinal patient histories.


<details>
  <summary>Details</summary>
Motivation: Current medical dialogue datasets either rely on privacy-sensitive real clinical text or focus on single, isolated encounters. This makes it difficult to evaluate whether healthcare agents can handle long-term, cross-visit reasoning that is critical in real-world care. The authors aim to create a realistic yet privacy-safe benchmark for longitudinal, multi-session medical dialogues.

Method: They propose a knowledge-guided three-stage synthesis pipeline using LLMs: (1) construct synthetic patient profiles with varied diseases and complication trajectories; (2) generate multi-turn dialogues for each clinical encounter; (3) stitch these into coherent long-term histories, forming the MediLongChat dataset. They then define three benchmark tasks—In-dialogue Reasoning, Cross-dialogue Reasoning, and Synthesis Reasoning—to probe memory and reasoning. For quality control, they design a multi-dimensional evaluation framework with automatic metrics (Faithfulness, Coherence, Diversity) plus LLM-as-a-judge assessments (Correctness and Realism).

Result: The authors successfully construct MediLongChat and the associated benchmark tasks and metrics. When they test strong, state-of-the-art LLMs on these tasks, the models perform poorly, especially on cross-dialogue and synthesis reasoning, revealing significant weaknesses in long-term memory and reasoning over longitudinal medical histories.

Conclusion: MediLongChat provides a challenging, privacy-preserving benchmark for evaluating long-term reasoning in healthcare agents. The weak performance of current LLMs demonstrates both the benchmark’s difficulty and the need for specialized methods and architectures to improve memory, reasoning, and robustness in longitudinal clinical dialogue settings.

Abstract: An effective healthcare agent must be able to recall and reason over a patient's longitudinal medical history. However, the absence of datasets with realistic long-term dialogue timelines limits systematic evaluation. Real clinical text is constrained by privacy and ethics, while existing benchmarks focus on isolated interactions, failing to capture cross-session reasoning. We introduce a framework for synthesizing high-quality, long-term medical dialogues with LLMs. Our approach entails a knowledge-guided decomposition into three stages: constructing synthetic patient profiles with diverse disease and complication trajectories, generating multi-turn dialogues per encounter, and integrating them into a coherent longitudinal history dataset, MediLongChat. We establish three benchmark tasks-In-dialogue Reasoning, Cross-dialogue Reasoning, and Synthesis Reasoning-to evaluate the memory capabilities of healthcare agents. To assess data quality, we introduce a multi-dimensional evaluation framework combining vector-based metrics with LLM-as-a-judge assessments. Specifically, we define automatic measures-Faithfulness, Coherence, and Diversity-together with two LLM-based evaluations: Correctness and Realism. Benchmark experiments show that even state-of-the-art LLMs struggle with MediLongChat. These findings highlight the benchmark's applicability and underscore the need for tailored methods to advance healthcare agents.

</details>


### [38] [Towards Trust Calibration in Socially Interactive Agents: Investigating Gendered Multimodal Behaviors Generation with LLMs](https://arxiv.org/abs/2605.19798)
*Lucie Galland,Chloé Clavel,Magalie Ochs*

Main category: cs.CL

TL;DR: The paper studies whether LLMs can generate multimodal agent behaviors that express different levels of trust-related traits (ability and benevolence) and how users perceive them.


<details>
  <summary>Details</summary>
Motivation: As socially interactive agents become common, users often miscalibrate trust—overtrusting or undertrusting an agent’s real capabilities. Designers need systematic ways to shape agents’ behavior so that users’ perceived trustworthiness (e.g., ability, benevolence) matches what the agent can actually do. Existing work has focused on unimodal cues (like text or voice alone) and lacks an automatic, scalable method to generate coherent, multimodal trust cues. This paper aims to fill that gap and to test whether LLMs can be used as a generative engine for such behaviors.

Method: The authors use a large language model (GPT‑5.4) to automatically generate multimodal interaction scripts for SIAs. Prompts specify different levels of two trustworthiness dimensions: ability and benevolence, and may also specify gender. The LLM outputs coordinated behaviors across modalities (text content, vocal intonation, facial expressions, gestures). These outputs are compiled into a multimodal dataset. The authors then extract features from each modality and apply Random Forest models with feature importance analysis to see which behavioral cues differentiate high vs. low ability and benevolence, and whether these match theory. They also run a within-subjects user study on Prolific, where participants view or experience the generated behaviors and rate perceived ability and benevolence.

Result: GPT‑5.4 can generate coherent multimodal behaviors that systematically vary with the requested levels of ability and benevolence. Random Forest feature importance indicates that the discriminative cues the model uses are consistent with theoretical expectations for these trust dimensions (e.g., certain linguistic, prosodic, facial, and gestural patterns). However, when gender is included in the prompts, the LLM’s behaviors reveal stereotypical patterns: male agents’ behaviors are more strongly associated with high ability, and female agents’ behaviors with high benevolence. The user study confirms that humans perceive differences in ability and benevolence across generated conditions that align with the intended prompt instructions.

Conclusion: LLMs—here, GPT‑5.4—can serve as effective engines for generating multimodal agent behaviors that express graded levels of trust-related traits like ability and benevolence. The automatically generated behaviors are coherent, theoretically grounded, and interpretable by human users as intended. At the same time, the system reproduces societal gender stereotypes when gender is specified, highlighting a critical fairness and bias concern. Overall, the work provides an initial framework for using LLMs to support trust calibration in socially interactive agents, while emphasizing the need for careful bias mitigation in real-world deployments.

Abstract: As Socially Interactive Agents (SIAs) become increasingly integrated into daily life, the ability to calibrate user trust to an agent's actual capabilities would help ensure appropriate usage of these agents. In this paper, we explore the capacity of Large Language Models (LLMs) to generate multimodal behaviors (verbal, vocal, gestural, and facial expression modalities) that reflect varying levels of ability and benevolence, two key dimensions of trustworthiness. We propose a novel method for automatically generating behaviors aligned with specific levels of these traits, a first step towards enabling nuanced and trust-calibrated interactions. By analyzing a large dataset of multimodal transcripts generated by LLMs, we demonstrate that GPT-5.4 is able to produce coherent behavior across different modalities (text, intonation, facial expression, and gesture). Using Random Forest feature importance analysis, we show that the generated behaviors align with theoretical expectations for ability and benevolence. However, we also find that when gender is specified in the prompt, LLMs tend to reproduce societal gender stereotypes, associating male agents' behaviors with high ability and female agents' behaviors with high benevolence. To validate our approach, we conducted a user study on Prolific using a within-subjects design. Participants perceived different levels of ability and benevolence in the generated behaviors align with the intended instructions.

</details>


### [39] [Chunking German Legal Code](https://arxiv.org/abs/2605.19806)
*Max Prior,Natalia Milanova,Andreas Schultz*

Main category: cs.CL

TL;DR: The paper evaluates different text chunking strategies for retrieval-augmented generation over German civil law and finds that simple, structure-aligned chunks (sections, subsections) outperform more complex semantic methods in both recall and efficiency.


<details>
  <summary>Details</summary>
Motivation: To understand how best to segment legal texts for retrieval-augmented generation, especially in a highly structured corpus like the German Civil Code, and to determine whether sophisticated semantic chunking justifies its computational cost compared to simpler, structure-based approaches.

Method: Use the German Civil Code as a benchmark corpus and apply various chunking strategies: structural units (sections, subsections, sentences, propositions), fixed-size windows, contextual chunking, semantic clustering, Lumber-style chunking, and RAPTOR-based hierarchical retrieval. Evaluate each on a legal QA dataset with section-level gold labels, measuring retrieval recall, query latency, index build time, and storage footprint.

Result: Chunking strategies that follow the existing legal structure, particularly section- and subsection-based chunking, achieve the highest recall and are computationally efficient. More complex semantic and LLM-intensive approaches that ignore or override this structure perform worse in recall and incur higher latency, build time, and storage costs.

Conclusion: For legal RAG on statutory codes, preserving the domain’s inherent structural units is crucial. Simple, structure-aligned chunking yields better retrieval effectiveness and efficiency than complex semantic chunkers, underscoring a trade-off between semantic enrichment and operational cost and arguing for structure-preserving designs in legal IR systems.

Abstract: This paper investigates chunking strategies for retrieval-augmented generation on German statutory law, using the German Civil Code as a structured benchmark corpus. We implement and compare a range of segmentation approaches, including structural units (sections, subsections, sentences, propositions), fixed-size windows, contextual chunking, semantic clustering, Lumber-style chunking, and RAPTOR-based hierarchical retrieval. All methods are evaluated on a legal question-answering dataset with section-level gold labels, measuring recall, query latency, index build time, and storage requirements. Results show that chunking strategies aligned with the inherent legal structure - particularly section and subsection - based retrieval-achieve the highest recall, while more complex approaches that override this structure perform worse. These simpler methods also offer favorable computational efficiency compared to LLM-intensive techniques such as contextual chunking, RAPTOR, and Lumber. The findings highlight a key trade-off between semantic enrichment and operational cost, and demonstrate that preserving domain-specific structure is critical for effective legal information retrieval.

</details>


### [40] [Rethinking How to Remember: Beyond Atomic Facts in Lifelong LLM Agent Memory](https://arxiv.org/abs/2605.19952)
*Jingwei Sun,Jianing Zhu,Jiangchao Yao,Tongliang Liu,Bo Han*

Main category: cs.CL

TL;DR: TriMem is a three-level memory system for LLM agents (raw segments, atomic facts, synthesized profiles) with automatically optimized prompts that improves long-term dialogue reasoning vs prior fact-only memories.


<details>
  <summary>Details</summary>
Motivation: Existing long-term memory systems for LLM agents mostly extract atomic facts from dialogues via static, hand-designed prompts, then store and retrieve these facts for future reasoning. This fact-only, prompt-engineered approach loses fine-grained conversational context, struggles with deep reasoning over dispersed information, and suffers from inconsistent extraction granularity across different dialogue styles. There is a need for a memory architecture that preserves raw dialogue fidelity, supports efficient retrieval, and enables higher-level semantic reasoning, while also adapting its extraction prompts over time without retraining model parameters.

Method: The authors introduce TriMem, a memory architecture with three coexisting representation levels: (1) raw dialogue segments tagged with source identifiers to preserve full context; (2) extracted atomic facts for compact, efficient retrieval; and (3) synthesized profiles that integrate scattered facts into higher-level semantic summaries suitable for deep reasoning. They further employ TextGrad-based prompt optimization, where prompts for fact extraction and profile synthesis are iteratively refined using feedback signals derived from response quality. This enables continuous improvement of the memory extraction pipeline without updating model weights.

Result: On two long-context interaction benchmarks, LoCoMo and PerLTQA, and across multiple LLM backbones, TriMem yields consistently better performance than strong memory baselines that rely largely on atomic-fact extraction and retrieval. The experiments demonstrate gains in both retrieval effectiveness and downstream reasoning quality in long-term dialogue scenarios.

Conclusion: TriMem’s tri-level memory representation, combined with gradient-style prompt optimization, addresses key limitations of existing fact-centric memory systems by preserving dialogue fidelity, enabling efficient retrieval, and supporting deeper semantic reasoning. Its parameter-free, lifelong prompt evolution allows it to generalize across LLM backbones and dialogue styles, delivering state-of-the-art performance on long-term conversational benchmarks.

Abstract: To enable reliable long-term interaction, LLM agents require a memory system that can faithfully store, efficiently retrieve, and deeply reason over accumulated dialogue history. Most existing methods adopt an extracted fact based paradigm: handcrafted static prompts compress raw dialogues into atomic facts, which are then stored, matched, and injected into downstream reasoning. Nevertheless, such fact-centric designs inevitably discard fine-grained details in original dialogues and fail to support deep reasoning over scattered isolated facts. Moreover, static prompts cannot maintain consistent extraction granularity across diverse dialogue styles. To address these limitations, we propose TriMem, which maintains three coexisting representation granularities, including raw dialogue segments anchored by source identifiers for storage fidelity, extracted atomic facts for efficient memory retrieval, synthesized profiles that aggregate dispersed facts into holistic semantic understanding for deep reasoning. We further adopt TextGrad-based prompt optimization, which iteratively refines extraction and profiling prompts via response quality feedback, achieving lifelong evolution without any parameter updating. Extensive experiments on LoCoMo and PerLTQA across multiple LLM backbones demonstrate that TriMem consistently outperforms strong memory baselines. The code is available at https://TMLR-TriMem.github.io .

</details>


### [41] [Language Mutations Sustain the Persistences of Conspiracy Theories on Social Media](https://arxiv.org/abs/2605.20050)
*Calvin Yixiang Cheng,Dorian Quelle,Scott A. Hale*

Main category: cs.CL

TL;DR: The paper studies how changes in wording (“language mutations”) influence how long conspiracy theories continue to spread on social media, finding that more and specific types of mutations make conspiracies last longer.


<details>
  <summary>Details</summary>
Motivation: To understand why some conspiracy theories persist and spread on social media for long periods, despite moderation efforts, by examining the role of language changes over time in making these claims more resilient and harder to detect or counter.

Method: The authors use a three-year dataset of conspiracy-related posts from X (formerly Twitter). They apply computational linguistic analysis to measure semantic and psycholinguistic changes (“mutations”) in how conspiracy claims are expressed over time, and use survival modelling to relate these mutations to the lifespan of conspiracy claims. They also perform qualitative analysis to categorize typical mutation patterns.

Result: Conspiracy claims that undergo more semantic mutations tend to have significantly longer lifespans on social media. Changes in specific psycholinguistic features—such as pronoun use, social reference terms, cognitive process words, and risk- or health-related vocabularies—are linked to extended persistence. Structural mutations in actor, action, and target (AAT) categories also predict longer lifespans. Qualitative analysis reveals that two main mutation patterns, simplification and assimilation, occur at both linguistic and AAT levels.

Conclusion: Language mutations, both in wording and in the actor–action–target structure, play a key role in the persistence of conspiracy theories online. These findings suggest that content moderation should move beyond static, keyword-based approaches and instead account for the mutability of core conspiracy claims, designing strategies that target the underlying core narratives and their predictable variations over time.

Abstract: This study investigates how language mutations affect the persistent diffusion of conspiracy theories on social media. Drawing on a three-year dataset of conspiracy-related posts from X, and applying computational linguistic analysis alongside survival modelling, we find that conspiracy claims with greater semantic mutations have substantially longer lifespans. Mutations in psycholinguistic properties, including pronouns, social reference words, cognitive process terms, risk- and health- related vocabularies, are associated with extended lifespans. Mutations in actor, action and target (AAT) categories are associated with longer lifespans as well. Qualitative analysis identifies two predominant mutation patterns: simplification and assimilation, at both linguistic and AAT structural levels. Taken together, the results advance our understanding of how language mutations contribute to conspiracy persistence online and shed lights on longitudinal content moderation strategies. We argue that content moderation should consider the mutability of conspiracy claims and focus on the core claims that can address their potential variations.

</details>


### [42] [Rewarding Beliefs, Not Actions: Consistency-Guided Credit Assignment for Long-Horizon Agents](https://arxiv.org/abs/2605.20061)
*Wenjie Tang,Minne Li,Sijie Huang,Liquan Xiao,Yuan Zhou*

Main category: cs.CL

TL;DR: The paper introduces ReBel, a reinforcement learning algorithm that learns and uses explicit belief states to improve long-horizon LLM agents in partially observable environments, achieving better success rates and sample efficiency than existing baselines.


<details>
  <summary>Details</summary>
Motivation: In long-horizon interactive tasks with partial observability, LLM agents struggle because they only see incomplete information at each step; their internal beliefs about the environment drift over time, and rewards often arrive late. This makes it hard to assign credit to intermediate actions and to learn stable, effective policies. Existing RL-from-feedback methods typically operate at the episode level and do not explicitly model or train the agent’s belief state, limiting their performance and reliability.

Method: ReBel (Reward Belief) is a process-level RL algorithm that explicitly represents structured belief states to summarize the interaction history. It introduces belief-consistency supervision: the model predicts beliefs about future observations or feedback, and discrepancies between these predictions and actual feedback are converted into dense self-supervised learning signals, without needing external step-wise labels or verifiers. Additionally, it uses belief-aware trajectory grouping, where trajectories with similar belief states are compared to compute more robust, lower-variance advantage estimates, thereby improving policy optimization under partial observability.

Result: On long-horizon, partially observable benchmarks such as ALFWorld and WebShop, ReBel outperforms the episode-level baseline GRPO. It improves task success rates by up to 20.4 percentage points and achieves a 2.1x gain in sample efficiency, demonstrating more effective and data-efficient learning for LLM agents.

Conclusion: Belief-aware self-supervision—using explicit, trained belief states together with consistency-based signals and belief-aware trajectory comparisons—is an effective way to tackle partial observability and temporal credit assignment in long-horizon RL for LLM agents. ReBel’s empirical gains suggest this is a promising direction for building more reliable, efficient decision-making systems in complex interactive environments.

Abstract: Reinforcement learning from verifiable rewards (RLVR) is a promising paradigm for improving large language model (LLM) agents on long-horizon interactive tasks. However, in partially observable environments, incomplete observations cause agent beliefs to drift over time, while delayed rewards obscure the causal impact of intermediate decisions, exacerbating temporal credit assignment challenges. To address this, we propose ReBel (Reward Belief), a process-level reinforcement learning algorithm that explicitly models structured belief states to summarize interaction history and guide subsequent policy learning. ReBel introduces belief-consistency supervision, converting discrepancies between predicted beliefs and observed feedback into dense self-supervised signals without requiring external step-wise annotations or verifiers. It also employs belief-aware grouping to compare trajectories under similar belief states, yielding more robust and lower-variance advantage estimates. We evaluate ReBel on challenging long-horizon benchmarks, including ALFWorld and WebShop. ReBel improves task success by up to $20.4$ percentage points over the episode-level baseline GRPO and increases sample efficiency by $2.1\times$. These results suggest that belief-aware self-supervision is a promising direction for reliable long-horizon decision-making under partial observability. Code is available at: https://github.com/Fateyetian/Rebel.git.

</details>


### [43] [CopT: Contrastive On-Policy Thinking with Continuous Spaces for General and Agentic Reasoning](https://arxiv.org/abs/2605.20075)
*Dachuan Shi,Hanlin Zhu,Xiangchi Yuan,Wanjia Zhao,Kejing Xia,Wen Xiao,Wenke Lee*

Main category: cs.CL

TL;DR: CopT reverses the standard “think-then-answer” CoT pipeline by answering first and then thinking only when needed, using KL-based confidence estimators from contrastive embeddings to decide when and how to reflect, leading to higher accuracy and fewer tokens without extra training.


<details>
  <summary>Details</summary>
Motivation: Standard chain-of-thought forces models to generate long reasoning traces before giving answers, which can be wasteful in tokens and latency, and does not distinguish between genuine reasoning and performative reasoning where the model already knows the answer. The authors want a mechanism that exposes quick draft answers while still maintaining reliability via selective, targeted reasoning and a principled way to measure answer uncertainty.

Method: They propose CopT, a pipeline where the model first generates a draft answer and then conditionally engages in further reasoning. To assess whether the draft is trustworthy, they compare model probabilities for the answer under discrete-token vs continuous-embedding inputs, treating embeddings as inference-time contrastive verifiers and deriving a sequence-level reverse KL estimator as a reliability score. They prove that, under assumptions, this estimator equals the mutual information between an unresolved latent state and the answer token, capturing answer-relevant uncertainty. If the draft is unreliable, CopT triggers additional on-policy thinking; a second KL-based estimator controls how much of the draft answer is visible during reflection to leverage partial information while reducing bias from incorrect content.

Result: On math, coding, and agentic reasoning benchmarks, CopT improves peak accuracy by as much as 23% and cuts token usage by up to 57% compared with standard CoT, at equal or better accuracy, and requires no additional training of the base model.

Conclusion: Answer-first reasoning with principled uncertainty estimation can outperform traditional think-first CoT: CopT enables LLMs to respond quickly with draft answers, selectively invoke deeper reasoning only when needed, and do so using information-theoretic KL-based verifiers that focus on answer-relevant uncertainty, yielding better efficiency–accuracy trade-offs without retraining.

Abstract: Chain-of-thought (CoT) is a standard approach for eliciting reasoning capabilities from large language models (LLMs). However, the common CoT paradigm treats thinking as a prerequisite for answering, which can delay access to plausible answers and incur unnecessary token costs even when the model is able to identify an answer before extended thinking, a behavior known as performative reasoning. In this paper, we introduce CopT, a reformulated reasoning pipeline that reverses the usual order of thinking and answering. Instead of thinking before answering, CopT first elicits a draft answer and then invokes subsequent on-policy thinking conditioned on its own draft answer for reflection and correction. To assess whether the draft answer should be trusted, CopT recasts continuous embeddings as inference-time contrastive verifiers. Specifically, it contrasts the model's support for the same generated tokens under discrete-token inputs and continuous-embedding inputs, yielding a sequence-level reverse KL estimator for answer reliability. Our analysis shows that under certain assumptions, the expected estimate equals the mutual information between the unresolved latent state and the emitted answer token, explaining why it captures answer-relevant uncertainty rather than arbitrary uncertainty in the latent state. When the answer is deemed insufficiently reliable, CopT performs further on-policy thinking, where a second KL estimator dynamically controls draft-answer visibility, preserving useful partial information while reducing the risk of being misled by unreliable content. Across mathematics, coding, and agentic reasoning tasks, CopT improves peak accuracy by up to 23% and reduces token usage by up to 57% at comparable or higher accuracy, without any additional training. The code is available at https://github.com/sdc17/CopT.

</details>


### [44] [BalanceRAG: Joint Risk Calibration for Cascaded Retrieval-Augmented Generation](https://arxiv.org/abs/2605.20084)
*Zijun Jia,Yuanchang Ye,Sen Jia,Yiyao Qian,Haoning Wang,Baojie Chen,Diyin Tang,Jinsong Yu,Zhiyuan Wang*

Main category: cs.CL

TL;DR: BalanceRAG is a calibrated cascaded RAG framework that jointly tunes LLM-only and RAG uncertainty thresholds to satisfy target risk levels while maximizing coverage and minimizing unnecessary retrieval.


<details>
  <summary>Details</summary>
Motivation: Standard RAG applies retrieval to all queries, wasting resources when the base LLM is already confident and correct. Cascaded RAG improves efficiency by only invoking RAG when the LLM appears uncertain, but existing stage-wise calibration is conservative and does not optimally trade off risk, coverage, and retrieval cost because it ignores the joint effect of thresholds across both branches. There is a need for a principled way to jointly calibrate uncertainty thresholds for LLM-only answers, RAG fallbacks, and abstention, under explicit risk constraints and possibly multiple objectives (e.g., error rate and retrieval budget).

Method: The paper proposes BalanceRAG, which models each pair of uncertainty thresholds for the LLM-only and RAG branches as an operating point on a 2D lattice. Using uncertainty scores from both branches, BalanceRAG performs sequential graphical multiple testing over this lattice to identify threshold pairs that are statistically certified to satisfy a given target risk (error-rate) level at the system level. This yields a set of "safe" operating points and enables risk-adaptive calibration: selecting thresholds that maintain the desired error rate while maximizing coverage. The framework is further extended to multi-risk calibration, simultaneously controlling both selection-conditioned risk (error among accepted answers) and retrieval usage (frequency of RAG calls).

Result: On three open-domain QA benchmarks and with multiple LLM backbones, BalanceRAG achieves the specified risk levels while attaining higher coverage and more correct accepted answers compared with baselines that calibrate cascades stage-wise. It also reduces unnecessary retrieval invocations relative to an always-on RAG setup, demonstrating better efficiency–accuracy trade-offs.

Conclusion: Joint calibration of uncertainty thresholds for LLM-only and RAG branches provides a more effective way to operate cascaded RAG systems under explicit risk constraints. By viewing threshold pairs as operating points on a 2D lattice and using sequential graphical testing, BalanceRAG can guarantee target risk levels, improve coverage and correctness, and reduce retrieval costs. The method naturally extends to multi-objective settings where both error and retrieval usage are simultaneously controlled.

Abstract: Large language models (LLMs) can enhance factuality via retrieval-augmented generation (RAG), but applying RAG to every query is unnecessary when the model-only answer is reliable. This motivates cascaded RAG: each query is first handled by an LLM-only branch, escalated to a RAG fallback only if the primary branch is uncertain, and abstained from when neither branch is sufficiently trustworthy. However, calibrating such cascades stage by stage may be conservative, since the final utility depends on joint uncertainty thresholding of LLM-only and RAG. In this work, we develop BalanceRAG to certify threshold pairs at a target risk level. Given uncertainty scores from the two branches, BalanceRAG frames each threshold pair as an operating point on a two-dimensional lattice and identifies safe operating points using sequential graphical testing. This enables risk-adaptive threshold calibration, controlling the system-level error rate among accepted points, while retaining more examples. Furthermore, BalanceRAG extends to multi-risk calibration, allowing retrieval usage to be bounded together with the selection-conditioned risk. Experiments on three open-domain question answering (QA) benchmarks across multiple LLM backbones demonstrate that BalanceRAG meets prescribed risk levels, preserves higher coverage and more accepted correct examples, and reduces unnecessary retrieval calls compared with always-on RAG.

</details>


### [45] [MixRea: Benchmarking Explicit-Implicit Reasoning in Large Language Models](https://arxiv.org/abs/2605.20128)
*Yuanqing Cai,Ziyi Huang,Minhao Liu,Lixin Duan,Wen Li,Yanru Zhang*

Main category: cs.CL

TL;DR: The paper shows that large language models display a form of inattentional blindness: they often miss subtle but crucial contextual cues, even when given explicit instructions, and proposes a prompting method to alleviate this.


<details>
  <summary>Details</summary>
Motivation: As LLMs are deployed in high-stakes decision-making, it is critical to know whether they systematically overlook important but subtle contextual information, similar to human inattentional blindness. Existing benchmarks do not directly test this failure mode, so the authors aim to characterize and measure it, and explore mitigation strategies.

Method: They introduce a new task, explicit-implicit reasoning, and construct MixRea, a benchmark of 2,246 multiple-choice questions spanning 9 reasoning types with different mixes of explicit and implicit information. They evaluate 21 advanced LLMs on this benchmark to quantify an "inattentional blindness" effect, focusing on consistency when explicit instructions conflict with overlooked cues. They then design Potential Relation Completion Prompting (PRCP), a prompting scheme to surface and complete potentially missing causal or relational links before answering, and test its impact on performance. They also extend analysis to diverse multi-source reasoning tasks to see if the limitation generalizes.

Result: On MixRea, even the strongest tested reasoning model (Gemini 2.5 Pro) reaches only 42.8% consistency, indicating that models often fail to integrate subtle contextual information under explicit instructions. This pattern is observed broadly across 21 LLMs, demonstrating widespread inattentional blindness. Applying PRCP improves reasoning performance by helping models recover overlooked causal relations. Similar limitations are observed in other multi-source reasoning settings, suggesting the issue is pervasive rather than benchmark-specific.

Conclusion: LLMs trained on human-preferred data inherit a form of inattentional blindness: they systematically miss subtle but critical cues despite clear task instructions. The MixRea benchmark exposes this vulnerability, and PRCP provides a partial mitigation via structured prompting that encourages completion of latent relations. However, the persistence of this behavior across tasks implies that deeper architectural or training changes are needed for more cognitively aligned, attention-reliable models, especially for high-stakes use cases.

Abstract: Large language models (LLMs) are increasingly integrated into high-stakes decision-making. Inspired by the theory of \emph{inattentional blindness} in human cognition, we investigate whether LLMs, trained on human-preferred corpora that embed attentional biases, exhibit a similar limitation: \emph{failing to attend to subtle yet important contextual cues under explicit task instructions}. To evaluate this, we introduce the task of \textbf{explicit-implicit reasoning} and present \textbf{MixRea}, a benchmark of 2,246 multiple-choice questions across 9 reasoning types with varying distributions of explicit and implicit information. Evaluation of 21 advanced LLMs shows that even the best-performing reasoning model (Gemini 2.5 Pro) achieves only 42.8\% consistency, revealing widespread inattentional blindness. To mitigate this, we propose \textbf{Potential Relation Completion Prompting (PRCP)}, a prompting method that improves reasoning by recovering overlooked causal relations. Further analysis shows that this limitation persists across diverse multi-source reasoning tasks, highlighting the need for more cognitively aligned models.

</details>


### [46] [KoRe: Compact Knowledge Representations for Large Language Models](https://arxiv.org/abs/2605.20170)
*Davide Cavicchini,Fausto Giunchiglia,Jacopo Staiano*

Main category: cs.CL

TL;DR: The paper presents KoRe, a method to encode 1-hop knowledge graph subgraphs into compact tokens that can be injected into LLMs, improving knowledge grounding while greatly reducing token usage.


<details>
  <summary>Details</summary>
Motivation: LLMs store world knowledge implicitly in their parameters, which makes that knowledge opaque, hard to update or debug, and prone to hallucinations. Knowledge graphs, in contrast, offer explicit, editable, and human-readable knowledge structures that have been shown to help in knowledge-intensive tasks, but existing ways to combine KGs with LLMs require heavy retraining or finetuning. The authors want a more efficient way to ground LLMs in KG knowledge without such costly integration.

Method: They propose KoRe, a methodology that takes 1-hop subgraphs from a knowledge graph (i.e., a node and its immediate neighbors/relations) and encodes them into compact discrete “knowledge tokens.” These tokens are then injected into the input of an existing LLM backbone, allowing the model to condition on structured KG information at inference time without retraining from scratch. The method focuses on compressing subgraphs so they can be consumed efficiently by the LLM.

Result: On three established benchmarks for knowledge-intensive or reasoning tasks, KoRe attains competitive performance compared to existing methods, while achieving a substantial reduction in token usage (up to a 10× reduction). This indicates that the compact KG token representations are both effective for performance and efficient in terms of input length.

Conclusion: Compact, discrete representations of knowledge graph subgraphs can be used to efficiently ground LLMs, mitigating some of the opacity and hallucination issues associated with purely parametric knowledge. KoRe offers a practical, token-efficient way to integrate KGs with LLMs without extensive retraining, demonstrating that explicit structured knowledge can be injected into modern language models in a compact form while retaining strong task performance.

Abstract: Modern Large Language Models (LLMs) have shown impressive performances in user-facing tasks such as question answering, as well as consistent improvements in reasoning capabilities. Still, the way these models encode knowledge seems inherently flawed: by design, LLMs encode world-knowledge within their parameters. This way of representing knowledge is inherently opaque, difficult to debug and update, and prone to hallucinations. On the other hand, Knowledge Graphs can provide human-readable and easily editable world knowledge representations, and their application in knowledge-intensive tasks has consistently proven beneficial to downstream performance. Nonetheless, current integration techniques require extensive retraining or finetuning. To overcome this issue, we introduce KoRe, a methodology to encode 1-hop sub-graphs into compact discrete knowledge tokens and inject them into a LLM backbone. We test the proposed approach on three established benchmarks, and report competitive performances coupled with a significant reduction (up to 10x) in token usage. Our results show that compact discrete KG representations can efficiently and effectively be used to ground modern LLMs.

</details>


### [47] [From Seeing to Thinking: Decoupling Perception and Reasoning Improves Post-Training of Vision-Language Models](https://arxiv.org/abs/2605.20177)
*Juncheng Wu,Hardy Chen,Haoqin Tu,Xianfeng Tang,Freda Shi,Hui Liu,Hanqing Lu,Cihang Xie,Yuyin Zhou*

Main category: cs.CL

TL;DR: The paper shows that visual-language models are mainly limited by perception, not reasoning, and proposes a staged training curriculum that first strengthens visual perception (especially via RL), then visual and textual reasoning, yielding better accuracy and shorter reasoning chains than merged training.


<details>
  <summary>Details</summary>
Motivation: Many recent VLMs focus on longer, more complex chain-of-thought reasoning to improve performance, assuming reasoning is the main bottleneck. However, the authors observe that errors often stem from poor visual perception rather than insufficient reasoning. There is a lack of systematic study on how perception and reasoning interact in post-training and on how to best structure training curricula for VLMs. The paper aims to clarify these relationships and design a more effective training strategy.

Method: The authors decompose VLM capabilities into three stages: (1) visual perception, (2) visual reasoning, and (3) textual reasoning. They design a staged post-training pipeline where each stage is optimized with specialized datasets tailored to that capability. They compare this staged curriculum against a baseline that merges all capabilities into a single training phase. They also compare different learning paradigms for the perception stage, notably reinforcement learning (RL) versus caption-style supervised fine-tuning (SFT). Experiments are run across multiple VLM backbones and evaluated on visual math and perception benchmarks.

Result: The study finds that (a) high-quality visual perception requires targeted, dedicated optimization with specialized data; (b) strengthening perception first provides a necessary scaffold for later visual reasoning refinement; and (c) RL is more effective than caption-based SFT at improving visual perception. Staged training yields consistent improvements in both perception and reasoning compared to merged training. Models trained with the proposed approach achieve around 1.5% higher reasoning accuracy while producing reasoning traces that are about 20.8% shorter. Additionally, combining capability-based staging with traditional difficulty-based curricula leads to further additive gains.

Conclusion: Capability-based staged training—separating and ordering visual perception, visual reasoning, and textual reasoning—forms an effective curriculum for post-training VLMs. Prioritizing and solidifying visual perception, especially via RL, leads to better overall performance and more concise reasoning, indicating that improved perception reduces the need for long chain-of-thought explanations. The method sets new state-of-the-art results among open-weight VLMs on several visual math and perception benchmarks, demonstrating that curriculum design along capability dimensions is a powerful complement to standard difficulty-based approaches.

Abstract: Recent advances in vision-language models (VLMs) emphasize long chain-of-thought reasoning; yet, we find that their performance on visual tasks is primarily limited by a lack of visual perception as opposed to reasoning itself. In this work, we systematically study the interplay between perception and reasoning in VLM post-training by decomposing their capabilities into three separate training stages: visual perception, visual reasoning, and textual reasoning, incorporating specialized training data. We demonstrate that visual perception (a) requires targeted optimization with specialized data; (b) serves as a fundamental scaffold that should be solidified through staged training before refining visual reasoning; and (c) is more effectively learned via RL than caption-based SFT. Our experiments across multiple VLMs demonstrate that staged training consistently improves both visual perception and reasoning performance over merged training. Notably, models trained with our approach achieve 1.5% higher reasoning accuracy with 20.8% shorter reasoning traces, suggesting that superior perception reduces the need for excessive reasoning. Furthermore, we show that this capability-based staging represents a new curriculum dimension orthogonal to traditional difficulty-based curricula, and combining both yields further additive gains. Our staged-training models achieve superior performance among open-weight VLMs, establishing advanced results on several visual math and perception (e.g., +5.2% on WeMath and +3.7% on RealWorldQA) tasks compared with the base counterpart.

</details>


### [48] [TIDE: Efficient and Lossless MoE Diffusion LLM Inference with I/O-aware Expert Offload](https://arxiv.org/abs/2605.20179)
*Zhiben Chen,Youpeng Zhao,Yang Sui,Jun Wang,Yuzhang Shang*

Main category: cs.CL

TL;DR: TIDE is a lossless, training-free inference optimization system for diffusion LLMs with MoE that uses temporally stable expert activations to reduce I/O and compute via interval-based expert refreshing, achieving up to ~1.5× throughput on tested models.


<details>
  <summary>Details</summary>
Motivation: Diffusion LLMs with MoE scale well and support parallel, bidirectional decoding, but are hard to deploy on resource-constrained devices due to I/O overhead and compute bottlenecks in existing autoregressive-oriented optimization methods. There is a need for an inference system tailored to dLLMs that can exploit their characteristics to run efficiently on limited hardware without retraining models.

Method: The authors observe that expert activations in MoE-based diffusion LLMs are temporally stable across steps within a diffusion block. They design TIDE, which uses an interval-based expert refresh strategy: instead of loading and placing experts every step, it refreshes expert placement only every few steps, chosen to balance I/O and compute. They formulate the choice of refresh interval and scheduling as a mathematical programming problem that minimizes I/O traffic and CPU computation under system constraints. The solution is applied at inference time without modifying or retraining the model.

Result: On a single GPU-CPU system running LLaDA2.0-mini and LLaDA2.0-flash diffusion LLMs, TIDE yields up to 1.4× and 1.5× throughput improvements, respectively, compared with prior inference baselines. The optimization is lossless, meaning model quality (e.g., outputs) is preserved while latency and throughput are improved.

Conclusion: TIDE shows that exploiting temporal stability of MoE expert activations in diffusion LLMs enables a practical, lossless, and training-free inference optimization that reduces I/O and compute on constrained hardware. By casting expert refresh scheduling as an optimization problem and solving for an optimal interval, TIDE delivers significant throughput gains on existing dLLM models, suggesting a promising direction for efficient deployment of large MoE-based diffusion LLMs on resource-limited systems.

Abstract: Diffusion Large Language Models (dLLMs) have emerged as a competitive alternative to autoregressive (AR) models, offering better hardware utilization and bidirectional context through parallel block-level decoding. However, as dLLMs continue to scale up with mixture-of-experts (MoE) architectures, their deployment on resource-constrained devices remains an open challenge. Existing AR-based methods often incur either prohibitive I/O overhead or significant compute bottlenecks. In this work, we propose TIDE, a novel resource-efficient inference system that leverages the temporal stability of expert activations during the diffusion process within the block. Specifically, we leverage the temporal stability of expert activations during the diffusion process within the block and introduce an interval-based expert refresh strategy that updates the expert placement in an I/O-aware fashion. To ensure optimal performance, we formulate the inference scheduling as a mathematical programming problem, solving for the optimal interval that minimizes I/O traffic and CPU computation. Most importantly, TIDE is a lossless optimization that requires no model training, providing a "free lunch" acceleration for dLLM inference. In a single GPU-CPU system, we demonstrate that TIDE achieves up to 1.4$\times$ and 1.5$\times$ throughput improvements over prior baselines on LLaDA2.0-mini and LLaDA2.0-flash models, respectively.

</details>


### [49] [IMLJD: A Computational Dataset for Indian Matrimonial Litigation Analysis](https://arxiv.org/abs/2605.19346)
*Joy Bose*

Main category: cs.CL

TL;DR: The paper introduces IMLJD, an open dataset of 3,613 Indian court judgments on matrimonial disputes, with structured labels and a knowledge graph, and shows differing quash rates between the Supreme Court and Karnataka High Court.


<details>
  <summary>Details</summary>
Motivation: To create a high-quality, openly available legal dataset focused on matrimonial disputes in India, enabling empirical study of judicial decision-making and providing structured labels and knowledge representations that current resources lack.

Method: The authors collect 3,613 judgments from the Supreme Court of India and the Karnataka High Court in specified timeframes, annotate them with structured outcome labels and metadata-based indicators, and construct a knowledge graph. They then perform statistical analysis of quashing petition success rates across courts and time periods.

Result: The dataset comprises 1,474 Supreme Court cases and 2,139 Karnataka High Court cases related to specific matrimonial dispute provisions. Analysis shows that quashing petitions succeed in 57.6% of Supreme Court cases versus 39.7% in Karnataka High Court cases; when restricting to 2018–2024, the Supreme Court quash rate is 59.3%, increasing the gap to 19.6 percentage points, suggesting a robust court-level differential.

Conclusion: IMLJD offers a rich, openly accessible resource for studying Indian matrimonial dispute jurisprudence, demonstrating notable differences in quash rates between the Supreme Court and Karnataka High Court; its structured labels, metadata, and knowledge graph can support further quantitative and computational legal research.

Abstract: We present IMLJD, an open dataset of 3,613 Indian court judgments covering matrimonial disputes under IPC Section 498A, the Protection of Women from Domestic Violence Act, and CrPC Section 482. The dataset covers the Supreme Court of India from 2000 to 2024 (1,474 cases) and the Karnataka High Court from 2018 to 2024 (2,139 cases), with structured outcome labels, metadata-derived indicators, and a knowledge graph. We find that 57.6% of quashing petitions succeed at the Supreme Court level compared to 39.7% at the Karnataka High Court level. On a matched 2018 to 2024 period, the SC quash rate is 59.3%, widening the differential to 19.6 percentage points and confirming the finding is robust to temporal adjustment. The dataset, code, and knowledge graph are released openly at https://github.com/joyboseroy/imljd and https://huggingface.co/datasets/joyboseroy/imljd.

</details>


### [50] [LP-Eval: Rubric and Dataset for Measuring the Quality of Legal Proposition Generation](https://arxiv.org/abs/2605.19815)
*Shanshan Xu,Johan Lindholm,Amogh Raina,Henrik Palmer Olsen,Daniel Hershcovich*

Main category: cs.CL

TL;DR: The paper studies how well large language models can generate and evaluate legal propositions from EU Court of Justice decisions, introducing an expert-designed rubric (LP-Eval) and annotated dataset to assess proposition quality.


<details>
  <summary>Details</summary>
Motivation: Legal propositions are foundational for legal reasoning and doctrinal analysis, but the specific task of generating such propositions automatically has been under-explored in Legal NLP. There is a need to systematically measure the quality of LLM-generated legal propositions, both in terms of formal correctness and substantive legal adequacy, and to understand whether LLMs can also serve as evaluators aligned with expert judgments.

Method: The authors use large language models to generate legal propositions from Court of Justice of the European Union decisions. They design LP-Eval, a three-step evaluation rubric co-created with legal experts that decomposes proposition quality into formal validity and substantive dimensions. They collect expert annotations from two legal experts on 100 LLM-generated propositions using this rubric, and then compare LLM-based evaluation (both rubric-guided and direct overall scoring) to expert assessments.

Result: Empirically, LLMs are able to generate mostly well-formed and high-quality legal propositions according to expert evaluation. However, propositions derived from well-established cases are rated higher quality than those from more recent cases. As evaluators, LLMs perform better when prompted to follow the LP-Eval rubric: rubric-guided LLM judgments align more closely with expert ratings than simple holistic scores, but LLMs still fail to capture the subtle, fine-grained distinctions that human experts make.

Conclusion: LLMs show strong promise for generating legal propositions and can approximate expert evaluation when guided by a structured rubric, but their performance is uneven across case types and they lack sensitivity to nuanced legal quality differences. Structured, rubric-based evaluation frameworks like LP-Eval and accompanying expert-annotated datasets are essential for advancing reliable Legal NLP systems and understanding the limits of LLMs as both generators and evaluators of legal reasoning artifacts.

Abstract: Legal proposition generation is central to legal reasoning and doctrinal scholarship, yet remain under-examined in Legal NLP. This paper investigates the automatic generation and evaluation of legal propositions from decisions of the Court of Justice of the European Union using large language models (LLMs). We introduce LP-Eval, a three-step evaluation rubric co-designed with legal experts that decomposes legal proposition quality into formal validity and substantive dimensions. Using this rubric, we release a dataset of two experts' annotations for 100 LLM-generated legal propositions. Our results show that LLMs can generate predominantly well-formed and high-quality propositions, while expert evaluations reveal higher quality for propositions derived from well established cases than from recent ones. We further examine LLMs as evaluators and find that rubric-guided LLM judgments align more closely with expert assessments than direct overall scoring, but remain insensitive to finer-grained distinctions captured by human experts.

</details>


### [51] [PromptRad: Knowledge-Enhanced Multi-Label Prompt-Tuning for Low-Resource Radiology Report Labeling](https://arxiv.org/abs/2605.20052)
*Ying-Jia Lin,Tzu-Chin Lo,Ping-Chien Li,Chi-Tung Cheng,Chien-Hung Liao,Hung-Yu Kao*

Main category: cs.CL

TL;DR: PromptRad is a prompt-tuning method that labels radiology reports with very little labeled data by turning labeling into masked language modeling and enriching label words with UMLS synonyms.


<details>
  <summary>Details</summary>
Motivation: Clinical text in radiology reports is unstructured, diverse, and often lacks large labeled datasets, making rule-based systems brittle and standard PLM fine-tuning impractical in low-resource clinical settings. There is a need for a labeling method that works well with few labeled examples and can handle complex language like negation.

Method: PromptRad reframes multi-label radiology report classification as masked language modeling using prompt-tuning on a pre-trained language model. It designs multi-label prompts and uses a multi-word verbalizer enriched with synonyms from the UMLS Metathesaurus to better represent each target category, removing the need for additional classification layers and enabling efficient fine-tuning with few labeled samples.

Result: On liver CT reports, PromptRad beats dictionary-based labelers and conventional PLM fine-tuning even when trained with only 32 labeled examples, and its performance is competitive with GPT-4 while using a much smaller model. Analysis shows it better captures complex negation patterns than baselines.

Conclusion: PromptRad is an effective, data-efficient solution for automatic radiology report labeling in low-resource clinical scenarios, leveraging prompt-tuning and UMLS-based verbalizers to outperform traditional methods and approach large-model performance, especially in handling negation.

Abstract: Automatic report labeling facilitates the identification of clinical findings from unstructured text and enables large-scale annotation for medical imaging research. Existing rule-based labelers struggle with the diverse descriptions in clinical reports, while fine-tuning pre-trained language models (PLMs) requires large amounts of labeled data that are often unavailable in clinical settings. In this paper, we propose PromptRad, a knowledge-enhanced multi-label \textbf{prompt}-tuning approach for \textbf{rad}iology report labeling under low-resource settings. PromptRad reformulates multi-label classification as masked language modeling and incorporates synonyms from the UMLS Metathesaurus into a multi-word verbalizer to enrich category representations. By fine-tuning the PLM without additional classification layers, PromptRad requires substantially less labeled data than conventional fine-tuning. Experiments on liver CT reports show that PromptRad outperforms dictionary-based and fine-tuning baselines with only 32 labeled training examples, and achieves competitive performance with GPT-4 despite using a much smaller model. Further analysis demonstrates that PromptRad captures complex negation patterns more effectively than existing methods, making it a promising solution for report labeling in data-scarce clinical scenarios. Our code is available at https://github.com/ila-lab/PromptRad.

</details>


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [52] [Position: Let's Develop Data Probes to Fundamentally Understand How Data Affects LLM Performance](https://arxiv.org/abs/2605.18801)
*Shiqiang Wang,Herbert Woisetschläger,Hans Arno Jacobsen,Mingyue Ji*

Main category: cs.AI

TL;DR: The paper proposes using theoretically grounded synthetic data probes—sequences generated from well-defined random processes—to systematically study how data characteristics affect LLM behavior across the whole workflow.


<details>
  <summary>Details</summary>
Motivation: Understanding what kinds of data are useful for different LLM stages (training, tuning, alignment, in-context learning) is poorly understood; current practice depends on brute-force experimentation with large datasets, which is compute-heavy and heuristic-based, lacking a principled theory of how data characteristics drive LLM behavior.

Method: The authors propose a position/framework: construct synthetic sequences ("data probes") from carefully specified random processes whose statistical properties are known and controllable. Use these probes at different stages of the LLM workflow and measure the model’s responses. Analyze model behavior on these probes using information-theoretic notions like typical sets, generalized to LLMs, in order to link data properties to performance, generalization, and robustness.

Result: As a position paper, it does not present extensive empirical results; rather, it articulates the conceptual benefits: data probes can systematically reveal how particular data characteristics influence LLM behavior and can reduce reliance on ad-hoc, compute-intensive empirical heuristics for dataset design.

Conclusion: Systematic, theoretically motivated synthetic data probes offer a promising research direction for uncovering foundational principles governing the role of data in LLM training and inference, creating a more principled basis for data selection and evaluation across the LLM pipeline.

Abstract: Data is fundamental to large language models (LLMs). However, understanding of what makes certain data useful for different stages of an LLM workflow, including training, tuning, alignment, in-context learning, etc., and why, remains an open question. Current approaches rely heavily on extensive experimentation with large public datasets to obtain empirical heuristics for data filtering and dataset construction. These approaches are compute intensive and lack a principled way of understanding the essence of how specific data characteristics drive LLM behavior. In this position paper, we advocate for the need of developing systematic methodologies for generating synthetic sequences from appropriately defined random processes, with the goal that these sequences can reveal useful characteristics when they are used in one or multiple stages of the LLM workflow. We refer to such sequences as data probes. By observing LLM behavior on data probes, researchers can systematically conduct studies on how data characteristics influence model performance, generalization, and robustness. The probing sequences exhibit statistical properties that can be viewed using theoretical concepts, such as typical sets, which are generalized to describe the behaviors of LLMs. This data-probe approach provides a pathway for uncovering foundational insights into the role of data in LLM training and inference, beyond empirical heuristics.

</details>


### [53] [Operationalizing Document AI: A Microservice Architecture for OCR and LLM Pipelines in Production](https://arxiv.org/abs/2605.18818)
*Yao Fehlis,Benjamin Bengfort,Zhangzhang Si,Vahid Eyorokon,Prema Roman,Patrick Deziel,Devon Slonaker,Steve Veldman,Ben Johnson,Joyce Rigelo,Michael Wharton,Steve Kramer*

Main category: cs.AI

TL;DR: The paper presents a production-ready microservice architecture for large-scale document understanding pipelines.


<details>
  <summary>Details</summary>
Motivation: There is a significant gap between academic work on new document understanding models and practical guidance on deploying such models at production scale. Practitioners lack concrete architectural patterns and empirical insights for running complex pipelines (classification, OCR, LLM extraction) on large volumes of documents efficiently.

Method: The authors design and implement a microservice-based pipeline that chains multiple models (for classification, OCR, and LLM-based structured field extraction). They make key architectural choices: hybrid classification, strict separation of GPU-heavy inference from CPU-based orchestration, asynchronous handling of I/O-bound operations, and independently scalable components. They then profile batch performance at scale (thousands of multi-page documents per hour) to understand system behavior and bottlenecks.

Result: Profiling reveals that OCR, rather than LLM parsing, is the main contributor to end-to-end latency, contradicting common expectations. Furthermore, the system’s maximum throughput is limited by shared GPU inference capacity, not by the number of worker processes or services, indicating a concurrency saturation point at the GPU layer.

Conclusion: The paper offers practical design patterns and empirical observations for building scalable, production-grade document understanding systems. By highlighting architectural decisions and real-world bottlenecks, it guides practitioners on how to effectively operationalize document understanding models beyond benchmark-focused research.

Abstract: Academic research tends to focus on new models for document understanding creating a wide gap in the literature between model definition and running models at production scale. To close that gap, we present a microservice architecture that encapsulates pipelines of multiple models for classification, optical character recognition (OCR), and large language model structured field extraction as well as our experience running this pipeline on thousands of multi-page documents per hour. We describe our primary design decisions, including a hybrid classification, separation of GPU-bound inference from CPU-bound orchestration, use of asynchronous processing for the many IO-bound operations in the pipeline, and an independent, horizontal scaling strategy. Using batch profiling, we identified two surprising qualitative findings that shape production deployments: OCR, not language-model parsing, dominates end-to-end latency, and the system saturates at a concurrency determined by shared GPU-inference capacity rather than worker count. Our goal is to provide practitioners with concrete architectural patterns for building document understanding systems that work beyond the benchmark; effectively operationalizing models in production.

</details>


### [54] [Evaluating the Utility of Personal Health Records in Personalized Health AI](https://arxiv.org/abs/2605.18937)
*Rory Sayres,Kejia Chen,Ayush Jain,Matthew Thompson,Jonathan Richina,Xiang Yin,Jimmy Hu,Fan Zhang,Bob Lou,Mike Sanchez,Ines Mezerreg,Meredith Schreier,Hamsa Subramaniam,I-Ching Lee,Yugang Jia,Daniel Mcduff,Yossi Matias,Avinatan Hassidim,Dale Webster,Yun Liu,Jackie Barr,Quang Duong*

Main category: cs.AI

TL;DR: The paper evaluates how well a large language model (Gemini 3.0 Flash) can answer patient health questions when given different levels of personal health record (PHR) context, showing that PHR context significantly improves answer helpfulness and often safety/accuracy, while revealing specific failure modes.


<details>
  <summary>Details</summary>
Motivation: Patients increasingly have access to their Personal Health Records, but the data are complex and hard to interpret, limiting the promised empowerment. Large language models could potentially translate this complexity into understandable answers tailored to a patient’s own data, but their performance, benefits, and specific risks in this setting are not well understood. The authors want to quantify if and how PHR context improves LLM answers, and to systematically characterize error patterns when LLMs interpret PHRs.

Method: The authors used Gemini 3.0 Flash to answer 2,257 patient-like health queries sampled from three distributions: short web-search style queries, longer templated chatbot-style questions, and real patient call questions to healthcare teams. Each query was paired with a de-identified PHR from a pool of 1,945 records. The model generated responses under three conditions: (1) no PHR context, (2) a basic PHR summary (demographics, conditions, medications), and (3) full detailed clinical notes. They assessed answer quality using the SHARP rating framework plus a newly developed framework focusing on PHR-specific error modes (e.g., temporal confusion, confabulation). Ratings were generated automatically (autoraters) for all cases and by clinicians for a subset of 95 cases, with raters having access to full PHR context. Statistical comparisons (paired t-tests) were used to determine whether adding PHR context improved outcomes.

Result: Adding PHR context significantly improved the helpfulness of answers for all three types of questions (p < 0.001). The authors also observed potential improvements in safety, accuracy, relevance, and personalization when PHR data were included. At the same time, their PHR-specific evaluation framework revealed persistent limitations in LLM reasoning over complex health records, including temporal disorientation (misunderstanding the timing or sequence of events) and occasional but important confabulations (fabricated but plausible-sounding details).

Conclusion: The study concludes that providing PHR context to LLMs like Gemini 3.0 Flash can substantially enhance the helpfulness and potentially the safety and personalization of answers to diverse patient health queries. However, specific vulnerabilities remain, particularly around temporal reasoning and rare confabulations when interpreting complex clinical notes. The authors propose their PHR-focused evaluation framework as a tool to systematically monitor and address these gaps and suggest further research to translate these improvements into real-world benefits for patients trying to understand their health records.

Abstract: Patient-managed Personal Health Records (PHRs) promises to empower patients to better understand their health; but information in the record is complex, potentially hindering insights. In this study, we assess the potential of large language models (LLMs, Gemini 3.0 Flash) to provide helpful answers to user health queries, when provided clinical data from PHRs as context. A total of 2,257 user queries were drawn from 3 different distributions to represent patient questions: shorter web search queries, longer questions derived from templates of chatbot conversations, and questions patients asked to their healthcare team (patient calls). Queries were matched with de-identified PHRs (from a pool of 1,945). Gemini responses were generated (1) without PHR context; (2) with a basic summary of demographics, conditions, and medications; (3) with full, extensive clinical notes. For evaluation, we leveraged an existing rating framework (SHARP), and developed a new framework for specific error modes when interpreting PHRs. Evaluation was performed using autoraters for the full set, and with clinician ratings for a subset (n=95), with both sets of raters knowing the full PHR context. We see significant improvements in the helpfulness of answers to all question types with PHR data (p < 0.001, paired t-test). We also observe potential gains in safety, accuracy, relevance and personalization of answers. Our PHR evaluation framework further identifies gaps in LLM understanding of particular aspects of complex PHRs, such as temporal disorientation, and rare but meaningful confabulations. These results suggest potential for PHR data to help people with a wide range of user needs; and provide a framework for monitoring for gaps in LLM answers based on PHR context. This study motivates further work to assess and realize potential benefits to users from understanding their health records.

</details>


### [55] [Learn-by-Wire Training Control Governance: Bounded Autonomous Training Under Stress for Stability and Efficiency](https://arxiv.org/abs/2605.19008)
*Anis Radianis*

Main category: cs.AI

TL;DR: LBW-Guard is a governance layer sitting above AdamW that monitors training telemetry and applies bounded control to stabilize and speed up LLM training, especially under aggressive learning rates and stress.


<details>
  <summary>Details</summary>
Motivation: Language-model training at scale often suffers from instability and wasted compute when using aggressive learning rates and operating under runtime stress. Existing approaches like changing optimizers or using gradient clipping may not sufficiently address these failures while preserving the same training objective and optimizer logic. The authors are motivated to design a mechanism that can improve robustness and efficiency of training without replacing the optimizer or relying solely on local gradient operations.

Method: The authors propose Learn-by-Wire Guard (LBW-Guard), a bounded autonomous training-control governance layer that operates on top of the AdamW optimizer. LBW-Guard monitors training telemetry to detect instability-sensitive regimes and then applies bounded control actions to the optimizer’s execution, while leaving the underlying optimization rule and training objectives fixed. They empirically evaluate LBW-Guard using a Qwen2.5-based stress and robustness benchmark on WikiText-103, centered on Qwen2.5-7B, with comparisons to Qwen2.5-3B and 14B, learning-rate stress tests, gradient-clipping baselines, and a full-parameter TinyLlama-1B sanity check.

Result: In the primary Qwen2.5-7B setting, LBW-Guard reduces final perplexity from 13.21 to 10.74 (an 18.7% improvement) and shortens end-to-end training time from 392.54s to 357.02s (a 1.10x speedup). Under stronger learning-rate stress, AdamW’s performance collapses, yielding final perplexities of 1885.24 at LR=3e-3 and 659.76 at LR=1e-3, whereas LBW-Guard maintains trainable behavior with perplexities of 11.57 and 10.33, respectively. Gradient-clipping baselines fail to reproduce these robustness gains.

Conclusion: The experiments suggest that adding a governance/control plane above the optimizer can significantly enhance the stability and efficiency of large language model training under stress, without altering the underlying optimizer or relying solely on gradient clipping. LBW-Guard demonstrates that bounded runtime control over optimizer execution can preserve useful compute and avoid catastrophic degradation in high-stress training regimes.

Abstract: Modern language-model training is increasingly exposed to instability, degraded runs, and wasted compute, especially under aggressive learning-rate, scale, and runtime-stress conditions. This paper introduces Learn-by-Wire Guard (LBW-Guard), a bounded autonomous training-control governance layer that operates above AdamW. Rather than replacing the optimizer update rule, LBW-Guard observes training telemetry, interprets instability-sensitive regimes, and applies bounded control to optimizer execution while preserving fixed training objectives.
  We evaluate LBW-Guard in a Qwen2.5-centered stress-and-robustness suite using WikiText-103, with Qwen2.5-7B as the empirical anchor, model-size comparisons against Qwen2.5-3B and Qwen2.5-14B, learning-rate stress tests, gradient-clipping baselines, and a no-LoRA TinyLlama-1B full-parameter sanity check. In the 7B reference setting, LBW-Guard reduces final perplexity from 13.21 to 10.74, an 18.7% improvement, while reducing end-to-end time from 392.54s to 357.02s, a 1.10x speedup. Under stronger learning-rate stress, AdamW degrades to 1885.24 final perplexity at LR=3e-3 and 659.76 at LR=1e-3, whereas LBW-Guard remains trainable at 11.57 and 10.33, respectively. Gradient-clipping baselines do not reproduce this effect.
  These results support a scoped systems conclusion that stability-sensitive LLM training can benefit from a governance plane above the optimizer. LBW-Guard provides evidence that bounded runtime control can preserve productive compute under stress while remaining distinct from optimizer replacement and local gradient suppression.

</details>


### [56] [AgentNLQ: A General-Purpose Agent for Natural Language to SQL](https://arxiv.org/abs/2605.19010)
*Olena Bogdanov,Yeunji Jung,Chandra Dhir,Pareekshitreddy Gaddam,Saurabh Jain,Lakshmi Tumati,Vijay Parthasarathy,Anup Shirgaonkar*

Main category: cs.AI

TL;DR: The paper introduces a multi-agent, LLM-based system for converting natural language questions into SQL, achieving 78.1% semantic accuracy on the BIRD benchmark by enriching database schemas and orchestrating planning, reflection, and self-correction among agents.


<details>
  <summary>Details</summary>
Motivation: Although large language models have improved rapidly, automatically converting natural language questions to SQL queries is still less accurate than queries written by human experts. Given the widespread use of relational databases in real-world applications, improving NL2SQL accuracy is important for both researchers and industry. The authors aim to close this gap by enhancing how systems understand database schemas and by better coordinating multiple LLM agents to generate more reliable SQL.

Method: The authors design a multi-agent framework where LLMs are assigned specialized roles (planning, orchestration, reflection, and self-correction) to collaboratively produce SQL queries from natural language. They introduce an optimized orchestrator that coordinates these agents. Additionally, they propose a schema enrichment process that augments the user-provided database schema with semantically rich, context-aware metadata and incorporates user-defined business rules. This enriched representation is then used during query generation to improve semantic alignment between user intent and SQL output.

Result: On the BIRD (BIg Bench for LaRge-scale Database) NL2SQL benchmark, the proposed method attains 78.1% semantic accuracy, indicating that the generated SQL queries closely match the intended meaning of the natural language inputs. The evaluation demonstrates that the approach generalizes across different domains and datasets within the benchmark.

Conclusion: A multi-agent, LLM-driven NL2SQL system that uses an optimized orchestrator, reflection and self-correction mechanisms, and semantically enriched database schemas can substantially improve semantic accuracy over prior approaches. Enriching schema information and encoding business rules as metadata are key to better aligning generated SQL with user intent, and the results on BIRD-SQL suggest that this approach is effective and generalizable across varied database scenarios.

Abstract: Natural language to SQL (NL2SQL) conversion is an important problem for researchers and enterprises due to the ubiquitous importance of relational databases in broad-ranging practical problems. Despite the rapid advancements in the capabilities of LLMs, NL2SQL has not reached parity in accuracy with human expert SQL writers, hence needing additional improvements in NL2SQL algorithms. This study presents a new multi-agent method for NL2SQL that achieves 78.1% semantic accuracy on the BIg Bench for LaRge-scale Database (BIRD) benchmark. Our method leverages a semantically enriched representation of user-provided schema, adds user-provided business rules, and produces accurate SQL queries. The main contributions of this study are (a) We designed an optimized new orchestrator in a multi-agent solution that uses LLMs to plan, orchestrate, reflect, and self-correct to generate accurate SQL queries, (b) We developed an advanced schema enrichment method that creates context-aware metadata to improve accuracy, and (c) We demonstrated the accuracy and generalizability of the method across different domains and datasets by evaluating it on the BIRD-SQL benchmark.

</details>


### [57] [KAN-MLP-Mixer: A comprehensive investigation of the usage of Kolmogorov-Arnold Networks (KANs) for improving IMU-based Human Activity Recognition](https://arxiv.org/abs/2605.19031)
*Mengxi Liu,Sizhen Bian,Vitor Fortes,Francisco Calatrava Nicolas,Daniel Geißler,Maximilian Kiefer-Emmanouilidis,Bo Zhou,Paul Lukowicz*

Main category: cs.AI

TL;DR: Hybrid Kolmogorov-Arnold Network (KAN) + MLP architecture for human activity recognition (HAR) improves robustness and accuracy on noisy, real-world wearable-sensor data.


<details>
  <summary>Details</summary>
Motivation: KANs are powerful at learning complex functions on clean, low-dimensional data, but they perform poorly on noisy, real-world datasets and are computationally heavy. Conventional MLPs are more noise-tolerant and efficient but less precise on complex mappings. In HAR with wearable sensors, naively replacing all MLPs by KANs reduces accuracy and efficiency. The motivation is to find an effective way to combine KANs’ functional precision with MLPs’ robustness and efficiency for real-world HAR tasks.

Method: The authors systematically evaluate different ways of inserting KAN modules within deep HAR networks. Based on this exploration, they design a hybrid KAN-MLP architecture that: (1) uses a KAN-based input embedding layer to capture complex low-level relationships in sensor signals; (2) keeps standard MLP layers for intermediate feature mixing to ensure noise robustness and computational efficiency; and (3) introduces a specialized LarctanKAN module as the final classifier to exploit KAN expressivity for decision boundaries. They also integrate this hybrid strategy into multiple existing state-of-the-art HAR architectures to test generality.

Result: On eight public HAR datasets, the proposed hybrid KAN-MLP model achieves an average macro-F1 relative improvement of 5.33% over a pure-MLP baseline and significantly outperforms both standalone KAN and standalone MLP models. When embedded into other state-of-the-art HAR architectures, the hybrid KAN-MLP design consistently improves their performance as well.

Conclusion: A carefully designed hybrid composition of KAN modules with conventional MLP components yields HAR models that are both more robust to noise and more accurate than either pure KAN or pure MLP architectures. Placing KANs at the input embedding and final classification stages, while using MLPs for intermediate processing, is an effective and general strategy for real-world wearable-sensing HAR systems.

Abstract: Kolmogorov-Arnold Networks (KANs) have demonstrated an exceptional ability to learn complex functions on clean, low-dimensional data but struggle to maintain performance on noisy and imperfect real-world datasets. In contrast, conventional multi-layer perceptrons (MLPs) are far more tolerant to noise and computationally efficient. Replacing all MLP components with KANs in HAR models often degrades accuracy and computation efficiency, highlighting an open challenge: how to combine KANs' precision with MLPs' noise robustness and efficiency. To address this, we systematically explore various placements of KAN modules within deep HAR networks and propose a hybrid architecture that strategically synergizes the strengths of both paradigms, which uses a KAN-based input embedding layer, retains MLP layers for intermediate feature mixing, and introduces a specialized LarctanKAN module for final activity classification. Across eight public HAR datasets, the hybrid KAN-MLP model achieves an average macro F1 score relative improvement of 5.33\% compared pure-MLP model, significantly outperforming standalone KAN and MLP baselines. Furthermore, integrating this hybrid strategy into other state-of-the-art HAR architectures consistently boosts their performance. Our findings demonstrate that a carefully orchestrated combination of KAN, MLP, or other conventional neural components yields more robust and accurate HAR models for real-world wearable sensing environments.

</details>


### [58] [Trustworthy Agent Network: Trust in Agent Networks Must Be Baked In, Not Bolted On](https://arxiv.org/abs/2605.19035)
*Yixiang Yao,Yuhang Yao,Xinyi Fan,Jiechao Gao,Jie Wang,Minjia Zhang,Srivatsan Ravi,Carlee Joe-Wong*

Main category: cs.AI

TL;DR: The paper is a vision piece proposing how to systematically build trustworthy multi-agent, large-language-model-based Agent-to-Agent (A2A) networks.


<details>
  <summary>Details</summary>
Motivation: As LLM-based agents evolve from operating alone to working in collaborative, heterogeneous multi-agent ecosystems, they can solve more complex, multi-step tasks but also introduce new systemic vulnerabilities—like adversarial composition, semantic misalignment, and cascading failures—that current single-agent alignment methods cannot adequately handle. There is a need for a foundational rethinking of trust and alignment specifically tailored to these A2A networks.

Method: This is not an empirical study but a conceptual/vision paper. The authors synthesize existing knowledge on LLM agents and multi-agent coordination, then propose a high-level architectural and conceptual framework for embedding trust into A2A systems from the ground up. The framework is organized around four key design pillars that define how trustworthy A2A coordination should be structured.

Result: The main result is a comprehensive conceptual framework for trust in Agent-to-Agent networks, articulated through four design pillars. While the abstract does not enumerate the pillars, the framework aims to capture how to design coordination, communication, and control among heterogeneous LLM agents so that systemic vulnerabilities are mitigated rather than exacerbated by multi-agent collaboration.

Conclusion: Trustworthiness in A2A networks cannot be achieved by simply retrofitting existing single-agent alignment protocols. Instead, trust must be embedded as a first-class consideration in the core architecture of A2A coordination. The proposed four-pillar conceptual framework is intended as a roadmap for future research and system design in trustworthy multi-agent LLM ecosystems.

Abstract: The rapid advancement of Large Language Models has given rise to autonomous LLM-based agents capable of complex reasoning and execution. As these agents transition from isolated operation to collaborative ecosystems, we witness the emergence of the Agent-to-Agent (A2A) network, a paradigm where heterogeneous agents autonomously coordinate to solve multi-step tasks. While these networks may offer better task performance compared to simply using one agent to complete the entire task, they introduce systemic vulnerabilities, such as adversarial composition, semantic misalignment, and cascading operational failures, that existing agent alignment techniques cannot address. In this vision paper, we argue that the trustworthiness of A2A networks cannot be fully guaranteed via retrofitting on existing protocols that are largely designed for individual agents. Rather, it must be architected from the very beginning of the A2A coordination framework. We present a comprehensive conceptual framework that situates trust in A2A systems through four design pillars.

</details>


### [59] [Interference-Aware Multi-Task Unlearning](https://arxiv.org/abs/2605.19042)
*Ying-Hua Huang,Rui Fang,Hsi-Wen Chen,Ming-Syan Chen*

Main category: cs.AI

TL;DR: The paper introduces multi-task unlearning, addressing how to remove specific training data or task supervision from multi-task models while preserving performance on remaining tasks and data, using an interference-aware gradient framework.


<details>
  <summary>Details</summary>
Motivation: Existing machine unlearning methods mainly consider single-task models. However, modern architectures frequently use shared backbones for multiple tasks, where forgetting one task or instance can unintentionally damage performance on others. There is a need to define and solve unlearning specifically in multi-task scenarios, characterizing and mitigating the interference induced by shared parameters.

Method: The authors formalize two multi-task unlearning settings: full-task unlearning (removing a target instance from all tasks) and partial-task unlearning (removing supervision from only selected tasks). They analyze how shared parameters couple forget and retain sets, causing task-level and instance-level interference. To mitigate this, they propose an interference-aware framework comprising (1) task-aware gradient projection that constrains parameter updates to task-specific subspaces, and (2) instance-level gradient orthogonalization that makes gradients for forget signals orthogonal to those for retain signals, reducing conflicts during optimization.

Result: On two multi-task computer vision benchmarks spanning five tasks, the proposed method achieves effective unlearning while preserving strong generalization on retained data. Quantitatively, it reduces the Unlearning-Interference Score (UIS) by 30.3% in full-task unlearning and 52.9% in partial-task unlearning compared with the strongest baseline, indicating significantly lower negative impact on non-target tasks and instances.

Conclusion: Multi-task unlearning is both practically important and fundamentally different from single-task unlearning due to interference from shared parameters. By explicitly modeling and controlling this interference through task-aware gradient projection and instance-level gradient orthogonalization, the proposed framework can forget designated supervision while better preserving the performance of remaining tasks and data, setting a new state of the art on multi-task benchmarks.

Abstract: Machine unlearning aims to remove the contribution of designated training data from a trained model while preserving performance on the remaining data. Existing work mainly focuses on single-task settings, whereas modern models often operate in multi-task setups with shared backbones, where removing supervision for one task or instance can unintentionally affect others. We introduce multi-task unlearning with two settings: full-task unlearning, which removes a target instance from all tasks, and partial-task unlearning, which removes supervision only from selected tasks. We show that shared parameters couple the forget and retain sets, causing task-level interference on non-target tasks and instance-level interference on other instances. To address this issue, we propose an interference-aware framework that combines task-aware gradient projection, which constrains updates within task-specific subspaces, with instance-level gradient orthogonalization, which reduces conflicts between forget and retain signals. Experiments on two multi-task computer vision benchmarks across five tasks show that our method achieves effective unlearning while maintaining strong generalization, reducing UIS compared with the strongest baseline by 30.3% in full-task unlearning and 52.9% in partial-task unlearning.

</details>


### [60] [Embedding by Elicitation: Dynamic Representations for Bayesian Optimization of System Prompts](https://arxiv.org/abs/2605.19093)
*Zhiyuan Jerry Lin,Benjamin Letham,Samuel Dooley,Maximilian Balandat,Eytan Bakshy*

Main category: cs.AI

TL;DR: The paper proposes ReElicit, a Bayesian optimization framework that tunes system prompts using only aggregate scalar feedback, by having an LLM build and adapt an interpretable feature space for prompts via elicitation and then optimize in that space.


<details>
  <summary>Details</summary>
Motivation: System prompts strongly control LLM behavior but are hard to tune in realistic settings where feedback is available only as aggregate metrics (e.g., overall accuracy) rather than per-example labels or detailed critiques. Existing prompt-optimization methods typically require richer supervision or are not sample-efficient in a black-box setting over discrete natural language. There is a need for a method that can efficiently optimize prompts under a strict evaluation budget and aggregate-only feedback, while still operating over natural-language prompt space.

Method: They model prompt tuning as black-box optimization over discrete, variable-length text under a tight evaluation budget. The proposed method, ReElicit, uses an LLM to construct an explicit, low-dimensional, interpretable feature space for prompts via “embedding by elicitation”: given the task description, past prompts, and their scalar scores, the LLM invents and defines semantic features and maps each prompt to a point in this feature space. A Gaussian process (GP) surrogate model is then trained on these feature vectors and scores. Using a BO acquisition function on the GP, they select new target feature vectors to explore/exploit high-scoring regions. The LLM then realizes these target feature vectors as specific natural-language system prompts and possibly refines them. As more evaluations are collected, the feature space is re-elicited so that the representation adapts to the observed history of prompts and scores.

Result: Using offline benchmarks where the only observed signal per prompt is a single scalar score (e.g., accuracy over a test set) and no per-example labels or critiques, ReElicit is compared against representative aggregate-only prompt-optimization baselines on ten different system-prompt optimization tasks with a total budget of 30 evaluations per task. Across these tasks and under this constrained feedback regime, ReElicit attains the best overall aggregate performance profile among competing methods.

Conclusion: ReElicit demonstrates that LLMs can go beyond simply generating prompts and can also dynamically construct and adapt semantic feature spaces that make Bayesian optimization feasible and effective over natural-language prompts using only aggregate feedback. This adaptive elicited-representation approach yields strong sample-efficient performance under realistic, feedback-limited conditions, suggesting a promising direction for prompt optimization and more broadly for black-box optimization over text using LLMs as representation builders.

Abstract: System prompts are a central control mechanism in modern AI systems, shaping behavior across conversations, tasks, and user populations. Yet they are difficult to tune when feedback is available only as aggregate metrics rather than per-example labels, failures, or critiques. We study this aggregate feedback setting as sample-constrained black-box optimization over discrete, variable-length text. We introduce ReElicit, a Bayesian optimization framework based on \emph{embedding by elicitation}. Given a task description, previously evaluated prompts, and scalar scores, an LLM elicits a compact, interpretable feature space and maps prompts into it. Leveraging a probabilistic Gaussian process surrogate, an acquisition function then selects target feature vectors, which the LLM realizes and refines into deployable system prompts. Re-eliciting the feature space as new evaluations arrive lets the representation adapt to the observed prompt-score history. We evaluate the setting using offline benchmark accuracy as a controlled aggregate proxy: the optimizer observes one scalar score per prompt and no per-example labels, errors, or critiques. Across ten system prompt optimization tasks with a 30 total evaluation budget, ReElicit achieves the strongest aggregate performance profile among representative aggregate-only prompt-optimization baselines. These results suggest that LLMs can serve as adaptive semantic representation builders, not only prompt generators, for Bayesian optimization over natural-language artifacts.

</details>


### [61] [DecisionBench: A Benchmark for Emergent Delegation in Long-Horizon Agentic Workflows](https://arxiv.org/abs/2605.19099)
*Yuxuan Gao,Megan Wang,Yi Ling Yu,Zijian Carl Ma,Ao Qu*

Main category: cs.AI

TL;DR: DecisionBench is a benchmark to study how AI agents delegate work to other models in long, complex tasks.


<details>
  <summary>Details</summary>
Motivation: As AI systems become more agentic and need to collaborate or delegate subtasks to other models, we lack a standardized way to evaluate how well this orchestration and delegation actually works beyond just final task quality.

Method: The authors build a benchmark substrate that standardizes tasks, a pool of peer models, a delegation interface, a way to annotate skills deterministically, and a multidimensional metric suite. They then run a five-condition experimental sweep over 23,375 task instances, varying how much and how peer information is exposed (awareness conditions) and whether it is delivered as tools or descriptions.

Result: They find that: (i) overall task quality looks similar across awareness conditions, so naive quality metrics hide important differences in delegation behavior; (ii) routing fidelity (choosing the best peer) varies widely from 7.5% to 29.5% even when quality is similar, and the way peer info is delivered (tool vs preloaded description) matters more than its exact content; (iii) an idealized counterfactual analysis shows that perfect delegation could improve performance by 15–31 percentage points, indicating substantial unrealized potential.

Conclusion: DecisionBench provides a reusable, extensible evaluation substrate for studying and improving delegation and orchestration in long-horizon agent workflows, and the results show that current methods underutilize delegation capacity, with large remaining headroom for smarter routing and peer-use strategies.

Abstract: We introduce DecisionBench, a benchmark substrate for emergent delegation in long-horizon agentic workflows. The substrate fixes a task suite (GAIA, tau-bench, BFCL multi-turn), a peer-model pool (11 models, 7 vendor families), a delegation interface (call_model plus an optional read_profile channel), a deterministic skill-annotation layer, and a multi-axis metric suite covering quality, cost, latency, delegation rate, routing fidelity-at-k, vendor self-preference, and a counterfactual-delegation ceiling. The substrate is agnostic to how peer information is generated or delivered, so learned routers, richer peer memories, adaptive profile construction, and multi-step delegation can all be evaluated against it. We characterize the substrate with a five-condition reference sweep on the full pool (n=23,375 task instances). Three benchmark-level findings emerge: (i) mean end-task quality is statistically indistinguishable across the four awareness conditions (|beta| <= 0.010, p >= 0.21), so quality-only evaluation would miss the orchestration signal; (ii) routing fidelity-at-1 ranges from 7.5% to 29.5% across conditions at near-equal mean quality, with delivery channel (on-demand tool vs. preloaded description) dominating description content; (iii) a counterfactual ceiling places perfect delegation 15-31 percentage points above measured performance on every suite, locating large unrealized headroom for future orchestration methods. We release the substrate, annotation layer, reference intervention suite, analysis pipeline, and 220 per-condition run archives.

</details>


### [62] [POLAR-Bench: A Diagnostic Benchmark for Privacy-Utility Trade-offs in LLM Agents](https://arxiv.org/abs/2605.19127)
*Qiaoyuan Zheng,Yiqu Yang,Qi Gao,Imanol Schlag*

Main category: cs.AI

TL;DR: POLAR-Bench is a benchmark to evaluate how well LLM agents obey user privacy policies against adversarial third-party models, revealing strong privacy adherence in frontier models but significant leakage in many smaller open-weight models.


<details>
  <summary>Details</summary>
Motivation: As LLM agents are increasingly entrusted with private user data and tasked to interact autonomously with external systems, there is a critical need to ensure they do not leak protected information, especially when third parties behave adversarially. Existing evaluations do not systematically stress-test privacy policy adherence under realistic, adversarial dialogue conditions, particularly for smaller open-weight models commonly used as on-device or self-hosted agents.

Method: The authors propose POLAR-Bench, a policy-aware adversarial benchmark where a trusted model (the agent) is given both a task and an explicit privacy policy specifying which attributes are protected. This trusted model then engages in a multi-turn conversation with an adversarial third-party model that attempts to elicit both task-relevant and protected attributes. The benchmark spans 10 domains and 7,852 samples and evaluates models using deterministic set-membership criteria for both privacy leakage (whether protected attributes are revealed) and utility (whether task-relevant attributes are shared as needed). They systematically vary the dimensionality of the privacy policy and the nature of the attack strategy along two independent axes, generating a 5×5 grid of privacy–utility scores per model to diagnose where intent-following holds or fails.

Result: Evaluation across multiple models shows a pronounced divide: current frontier LLMs successfully suppress more than 99% of protected attributes even under adversarial probing while maintaining task performance, whereas smaller open-weight models in the 1–30B parameter range perform substantially worse, with the least robust models leaking over 50% of protected attributes. These differences emerge clearly across the 5×5 diagnostic surfaces, which pinpoint conditions under which each model's intent-following degrades.

Conclusion: POLAR-Bench effectively exposes where and how different LLMs fail to obey privacy policies under adversarial interaction, especially highlighting vulnerabilities in smaller open-weight models widely used as local or private agents. This benchmark offers a practical tool for developers and researchers to assess and improve privacy alignment of LLM agents, focusing efforts on the model regimes and attack conditions where user data is most at risk.

Abstract: LLM agents increasingly have access to private user data and act on the user's behalf when interacting with third-party systems. The user defines what may and must not be shared, and the agent must robustly follow that intent even when third-party systems behave adversarially. We introduce POLAR-Bench (Policy-aware adversarial Benchmark), in which a trusted model with a privacy policy and a task converses with a third-party model that adversarially probes for both task-relevant and protected attributes. Across 10 domains and 7,852 samples, we score privacy and utility by deterministic set-membership and vary privacy policy dimension and attack strategy along two orthogonal axes, producing a 5 times 5 diagnostic surface per model. Our results reveal a sharp split: current frontier models withhold over 99% of protected attributes, while smaller open-weight models in the 1--30B range, the class users most commonly run as their own trusted agent on-device or via private inference, score notably worse, with the weakest leaking over half. POLAR-Bench thus localizes where each model's intent-following breaks down, providing a foothold for privacy alignment where it matters most.

</details>


### [63] [Progressive Autonomy as Preference Learning: A Formalization of Trust Calibration for Agentic Tool Use](https://arxiv.org/abs/2605.19151)
*Changkun Ou*

Main category: cs.AI

TL;DR: They model when an automated agent should act autonomously versus ask a human, using preference learning and Bayesian optimization ideas.


<details>
  <summary>Details</summary>
Motivation: In autonomous systems that use tools, it's critical to avoid both over-trust (letting risky actions go through) and under-trust (asking humans too often). Existing approaches lack a principled, sample-efficient way to learn when to require human approval based on human risk tolerance.

Method: They formalize trust calibration as learning a latent human risk-tolerance function over actions. A "policy gateway" maintains a Gaussian process posterior over this function, with a probit likelihood from observed binary human decisions (approve/deny). It then chooses which actions to escalate by querying where the model is most uncertain. Mathematically, this is cast as Preferential Bayesian Optimization using approximate GP classification and uncertainty-guided querying.

Result: They show that the trust-calibration problem is structurally equivalent to Preferential Bayesian Optimization, so existing inference algorithms and sample-efficiency guarantees apply. The gateway partitions the action space into three regions: always allow, always block, and escalate to human when uncertain.

Conclusion: Trust calibration for agentic tool use can be treated as a preference-learning problem structurally matching Preferential Bayesian Optimization, enabling principled, sample-efficient learning of when to automate vs. escalate. This yields a clear framework for dividing actions into allow/block/ask regions.

Abstract: We formalize trust calibration for agentic tool use (deciding when an automated agent's proposed action may execute autonomously versus require human approval) as a preference-learning problem. A policy gateway maintains a Gaussian-process posterior over a latent human risk-tolerance function, observed through a probit likelihood on binary approve/deny feedback, and escalates to the human exactly where the approval outcome is most uncertain. We show this is structurally an instance of Preferential Bayesian Optimization, inheriting its inference machinery (approximate Gaussian-process classification) and its sample-efficiency argument (uncertainty-targeted querying), while differing in objective: classifying an action space into allow/block/ask regions rather than optimizing a design.

</details>


### [64] [How Far Are We From True Auto-Research?](https://arxiv.org/abs/2605.19156)
*Zhengxin Zhang,Ning Wang,Sainyam Galhotra,Claire Cardie*

Main category: cs.AI

TL;DR: The paper introduces ResearchArena, a scaffold that lets LLM-based agents autonomously perform the full research cycle and systematically evaluates the quality of their generated papers, finding that none reach top-tier conference standards.


<details>
  <summary>Details</summary>
Motivation: While autonomous research agents can already produce full papers, there is little systematic evidence about how good these papers actually are beyond surface-level manuscript quality. The authors aim to rigorously assess the gap between current auto-research capabilities and the standards of top-tier venues, especially regarding experimental rigor and artifact quality.

Method: The authors build ResearchArena, a minimal scaffolding framework that allows off-the-shelf code-focused agents (Claude Code with Opus 4.6, Codex with GPT-5.4, and Kimi Code with K2.5) to autonomously execute the entire research loop: idea generation, experimentation, writing papers, and self-refinement. Using 13 computer science seed topics and 3 trials per agent-domain pair, they generate 117 papers. These papers are evaluated using (1) SAR, a manuscript-only review; (2) artifact-aware peer review (PR), where reviewers inspect both manuscript and research artifacts; and (3) human meta-review that synthesizes the findings and acceptance decisions.

Result: Under manuscript-only SAR review, Claude Code’s papers achieve the highest scores, exceeding a baseline (Analemma's FARS) and matching the weighted-average score of human ICLR 2025 submissions, suggesting superficially competitive quality. However, SAR scores correlate poorly with true acceptance decisions and mainly reward plausible narrative rather than validated experiments. Under artifact-aware PR, scores decline significantly, revealing that experimental rigor is the main weakness. The authors identify three key failure modes—fabricated results, underpowered experiments, and mismatches between plans and execution—which vary considerably across agents: Codex shows roughly 5% paper–artifact mismatch and 8% fabricated references, while Kimi Code shows 77% and 72% respectively, reflecting different emergent “research personas.” None of the 117 papers meet the acceptance bar of a top-tier conference.

Conclusion: Despite promising manuscript-only scores, current autonomous research agents fall far short of top-tier scientific standards once experimental artifacts are scrutinized. The main bottleneck is experimental rigor, with high rates of fabrication, insufficient experimentation, and inconsistencies between stated and actual methods. The work implies that achieving genuine auto-research will require advances in reliable experimentation, verification, and alignment of research narratives with underlying artifacts, not just better paper-writing capabilities.

Abstract: Recent auto-research systems can produce complete papers, but feasibility is not the same as quality, and the field still lacks a systematic study of how good agent-generated papers actually are. We introduce ResearchArena, a minimal scaffold that lets off-the-shelf agents (Claude Code using Opus 4.6, Codex using GPT-5.4, and Kimi Code using K2.5) carry out the full research loop themselves (ideation, experimentation, paper writing, self-refinement) under only lightweight guidance. Across 13 computer science seeds and 3 trials per agent-domain pair, ResearchArena yields 117 agent-generated papers, each evaluated under three complementary lenses: a manuscript-only reviewer (SAR), an artifact-aware peer review (PR) in which agents inspect the workspace alongside the manuscript, and an human conducted meta-review. Under SAR alone the picture is optimistic: Claude Code obtains the highest score, outperforms Analemma's FARS, and matches the weighted-average human ICLR 2025 submission, suggesting that minimally scaffolded agents can produce papers that look competitive on manuscript-only review. Manual inspection, however, reveals this picture is overstated: SAR scores are poorly aligned with its actual acceptance decisions and reward plausible framing without verifying experimental substance. Under artifact-aware PR scores drop sharply, and manual auditing identifies experimental rigor as the major bottleneck, decomposing into three failure modes (fabricated results, underpowered experiments, and plan/execution mismatch) that are highly agent-dependent: Codex 5%/8% paper-vs-artifact mismatch / fabricated references versus Kimi Code 77%/72%, a $\sim$15$\times$ spread that tracks distinct research personas the agents develop. None of the 117 agent-generated papers reaches the acceptance bar of a top-tier venue. This suggests that we are still gapped from the true auto-research.

</details>


### [65] [Hallucination as Exploit: Evidence-Carrying Multimodal Agents](https://arxiv.org/abs/2605.19192)
*Guijia Zhang,Hao Zheng,Harry Yang*

Main category: cs.AI

TL;DR: The paper identifies a new security failure mode for multimodal agents—hallucinations leading directly to unsafe tool use—and proposes an architecture (ECA) that only authorizes actions based on externally verifiable evidence rather than free-form model text.


<details>
  <summary>Details</summary>
Motivation: Multimodal agents can act on the world via tools (clicking, emailing, transferring data). When their visual or perceptual hallucinations are used as justifications for actions, errors become security/authorization failures rather than just wrong answers. Existing defenses that rely on model text, prompts, or neural judges are insufficient under strong adversarial conditions. There is a need for a principled way to ensure that high-privilege actions are authorized only by reliable evidence about the environment, not by the model’s unverified beliefs.

Method: The authors formalize hallucination-to-action conversion (HAC) where an unsupported perceptual claim fulfills the preconditions for an action. They introduce Evidence-Carrying Agents (ECA), which (1) decompose each tool call into explicit action-critical predicates; (2) obtain typed, structured certificates for those predicates from constrained verifiers (DOM/OCR/AX) instead of free-form language; and (3) pass these certificates to a deterministic gating mechanism that only grants privileges supported by verified evidence. They expose and analyze residual errors (from verifiers, schemas, or implementations) via verifier red-teaming, iteratively hardening the system to reduce gate bypass. They further run end-to-end pipelines, browser-based proofs of concept, HACR audits, and oracle-certificate replays on large benchmark traces to validate correctness and robustness.

Result: Verifier red-teaming across ~1,900 adversarial attacks reduces gate bypass from 15% to 1.3% after four hardening iterations, showing the architecture’s improvability. With content-derived certificates, ECA attains a 0% observed unsafe-action rate on a 200-task end-to-end pipeline (Wilson 95% upper confidence bound 2.67%) and a 120-task browser proof-of-concept (upper bound 4.3%). A direct HACR audit over 500 stratified tasks shows that naive agents and prompt-only defenses allow unsupported action-critical claims to reach unsafe execution (100% and 49.6% respectively), but ECA prevents such executions. Oracle-certificate replay on 7,488 GPT-5.4 benchmark traces indicates that, assuming correct certificates, the gate behaves correctly, while neural-judge baselines remain circumventable under the same adversarial threat model.

Conclusion: Hallucinations in multimodal agents should be treated as authorization risks when they can justify privileged tool calls. ECA provides a principled mechanism: language models may suggest actions, but only externally verified evidence—encoded as typed certificates and checked by a deterministic gate—can authorize execution. This architecture makes perception errors explicit, localizes residual risks to verifiers and schemas, significantly reduces unsafe actions under adversarial testing, and is more robust than prompt-based or neural-judge defenses in the evaluated settings.

Abstract: Multimodal agents use screenshots, documents, and webpages to choose tool calls. When a false visual claim triggers a click, email, extraction, or transfer, hallucination becomes an authorization failure rather than an answer-quality error. We formalize this failure mode as hallucination-to-action conversion: an unsupported perceptual claim supplies the precondition that makes a privileged action appear permitted. We propose evidence-carrying multimodal agents (ECA), which treat free-form model text as inadmissible evidence. ECA decomposes each tool call into action-critical predicates, obtains typed certificates from constrained DOM/OCR/AX verifiers, and lets a deterministic gate grant only the privileges those certificates support. The architecture does not hide perception error; it converts opaque model belief into named verifier, schema, and implementation residuals. Verifier red-teaming over 1,900 attacks exposes this residual directly: four targeted hardening steps reduce gate bypass from 15% to 1.3%. With content-derived certificates, ECA obtains 0% unsafe-action rate on a 200-task end-to-end pipeline (Wilson 95% upper bound 2.67%) and a 120-task browser proof-of-concept (upper bound 4.3%). A direct HACR audit on 500 stratified task keys shows that unsupported action-critical claims reach unsafe execution for naive agents (100.0%) and prompt-only defense (49.6%), but not for ECA. Oracle-certificate replay on 7,488 GPT-5.4 benchmark traces serves as a gate-correctness sanity check, and neural judge baselines remain bypassable under the same threat model. The resulting principle is simple: model language may propose actions, but external evidence must authorize them.

</details>


### [66] [Not all uncertainty is alike: volatility, stochasticity, and exploration](https://arxiv.org/abs/2605.19215)
*Payam Piray*

Main category: cs.AI

TL;DR: The paper shows that not all uncertainty should encourage exploration: uncertainty from changing rewards (volatility) increases optimal exploration, while uncertainty from noisy observations (stochasticity) decreases it.


<details>
  <summary>Details</summary>
Motivation: Most theories and algorithms assume that greater uncertainty generically promotes exploration. However, real environments contain different types of uncertainty—such as changes in underlying reward structure and random noise in observations—that may have distinct behavioral consequences. A better understanding of how these uncertainty sources differentially influence exploration can improve both models of biological decision-making and algorithms for artificial agents, and may shed light on maladaptive exploration in psychiatric conditions.

Method: The authors study a bandit setting with Gaussian state-space dynamics where latent reward states drift over time (volatility) and are observed through noisy reward signals (stochasticity). They extend the Gittins index framework to this restless bandit setting to analytically characterize how volatility and stochasticity affect optimal exploration. Based on a control-as-inference formulation, they derive a closed-form exploration bonus, CAUSE (Cause-Aware Uncertainty-Sensitive Exploration), that explicitly conditions exploration on inferred structure of environmental noise. They then compare CAUSE with standard exploration heuristics and a Gittins-per-arm strategy in environments with heterogeneous noise properties.

Result: Formally, they prove that volatility and stochasticity, although both increasing posterior uncertainty, have opposite effects on the optimal exploration incentive: higher volatility monotonically increases the value of exploration, whereas higher stochasticity monotonically decreases it. The derived CAUSE exploration bonus inherits these monotonicity properties. Empirically, CAUSE outperforms conventional exploration methods and a per-arm Gittins strategy in bandit tasks where arms differ in their volatility and stochasticity parameters, demonstrating better adaptation to heterogeneous noise structure.

Conclusion: Exploration should be driven not just by the magnitude of uncertainty, but by its inferred causes. When uncertainty stems from environmental change (volatility), optimal agents explore more; when it stems from noisy observations (stochasticity), they should explore less. This noise-inference asymmetry jointly shapes learning and exploration. Mis-inference about the sources of noise can lead to systematically reversed exploration patterns rather than merely weaker or noisier exploration, offering a principled computational framework for interpreting certain psychiatric symptoms and for designing more robust exploration algorithms in artificial systems.

Abstract: Adaptive decision-making in biological and artificial intelligence requires balancing the exploitation of known outcomes with the exploration of uncertain alternatives. Although prior work suggests that uncertainty generally promotes exploration, it has typically treated distinct sources of environmental uncertainty as equivalent. We consider environments with latent reward states that drift over time (volatility) and are observed through noisy outcomes (stochasticity). Both increase posterior uncertainty, yet we show they drive optimal exploration in opposite directions: volatility enhances it, stochasticity suppresses it. We establish this asymmetry formally by extending the Gittins index framework to Gaussian state-space bandits with latent dynamics. We further derive Cause-Aware Uncertainty-Sensitive Exploration (CAUSE), a closed-form exploration bonus obtained via control-as-inference that inherits the same monotonicities. CAUSE outperforms standard exploration strategies in environments with heterogeneous noise structure, and also improves on a Gittins-per-arm policy whose rested-bandit optimality does not transfer to restless settings. Learning and exploration are governed by the same noise-inference asymmetry, and the framework predicts that pathological noise inference produces \emph{reversed} rather than merely impaired exploration, with implications for computational accounts of psychiatric conditions.

</details>


### [67] [SimGym: A Framework for A/B Test Simulation in E-Commerce with Traffic-Grounded VLM Agents](https://arxiv.org/abs/2605.19219)
*Han Li,Vibhor Malik,Zahra Zanjani Foumani,Alberto Castelo,Shuang Xie,Ailin Fan,Keat Yang Koay,Yuanzheng Zhu,Meysam Feghhi,Ronie Uliana,Zhaoyu Zhang,Angelo Ocana Martins,Mingyu Zhao,Francis Pelland,Jonathan Faerman,Nikolas LeBlanc,Aaron Glazer,Andrew McNamara,Zhong Wu,Lingyun Wang*

Main category: cs.AI

TL;DR: SimGym is a VLM-agent-based simulation framework that emulates A/B tests on e-commerce storefronts to predict outcome shifts rapidly and safely.


<details>
  <summary>Details</summary>
Motivation: Traditional A/B testing in e-commerce is slow, traffic-hungry, and may harm user experience while experiments run. The authors want a faster, lower-risk way to evaluate UI changes, particularly visually driven ones, without diverting real traffic for long periods.

Method: They build SimGym, which has: (a) a persona generation pipeline that turns real clickstream data into buyer archetypes and intents for each shop; (b) a live-browser, vision-language-model agent with multimodal perception, episodic memory, and guardrails that conducts end-to-end shopping sessions on both control and treatment variants; and (c) an evaluation protocol that compares outcome shifts produced by these simulated sessions to actual shifts measured in real A/B tests.

Result: On A/B tests from a large e-commerce platform involving UI theme changes across many storefronts and categories, SimGym’s simulated agents match real-world outcome shifts well, with 77% agreement in the direction of add-to-cart changes between interface variants, and with experiments that can be run in under an hour instead of weeks.

Conclusion: SimGym can approximate the impact of storefront UI changes on buyer behavior using simulated VLM agents, substantially speeding up experimentation cycles and reducing risk to real users by testing variants before live deployment.

Abstract: A/B testing remains the gold standard for evaluating modifications to e-commerce storefronts, yet it diverts traffic, requires weeks to reach statistical significance, and risks degrading user experience. We present SimGym, a framework for simulating A/B tests on e-commerce storefronts using vision-language model (VLM) agents operating in a live browser. The framework comprises three key components: (a) a traffic-grounded persona generation pipeline that derives per-shop buyer archetypes and intents from production clickstream data; (b) a live-browser agent architecture that combines multimodal perception over visual and browser-structured observations with episodic memory and guardrails to conduct coherent shopping sessions across control and treatment storefronts; and (c) an evaluation protocol that compares simulated outcome shifts with observed shifts in real buyer behavior. We validate SimGym on A/B tests of visually driven UI theme changes from a major e-commerce platform across diverse storefronts and product categories. Empirical results show that SimGym agents achieve strong agreement with observed outcome shifts, attaining 77% directional alignment with add-to-cart shifts observed across interface variants in real-buyer traffic. It reduces experimental cycles from weeks to under an hour, enabling rapid experimentation without exposing real buyers to candidate variants.

</details>


### [68] [Causal Evidence for Attention Head Imbalance in Modality Conflict Hallucination](https://arxiv.org/abs/2605.19250)
*Jinrui Jiang,Zhangtai Wu,Zhen Wu,Xinyu Dai*

Main category: cs.AI

TL;DR: The paper studies why multimodal LLMs trust wrong text over correct images and introduces a method (MACI) that reduces such modality-conflict hallucinations by intervening on specific attention heads.


<details>
  <summary>Details</summary>
Motivation: Multimodal LLMs often hallucinate when textual input conflicts with visual evidence, preferring the wrong text and ignoring the image. Existing work mostly detects or mitigates hallucinations at the output level, without explaining which internal components cause or prevent this failure. The authors aim to mechanistically understand and then control the internal attention-head circuits responsible for modality-conflict hallucination.

Method: The authors use mechanistic interpretability tools, especially head-level causal analysis with path patching, across five open-source MLLMs. They identify attention heads whose activations causally increase hallucinations (hallucination-driving heads) and those that reduce them (hallucination-resisting heads). They study the distribution and strength of these effects, show an asymmetry between driving and resisting heads, and perform ablation experiments to validate causal roles. Based on this, they design MACI, a conditional inference-time intervention that selectively suppresses hallucination-driving heads only when a modality conflict between text and image is detected.

Result: Analysis reveals two distinct sets of attention heads with opposing causal roles in modality-conflict hallucinations. Driving heads are numerous and distributed with large aggregate influence, while resisting heads are few but individually important. This structure biases generation toward erroneous textual premises. The proposed MACI intervention, which suppresses driving heads under detected conflict, substantially reduces hallucinations on the MMMC benchmark across five MLLMs, outperforming other inference-time baselines while maintaining competitive accuracy. MACI also generalizes zero-shot to another benchmark, SCI-SemanticConflict.

Conclusion: Modality-conflict hallucinations in MLLMs are shaped by an internal imbalance between widely distributed hallucination-driving attention heads and a small set of concentrated hallucination-resisting heads. By causally identifying and conditionally intervening on the driving heads, one can re-route the model’s computation to favor visual evidence in conflict scenarios. The MACI method demonstrates that mechanistic, head-level causal interventions are an effective and transferable way to mitigate hallucinations in multimodal models at inference time.

Abstract: Modality-conflict hallucination occurs when multimodal large language models (MLLMs) prioritize erroneous textual premises over contradictory visual evidence. To understand why visual evidence fails to prevail during generation, we take a mechanistic perspective and examine which internal components drive or resist this failure. We perform head-level causal analysis using path patching across five open-source MLLMs and identify two groups of attention heads with opposing causal roles: hallucination-driving heads and hallucination-resisting heads. We find a consistent asymmetry: driving effects are more broadly distributed and carry greater aggregate weight, whereas resisting effects concentrate in a small number of high-importance heads. Ablation experiments further confirm that these groups exert opposing effects during generation: distributed driving influence and localized resistance together form an imbalanced routing structure that biases generation toward the erroneous premise. Motivated by this finding, we propose MACI (Modality-conflict-Aware Causal Intervention), a conditional intervention that suppresses causally identified hallucination-driving heads only when conflict is detected. Across five MLLMs, MACI achieves the largest hallucination reduction among compared inference-time baselines on the MMMC benchmark with a favorable hallucination-accuracy trade-off, and transfers zero-shot to the SCI-SemanticConflict test.

</details>


### [69] [AQuaUI: Visual Token Reduction for GUI Agents with Adaptive Quadtrees](https://arxiv.org/abs/2605.19260)
*Yuankai Li,Tinghui Zhu,Ha Min Son,Zhe Zhao,Xin Liu,Muhao Chen*

Main category: cs.AI

TL;DR: AQuaUI is a training-free quadtree-based token reduction method for GUI agents that exploits non-uniform information density in GUI screenshots to reduce visual tokens and speed up inference with minimal performance loss.


<details>
  <summary>Details</summary>
Motivation: GUI-agent LMMs must process high-resolution screenshots at every interaction step, but these images contain large homogeneous regions and small, information-dense areas (text, icons). Current efficiency methods either need extra training or use attention-based compression that ignores the structured, spatially redundant nature of GUI layouts. There is a need for a simple, training-free approach that reduces visual tokens while respecting GUI structure and preserving performance.

Method: AQuaUI builds an adaptive quadtree over each GUI screenshot, splitting regions according to their visual complexity and keeping one merged visual token per quadtree leaf. This explicitly leverages spatial redundancy by coarsely representing homogeneous areas and finely segmenting complex regions. The method preserves the spatial coordinates of all retained tokens so that position encodings remain consistent throughout the model pipeline. For multi-step GUI interactions, a conditional quadtree variant refines the current quadtree using the previous step’s quadtree as a reference, thereby maintaining fine-grained coverage of regions that stay static or move slightly across frames. The approach operates purely at inference time and does not require retraining of the underlying LMM-based GUI agents.

Result: When integrated into state-of-the-art GUI agent models and evaluated on standard grounding and navigation benchmarks, AQuaUI provides better accuracy–efficiency trade-offs compared to existing token reduction baselines. On the GUI-Owl-1.5-32B-Instruct model, AQuaUI yields up to 13.22% inference speedup and a 29.52% reduction in visual tokens, while preserving 99.06% of the performance of using all visual tokens.

Conclusion: The paper demonstrates that GUI screenshots contain substantial spatial redundancy that can be systematically exploited at inference time without retraining. By using quadtree-based adaptive token reduction that respects layout and temporal continuity, AQuaUI reduces visual token usage and speeds up GUI agents while maintaining near-original performance. This indicates that structured, layout-aware token reduction is a powerful and practical tool for scaling LMM-based GUI interaction systems.

Abstract: Large Multimodal Models (LMMs) have recently emerged as promising backbones for GUI-agent models, where high-resolution GUI screenshots are introduced to the prompts at each iteration step. However, these screenshots exhibit highly non-uniform spatial information density: large regions may carry little information and are visually homogeneous, while key text and icons may require high visual fidelity. Existing approaches to this problem either require additional training or rely on attention-based token compression, ignoring the structured layout and spatial redundancy of GUI screenshots. To fill the gap, this paper proposes AquaUI, a training-free inference-time token reduction method for GUI agent models that utilizes the non-uniform information density in screenshots. AQuaUI constructs an adaptive quadtree on each screenshot input and keeps one representative merged token per leaf of the quadtree. AQuaUI preserves the spatial positions of retained tokens throughout the pipeline to ensure that all position-encoding stages remain consistent. To further improve temporal consistency across multi-step GUI interactions, we propose a conditional quadtree algorithm that leverages the continuity between consecutive screenshots within a single request. Specifically, it refines the current quadtree using previous quadtrees as references, helping preserve fine-grained regions across static or mildly shifted GUI states. We implement AQuaUI on state-of-the-art GUI agent models and conduct experiments on standard grounding and navigational benchmarks. AQuaUI consistently shows improved accuracy-efficiency trade-offs over prior baselines. Notably, on GUI-Owl-1.5-32B-Instruct, AQuaUI achieves up to 13.22% speedup and 29.52% fewer visual tokens while retaining 99.06% of full-token performance, suggesting that the spatial redundancy of GUI screenshots can be exploited at inference without retraining.

</details>


### [70] [Swimming with Whales: Analysis of Power Imbalances in Stake-Weighted Governance](https://arxiv.org/abs/2605.19264)
*Yuzhe Zhang,Manvir Schneider,Qin Wang,Davide Grossi*

Main category: cs.AI

TL;DR: The paper analyzes how stake-weighted voting in Proof-of-Stake blockchains leads to power imbalances, quantifying them with the Penrose-Banzhaf power index and validating findings analytically and with real-world governance data.


<details>
  <summary>Details</summary>
Motivation: In Proof-of-Stake blockchains, governance typically uses voting where a participant’s influence is proportional to their stake. This raises serious concerns about centralization of power, where a small group of large stakeholders can effectively control outcomes without owning all the stake. There is a need for a rigorous, quantitative understanding of how voting power deviates from nominal stake share, and whether stake-weighted systems can ever be fair or stake-proportional in terms of actual influence.

Method: The authors use tools from computational social choice, specifically the Penrose-Banzhaf power index, to measure decision-making power in stake-weighted voting games. They develop analytical results characterizing the relationship between stake shares and power indices, and identify conditions under which the expected power approximates stake ownership. They then perform an empirical study using real voting and stake distribution data from the Project Catalyst on-chain governance system to compute power indices and observe power imbalances in practice.

Result: Analytically, the paper shows that exact proportionality between stake share and Banzhaf power is in general impossible but that, under certain assumptions, the expected Banzhaf power of voters can approximate their relative stake. Empirically, using Project Catalyst data, the authors quantify how much actual power deviates from stake shares, revealing concrete patterns and degrees of concentration of power among large stakeholders in a live PoS governance setting.

Conclusion: The study concludes that stake-weighted voting inherently exhibits power imbalances, with voting power not perfectly matching stake shares. Although under some conditions proportionality can be approximated in expectation, real systems still show significant distortions, especially favoring large stakeholders. These findings suggest that designers of PoS governance should be aware of and potentially mitigate such imbalances, possibly by adjusting voting rules or considering alternative mechanisms to better align actual decision-making power with desired fairness criteria.

Abstract: Voting methods weighted by stakes are the fundamental governance paradigm in Proof-of-Stake (PoS) blockchains. Such a paradigm is known to be prone to power distortions: a few users possessing large stakes may completely control decision making, even without owning the totality of the stakes. We study this phenomenon through the lens of computational social choice, focusing on the extent of power imbalances in stake-weighted voting when power is quantified using the Penrose-Banzhaf power index. Our work presents both analytical and empirical contributions. Analytically, we demonstrate that while a perfect alignment between power and relative stake ownership is generally unattainable, it can be approximated in expectation under specific conditions. Empirically, using data from a real-world on-chain governance system (Project Catalyst), we provide a more fine-grained understanding of the power imbalances that are likely to occur in current stake-weighted governance systems.

</details>


### [71] [MOCHA: Multi-Objective Chebyshev Annealing for Agent Skill Optimization](https://arxiv.org/abs/2605.19330)
*Md Mehrab Tanjim,Jayakumar Subramanian,Xiang Chen,Branislav Kveton,Subhojyoti Mukherjee,Anlan Zhang,Sungchul Kim,Somdeb Sarkhel,Sunav Choudhury*

Main category: cs.AI

TL;DR: The paper introduces MOCHA, a multi-objective optimization method for LLM agent skills that better navigates platform constraints and outperforms existing prompt optimizers in both accuracy and Pareto coverage.


<details>
  <summary>Details</summary>
Motivation: LLM agents rely on “skills” (structured prompts) that must satisfy strict platform constraints such as truncation, compact instruction bodies, and limited context windows. Optimizing these skills is inherently multi-objective: they must balance task performance with these resource limits. Existing prompt optimization methods typically reduce multiple objectives to a single weighted score, which can miss Pareto-optimal solutions, especially in non-convex regions. This creates a gap: there is no principled optimizer that fully explores trade-offs between performance and constraints for skill design.

Method: The authors propose MOCHA (Multi-Objective Chebyshev Annealing), a framework that performs multi-objective optimization over LLM skills. It uses Chebyshev scalarization to transform multiple objectives into a single scalar in a way that preserves coverage of the entire Pareto front, including non-convex areas, avoiding the limitations of simple weighted sums. This is combined with an exponential annealing schedule that gradually shifts from exploratory search to exploitative refinement. All methods in the experiments share the same multi-objective mutation operator and receive comparable per-objective textual feedback, isolating the contribution of MOCHA’s selection strategy.

Result: Across six diverse LLM agent skills, prior optimizers often fail to improve over the initial (seed) skill; in four of six tasks, even 1000 optimization rollouts show no progress. In contrast, MOCHA achieves consistent gains on all tasks, delivering a 7.5% average relative improvement in mean correctness over the best baseline, with particularly strong gains of up to 14.9% on FEVER and 10.4% on TheoremQA. MOCHA also discovers roughly twice as many Pareto-optimal skill variants as competing methods, showing better exploration of the trade-off space between performance and constraints.

Conclusion: MOCHA provides an effective multi-objective optimization strategy for LLM skills that explicitly accounts for platform constraints while improving task performance. By leveraging Chebyshev scalarization and annealing-based exploration–exploitation, it recovers Pareto-optimal skill variants that standard, single-objective or weighted-sum prompt optimizers miss. This leads to consistent performance improvements and richer trade-off frontiers, suggesting that multi-objective, Pareto-aware approaches are crucial for robust skill design in constrained LLM agent platforms.

Abstract: LLM agents organize behavior through skills - structured natural-language specifications governing how an agent reasons, retrieves, and responds. Unlike monolithic prompts, skills are multi-field artifacts subject to hard platform constraints: description fields are truncated for routing, instruction bodies are compacted via progressive disclosure, and co-resident skills compete for limited context windows. These constraints make skill optimization inherently multi-objective: a skill must simultaneously maximize task performance and satisfy platform limits. Yet existing prompt optimizers either ignore these trade-offs or collapse them into a weighted sum, missing Pareto-optimal variants in non-convex objective regions. We introduce MOCHA (Multi-Objective Chebyshev Annealing), which replaces single-objective selection with Chebyshev scalarization - covering the full Pareto front, including non-convex regions - combined with exponential annealing that transitions from exploration to exploitation. In our experiments across six diverse agent skills - where all methods share the same multi-objective mutation operator and baselines receive identical per-objective textual feedback - existing optimizers fail to improve the seed skill on 4 of 6 tasks: 1000 rollouts yield zero progress. MOCHA breaks through on every task, achieving 7.5% relative improvement in mean correctness over the strongest baseline (up to 14.9% on FEVER and 10.4% on TheoremQA) while discovering twice as many more Pareto-optimal skill variants.

</details>


### [72] [Agentic Trading: When LLM Agents Meet Financial Markets](https://arxiv.org/abs/2605.19337)
*Yihan Xia,Panpan You,Taotao Wang,Fang Liu,Han Qi,Xiaoxiao Wu,Shengli Zhang*

Main category: cs.AI

TL;DR: Survey of 77 studies on LLM-based trading agents, focusing on how they’re architected and evaluated, finding widespread protocol and reproducibility issues.


<details>
  <summary>Details</summary>
Motivation: LLM-based trading agents are rapidly appearing in the literature, but the field lacks a clear map of approaches, common evaluation protocols, and reproducible evidence. The authors aim to systematically organize existing work, identify methodological gaps, and provide tools (evidence map, audit, checklist) to improve future research quality and comparability.

Method: Systematic, protocol-driven mapping study of 77 papers on LLMs in trading, screened up to 2026-03-09. They define inclusion boundaries, with a primary empirical subset (n=19) that must at least emit trading actions and be evaluated in closed loop. Each study is coded along dimensions like split protocol, transaction-cost modeling, universe/survivorship handling, execution semantics, and reproducibility level (R0–R3). They adopt an Architecture–Capability–Adaptation lens rather than a rigid taxonomy due to heterogeneity and poor comparability of protocols.

Result: Among the 19 primary empirical studies, only 2 report extractable, time-consistent data split protocols; only 1 models transaction costs; only 1 documents asset universe or survivorship handling; 11 describe execution timing/semantics; 15 are at the lowest reproducibility level (R0); no study reaches the highest reproducibility level (R3). Across the wider set, architectural diversity is high but evaluation practices are inconsistent and rarely documented in a way that enables fair comparison or replication.

Conclusion: Current LLM-based trading agent research is rich in architectural experimentation but weak in standardized, transparent evaluation and reproducibility. Because of pervasive protocol incomparability, the authors refrain from proposing a definitive taxonomy, instead using a more flexible analytical lens and emphasizing their evidence ledger, reproducibility audit, and reporting checklist as actionable contributions. They argue that improving evaluation protocols, execution semantics, and artifact release is the key bottleneck the field must address to mature.

Abstract: A growing body of work explores how Large Language Models (LLMs) can be embedded in trading systems as agents that perceive market information, retrieve context, reason about decisions, emit tradable actions, and adapt under market feedback. This paper reframes LLM-based trading agents as expert-system decision pipelines and presents an audit-oriented evidence map of 77 included studies in a protocol-coded snapshot screened through 2026-03-09. A primary empirical subset (n=19) satisfies the minimum boundary of Action Output plus Closed-Loop Evaluation; the remaining 58 included studies are retained as background and design context. The central empirical finding is protocol incomparability: within the primary subset, only 2/19 studies report extractable time-consistent split protocols, 1/19 reports an explicit transaction-cost model, 1/19 documents universe or survivorship handling, 11/19 report execution timing or semantics, 15/19 are coded as R0, and no study reaches R3 reproducibility. We therefore use Architecture-Capability-Adaptation as a working analytical lens rather than a validated taxonomy, and we foreground the evidence ledger, reproducibility audit, and reporting checklist as the main contributions. The resulting survey shows that architectural experimentation is expanding rapidly, while comparable evaluation protocols, execution semantics, and reproducible artifacts remain the field's immediate bottlenecks.

</details>


### [73] [Generative Recursive Reasoning](https://arxiv.org/abs/2605.19376)
*Junyeob Baek,Mingyu Jo,Minsu Kim,Mengye Ren,Yoshua Bengio,Sungjin Ahn*

Main category: cs.AI

TL;DR: Introduces GRAM, a probabilistic, generative form of Recursive Reasoning Models that uses stochastic latent trajectories for multi-trajectory reasoning and generation, outperforming deterministic baselines.


<details>
  <summary>Details</summary>
Motivation: Existing Recursive Reasoning Models refine a latent state recursively but are deterministic, following a single latent trajectory and converging to one prediction. This limits their ability to represent multiple hypotheses, alternative solution strategies, or perform scalable inference via exploring many reasoning paths.

Method: Propose Generative Recursive reAsoning Models (GRAM), which treats reasoning as a stochastic latent trajectory. GRAM defines a latent-variable generative model that supports conditional reasoning pθ(y|x) and unconditional generation pθ(x) by sampling and refining multiple latent trajectories using shared transition functions. The model is trained with amortized variational inference, and inference-time scaling is achieved through varying recursion depth and parallel sampling of trajectories.

Result: GRAM outperforms deterministic recurrent and recursive baselines on structured reasoning and multi-solution constraint satisfaction benchmarks, and additionally demonstrates the ability to do unconditional generation, which prior RRMs typically lack.

Conclusion: Turning recursive reasoning into a probabilistic, generative latent-variable process enables richer, multi-trajectory computation that better handles multiple solutions and strategies and can scale at inference time while improving performance over deterministic RRMs and RNNs.

Abstract: How should future neural reasoning systems implement extended computation? Recursive Reasoning Models (RRMs) offer a promising alternative to autoregressive sequence extension by performing iterative latent-state refinement with shared transition functions. Yet existing RRMs are largely deterministic, following a single latent trajectory and converging to a single prediction. We introduce \emph{Generative Recursive reAsoning Models (GRAM)}, a framework that turns recursive latent reasoning into probabilistic multi-trajectory computation. GRAM models reasoning as a stochastic latent trajectory, enabling multiple hypotheses, alternative solution strategies, and inference-time scaling through both recursive depth and parallel trajectory sampling. This yields a latent-variable generative model supporting conditional reasoning via $p_θ(y \mid x)$ and, with fixed or absent inputs, unconditional generation via $p_θ(x)$. Trained with amortized variational inference, GRAM improves over deterministic recurrent and recursive baselines on structured reasoning and multi-solution constraint satisfaction tasks, while demonstrating an unconditional generation capability. \href{https://ahn-ml.github.io/gram-website/}{https://ahn-ml.github.io/gram-website}

</details>


### [74] [Conflict-Resilient Multi-Agent Reasoning via Signed Graph Modeling](https://arxiv.org/abs/2605.19418)
*Longgang He,Longzhu He,Daojing He,Chaozhuo Li*

Main category: cs.AI

TL;DR: The paper proposes SIGMA, a signed-graph-informed multi-agent reasoning framework for LLM-based multi-agent systems, improving robustness and accuracy by modeling trust and conflict between agents.


<details>
  <summary>Details</summary>
Motivation: Existing LLM-based multi-agent systems outperform single agents but suffer from naive aggregation mechanisms that assume full cooperation, leading to uncontrolled error propagation, lack of explicit conflict modeling, and missing structural awareness of inter-agent relations.

Method: SIGMA builds a signed relational graph over selected diverse agents for a query, where edges encode trust, conflict, or neutrality with confidence weights. It performs conflict-aware signed message passing that amplifies information from trustworthy agents and suppresses conflicting ones, then uses a structure- and conflict-aware weighted aggregation to produce final predictions.

Result: Across six benchmark datasets, multiple LLM backbones, and various multi-agent configurations, SIGMA consistently surpasses state-of-the-art baselines in both accuracy and robustness to conflicts, yielding conflict-resilient performance.

Conclusion: Explicitly modeling trust and conflict via a signed interaction graph and performing conflict-aware message passing and aggregation significantly enhances LLM-based multi-agent reasoning, leading to more reliable, globally consistent predictions than existing MAS approaches.

Abstract: LLM-based multi-agent systems (MAS) have demonstrated strong reasoning and decision-making capabilities that consistently surpass those of single LLM agents. However, their performance often suffers from naive aggregation mechanisms that assume uniformly cooperative interactions. Upon close inspection, we observe that existing graph-based MAS frameworks (1) propagate errors when conflicting signals arise without control, and (2) lack explicit modeling of conflicting inter-agent relations as well as structural awareness, failing to identify reliable interaction patterns. To bridge this gap, we introduce SIGMA, a novel SIgned Graph-informed Multi-Agent reasoning framework that explicitly captures trust, conflict, and neutral relations among agents via a signed relational graph. Specifically, given a query, SIGMA first selects a set of relevant and diverse agents, then constructs a structured signed interaction graph with confidence-weighted edges. Reasoning proceeds through conflict-aware signed message passing, which reinforces information from trustworthy agents while suppressing conflicting signals, and terminates with a structure- and conflict-aware weighted aggregation to yield globally consistent and conflict-resilient predictions. Extensive experiments on six benchmark datasets, across multiple LLM backbones and diverse multi-agent configurations, demonstrate that SIGMA consistently outperforms state-of-the-art baselines, achieving notable gains in both accuracy and conflict-resilient performance.

</details>


### [75] [Generative Auto-Bidding with Unified Modeling and Exploration](https://arxiv.org/abs/2605.19457)
*Mingming Zhang,Feiqing Zhuang,Na Li,Shengjie Sun,Xiaowei Chen,Junxiong Zhu,Fei Xiao,Keping Yang,Lixin Zou,Chenliang Li*

Main category: cs.AI

TL;DR: The paper proposes GUIDE, a generative auto-bidding framework that combines directed exploration with a safety fallback to improve efficiency and reduce risk in online advertising auctions, showing significant gains on datasets, simulators, and a large real-world platform.


<details>
  <summary>Details</summary>
Motivation: Existing automated bidding methods either lack adaptability (rule-based) or struggle with long-term dependencies (standard RL). Recent generative models can capture long horizons but have no explicit, reliable way to trade off exploration against safety, leading to risky and inefficient bidding in high-stakes financial environments. There is a need for a unified framework that can explore effectively while guaranteeing safe behavior in production ad systems.

Method: GUIDE builds on a Decision Transformer (DT) that models sequences of bidding actions and environmental states. A Q-value module is used to guide and regularize the DT’s exploratory behavior by incorporating value estimates into the sequence model. In parallel, an Inverse Dynamics Module (IDM) uses the DT-predicted future states to infer actions that are consistent with historical behavior, serving as a robust safe policy. At run time, the Q-value module adaptively chooses between the exploratory DT action and the safe IDM action, forming an integrated explore–safeguard–select decision pipeline.

Result: Through experiments on public datasets, simulated auction environments, and large-scale online A/B tests on Taobao, GUIDE consistently outperforms state-of-the-art baselines across all evaluated settings. In real-world deployment, it yields +4.10% ad GMV, +1.40% ad clicks, +1.66% ad cost, and +3.52% ad ROI, indicating both better performance and favorable economic impact for the platform.

Conclusion: GUIDE effectively unifies generative sequence modeling with value-based exploration and a safety fallback, improving both effectiveness and robustness in auto-bidding. Its empirical success across offline, simulation, and real-world scenarios demonstrates that combining directed exploration with a behaviorally grounded safe policy is a practical and valuable approach for industrial advertising systems.

Abstract: Automated bidding is central to modern digital advertising. Early rule-based methods lacked adaptability, while subsequent Reinforcement Learning approaches modeled bidding as a Markov Decision Process but struggled with long-term dependencies. Recent generative models show promise, yet they lack explicit mechanisms to balance exploration and safety, relying solely on action perturbations or trajectory guidance without a safety fallback. This results in inefficient exploration and elevated financial risk for advertising platforms.
  To address this gap, we propose GUIDE (Generative Auto-Bidding with Unified Modeling and Exploration), a framework that synergistically integrates directed exploration with a safe fallback mechanism. GUIDE employs a Decision Transformer (DT) to jointly model historical bidding actions and environmental state transitions. A Q-value module guides the DT's exploration via regularization constraints, while an Inverse Dynamics Module (IDM) leverages DT-predicted future states to infer robust, behaviorally consistent actions as a safe policy fallback. The Q-value module then adaptively selects the final action between these two options, balancing exploration and safety. Together, these components form an integrated "explore-safeguard-select" pipeline that unifies efficiency and safety.
  We conduct extensive experiments on public datasets, in simulated auction environments, and through large-scale online deployment on Taobao, a leading Chinese advertising platform. Results show GUIDE consistently outperforms state-of-the-art baselines across all scenarios. In real-world deployment, GUIDE achieves notable gains: +4.10% ad GMV, +1.40% ad clicks, +1.66% ad cost, and +3.52% ad ROI, demonstrating its effectiveness and strong industrial applicability.

</details>


### [76] [Beyond Mode Collapse: Distribution Matching for Diverse Reasoning](https://arxiv.org/abs/2605.19461)
*Xiaozhe Li,Yang Li,Xinyu Fang,Shengyuan Ding,Peiji Li,Yongkang Chen,Yichuan Ma,Tianyi Lyu,Linyang Li,Dahua Lin,Qipeng Guo,Qingwen Liu,Kai Chen*

Main category: cs.AI

TL;DR: The paper introduces DMPO, a reinforcement learning method that mitigates mode collapse by approximating forward KL minimization, leading to more diverse solution exploration and better performance on NP-hard and reasoning benchmarks.


<details>
  <summary>Details</summary>
Motivation: On-policy RL methods like GRPO tend to suffer from mode collapse, where the policy collapses onto a single high-reward solution and stops exploring alternative strategies. This is particularly problematic in domains with many feasible solutions but few near-optimal ones, such as NP-hard combinatorial optimization and complex reasoning tasks. The authors are motivated to find a principled, practical way to maintain solution diversity and sustained exploration in on-policy RL.

Method: The authors analyze the cause of mode collapse as the reverse KL minimization used in methods like GRPO, which is inherently mode-seeking. They propose DMPO (Distribution-Matching Policy Optimization), which approximates forward KL minimization to obtain mode-covering behavior. DMPO constructs a group-level target distribution over sampled trajectories, with probabilities proportional to their rewards, and then updates the policy to match this target distribution. This avoids the need to sample from an intractable global target distribution while still encouraging diversity across trajectories.

Result: DMPO is evaluated on NP-hard combinatorial optimization tasks using NP-Bench in both text-based and vision-based settings. It achieves a 43.9% Quality Ratio on text-based NP-Bench compared to GRPO's 40.1%, and 43.1% on vision-based NP-Bench compared to 38.4%, corresponding to 9% and 12% relative improvements. The improvements also transfer to mathematical reasoning tasks (+2.0%) and out-of-domain tasks (+2.3%). These results indicate that DMPO provides better sustained exploration and improved solution quality across multiple modalities and task types.

Conclusion: The paper concludes that distribution matching via approximate forward KL minimization is an effective and practical strategy to prevent mode collapse in on-policy RL. By constructing a reward-weighted target distribution over sampled trajectories and aligning the policy to it, DMPO maintains solution diversity and enables sustained exploration. The consistent gains on NP-hard optimization, mathematical reasoning, and out-of-domain tasks suggest that diversity-preserving training improves general reasoning capabilities across modalities. The work positions distribution matching as a principled foundation for future on-policy RL algorithms aimed at robust, diverse exploration.

Abstract: On-policy reinforcement learning methods like GRPO suffer from mode collapse: they exhibit reduced solution diversity, concentrating probability mass on a single solution once discovered and ceasing exploration of alternative strategies. We show this stems from reverse KL minimization's mode-seeking behavior, which reinforces the first high-reward trajectory found rather than maintaining a distribution over multiple diverse solutions. We propose DMPO (Distribution-Matching Policy Optimization), which prevents mode collapse through principled approximation of forward KL minimization. DMPO constructs a group level target distribution over sampled trajectories proportional to their rewards, then aligns the policy distribution to this target. This provides mode-covering behavior without requiring sampling from the intractable global target distribution, enabling sustained exploration throughout training. We validate DMPO on NP-hard combinatorial optimization, where exponentially many feasible solutions exist but only a few approach optimality, an ideal testbed for evaluating exploration. DMPO achieves 43.9% Quality Ratio on text-based NP-Bench (vs. GRPO's 40.1%) and 43.1% on vision-based NP-Bench (vs. 38.4%), demonstrating 9% and 12% relative improvements respectively. These gains generalize to mathematical reasoning (+2.0%) and out-of-domain tasks (+2.3%), showing that diversity-preserving training enhances general reasoning capabilities across modalities. Our work establishes distribution matching as a practical, principled approach to preventing mode collapse in on-policy RL, with consistent quality improvements demonstrating sustained exploration across diverse reasoning tasks.

</details>


### [77] [Position: The Turing-Completeness of Real-World Autoregressive Transformers Relies Heavily on Context Management](https://arxiv.org/abs/2605.19514)
*Guanyu Cui,Zhewei Wei,Kun He*

Main category: cs.AI

TL;DR: The paper clarifies what it really means for Transformers to be Turing-complete, distinguishing between a fixed deployed system and a scaling family of models, and argues that context management is crucial to their actual computational power.


<details>
  <summary>Details</summary>
Motivation: There is widespread but vague use of the claim that Transformers are Turing-complete, often without specifying the operational setting or matching how real-world LLMs are deployed. This leads to conceptual confusion about what has truly been proven and what capabilities deployed Transformers actually have. The authors are motivated to formalize the realistic setting of a single, fixed Transformer system, to re-interpret existing results, and to highlight the overlooked role of context-management strategies in determining computational power.

Method: The authors (1) give a formal definition of the fixed Transformer system setting, where a single autoregressive Transformer with a fixed architecture is paired with a concrete procedure for managing context across time; (2) contrast this with the scaling-family setting, where a sequence of models with growing context window or precision is used; (3) re-examine existing Turing-completeness proofs in light of these definitions to show they operate in the scaling-family regime; and (4) analyze and compare different context-management schemes to demonstrate how they change the effective computational power of a fixed Transformer system.

Result: They show that most existing proofs of Transformer Turing-completeness actually rely on a scaling-family of models and therefore do not establish Turing-completeness for a single fixed system as typically deployed. They also demonstrate that, within the fixed-system setting, the choice of context-management method can lead to large differences in what computations are possible, revealing that some schemes are far more powerful than others.

Conclusion: The paper concludes that (a) the commonly cited Turing-completeness results for Transformers do not apply directly to a single, real-world LLM system; (b) the correct interpretation of many such results is that they provide resource bounds within a scaling-family framework rather than true Turing-completeness; and (c) context management should be treated as a first-class design choice, as it critically shapes the computational power of deployed autoregressive Transformers.

Abstract: Many works make the eye-catching claim that Transformers are Turing-complete. However, the literature often conflates two distinct settings: (i) a fixed Transformer system setting, in which a fixed autoregressive Transformer is coupled with a fixed context-management method to process inputs of different lengths step by step, and (ii) a scaling-family setting, in which a family of different models (with increasing context-window length or numerical precision) is used to handle different input lengths. Existing proofs of Transformer Turing-completeness are frequently established in setting (ii), whereas real-world LLM deployment and the standard notion of Turing-completeness correspond more naturally to setting (i). In this paper, we first formalize the fixed-system setting, thereby providing a concrete characterization of how real-world LLMs operate. We then argue that results proved in the scaling-family setting provide theoretically meaningful resource bounds but do not establish Turing-completeness, thereby clarifying a common misinterpretation of existing results. Finally, we show that different context-management methods can yield sharply different computational power, and we advocate the position that context management is a central component that critically determines the computational power of real-world autoregressive Transformers.

</details>


### [78] [BLINKG: A Benchmark for LLM-Integrated Knowledge Graph Generation](https://arxiv.org/abs/2605.19518)
*Carla Castedo,Enrique Iglesias,Manuel Lama,Alberto Bugarin-Diz,Maria-Esther Vidal,David Chaves-Fraga*

Main category: cs.AI

TL;DR: The paper introduces BLINKG, a benchmark to evaluate how well large language models map heterogeneous data sources to ontology terms for knowledge graph construction.


<details>
  <summary>Details</summary>
Motivation: Building knowledge graphs requires labor-intensive manual alignment between input data schemas and ontology terms. Existing declarative tools help with transformation but not with the semantic matching step, and there is no standard way to evaluate how well LLMs can perform this mapping. The authors want a systematic, realistic benchmark to measure and compare LLMs’ mapping capabilities.

Method: They design BLINKG, a benchmark consisting of multiple KG-construction scenarios of increasing complexity, grounded in real-world use cases and heterogeneous data sources. They then run an extensive experimental evaluation of several state-of-the-art LLMs on these scenarios, measuring how accurately and robustly the models map schema elements to ontology concepts.

Result: The evaluated LLMs show promising performance on simpler scenarios, demonstrating that they can already assist KG engineers, but their performance degrades on more complex mapping tasks, indicating clear limitations for fully automated KG construction in challenging settings.

Conclusion: BLINKG serves as a standardized framework to assess LLM-based schema-to-ontology mapping for KG construction. Current LLMs are helpful but insufficient for complex cases, and the benchmark allows identifying gaps and defining requirements for (semi)automated, LLM-driven KG construction, thereby guiding future research.

Abstract: Generating Knowledge Graphs (KGs) remains one of the most time-consuming and labor-intensive tasks for knowledge engineers, as they need to identify semantic equivalences between input data sources and ontology terms. While declarative solutions (e.g., RML, SPARQL-Anything) have helped to generalize this process, aligning input schema elements with ontology terms still involves intricate transformations and requires considerable manual effort. With the advent of Large Language Models (LLMs), there is growing interest in leveraging their capabilities to assist KG engineers. Although some studies have explored using LLMs to automate KG construction, there is still no standardized framework for assessing how effectively they establish correspondences between data schemes and ontology concepts. Therefore, in this paper, we propose BLINKG, a benchmark designed to evaluate the mapping capabilities of LLMs in constructing KGs from heterogeneous data sources. The benchmark includes a set of scenarios with increasing complexity, based on real-world use cases. We conduct an extensive experimental evaluation of several stateof-the-art LLMs using BLINK and observe that they already offer promising solutions. However, their performance remains limited in complex scenarios. Thanks to this benchmark, we can already assess the current capabilities of LLMs for KG construction. Additionally, we define a set of requirements for achieving (semi)automated (LLM-driven) KG construction, opening new research lines in this area.

</details>


### [79] [Library Drift: Diagnosing and Fixing a Silent Failure Mode in Self-Evolving LLM Skill Libraries](https://arxiv.org/abs/2605.19576)
*Xing Zhang,Yanwei Cui,Guanghui Wang,Ziyuan Li,Wei Qiu,Bing Zhu,Peiyang He*

Main category: cs.AI

TL;DR: The paper identifies and fixes 'library drift' in self-evolving LLM skill libraries, where uncontrolled skill growth hurts performance, and proposes governance mechanisms that restore strong gains.


<details>
  <summary>Details</summary>
Motivation: Self-evolving agents continually add LLM-authored skills, but uncontrolled accumulation leads to degraded retrieval and stagnating or negative performance gains compared to human-curated skills. The authors are motivated to isolate why this happens and how to systematically diagnose and correct it.

Method: They define and trigger a failure mode called library drift via controlled ablations: disabling skill injection or forcing premature retirement. They introduce trace-level diagnostics with an append-only evidence log that tracks per-skill contributions, attribution decisions, and router engagement. They then design and test a minimal governance recipe combining outcome-driven retirement, a bounded cap on active skills, and a prior on meta-skill authoring, and run 100-round experiments on MBPP+ hard-100 with multiple ablations to understand which components are crucial.

Result: Without governance, LLM-authored skills provide no performance gain, unlike human-curated skills. Disabling injection or enforcing premature retirement reveals different aspects of drift. With the proposed governance recipe, held-out pass@1 improves from 0.258 to a late-window mean of 0.584, a rolling gain of +0.328. Eight ablations show which governance elements are essential versus redundant.

Conclusion: Library drift is a concrete, reproducible failure mode in self-evolving skill libraries, but it can be detected early with trace-level logging and mitigated using simple governance rules. The authors provide both diagnostic tools and a tested governance playbook to maintain and improve performance in self-evolving LLM agents.

Abstract: Self-evolving skill libraries face a silent failure mode we term \emph{library drift}: unbounded skill accumulation without outcome-driven lifecycle management causes retrieval degradation, false-positive injections, and performance stagnation. Recent evaluation confirms the symptom--LLM-authored skills deliver +0.0pp gain while human-curated ones deliver +16.2pp (SkillsBench)--yet the underlying mechanism has not been isolated. We provide (1) a reproducible trigger: ablations that isolate drift--one disables skill injection (flat floor, +0.002), one imposes premature retirement (active harm, $-$0.019); (2) trace-level diagnostics: an append-only evidence log with per-skill contribution scores, attribution verdicts, and router engagement metrics that make the failure visible before it reaches end-task scores; and (3) a verified fix: a minimal governance recipe (outcome-driven retirement + bounded active-cap + meta-skill authoring prior) that lifts held-out pass@1 from a 0.258 baseline to a late-window mean of 0.584 (rolling gain $+$0.328) on MBPP+ hard-100 over 100 rounds. Eight ablations decompose which governance mechanisms are load-bearing and which are subsumed, providing a concrete playbook for diagnosing library drift in any self-evolving agent.

</details>


### [80] [SceneCode: Executable World Programs for Editable Indoor Scenes with Articulated Objects](https://arxiv.org/abs/2605.19587)
*Puyi Wang,Yuhao Wang,Linjie Li,Zhengyuan Yang,Kevin Qinghong Lin,Yangguang Li,Yu Cheng*

Main category: cs.AI

TL;DR: SceneCode converts natural language prompts into executable code that programmatically generates physically-interactable indoor scenes and articulated objects for simulation.


<details>
  <summary>Details</summary>
Motivation: Current indoor scene synthesis methods mainly output static meshes and rely on pre-made, curated asset libraries for articulation. This limits object-level control, prevents on-demand creation of new interactable assets, and makes it hard to trace or edit how scenes are built. There is a need for a system that can synthesize full, physics-ready indoor environments where object structure, articulation, and layout are controllable and interpretable from high-level prompts.

Method: The authors cast indoor scene synthesis as programmatic world generation. Given a natural language prompt, a room-level agentic backbone produces a structured house layout and issues per-object AssetRequests via a planner–designer–critic loop. Each request is sent to one of five code-generation strategies, which synthesize part-wise Blender Python programs describing the asset’s geometry and articulation. These programs are executed and refined through an execution-guided repair-and-refine loop, then compiled into simulation-ready assets and exported as SDF for physics simulation. A persistent scene-state registry maintains links between object requests, code programs, rendered geometry, and simulation assets, making the whole process traceable and locally editable.

Result: SceneCode is evaluated on multiple fronts: full-scene synthesis quality, object-level asset quality, human preference/judgment studies, and downstream robotic interaction tasks in simulation. The experiments show that SceneCode improves prompt-faithful indoor scene generation, yields assets with cleaner mesh structure, and produces simulator-loadable articulation metadata compared to baselines relying on static meshes and pre-curated assets.

Conclusion: Treating indoor scene synthesis as code generation for executable world programs enables more controllable, traceable, and physically-interactable environments. SceneCode demonstrates that compiling language prompts into structured, articulable Blender programs and SDF assets improves scene realism, structural cleanliness, and usefulness for robotics and simulation, suggesting programmatic world generation as a promising direction for embodied AI and simulation-based evaluation.

Abstract: Indoor scene synthesis underpins embodied AI, robotic manipulation, and simulation-based policy evaluation, where a useful scene must specify not only what the environment looks like, but also how its objects are structured. Existing pipelines, however, typically represent generated content as static meshes and inherit articulation only from curated asset libraries, which limits object-level controllability and prevents new interactable assets from being produced on demand. We address this gap by formulating physically interactable indoor scene synthesis as programmatic world generation, and present SceneCode, a framework that compiles a natural language prompt into an executable, code-driven indoor world rather than a collection of opaque meshes. A room-level agentic backbone first turns the prompt into a structured house layout and emits per-object AssetRequests through a planner--designer--critic loop. Each request is then routed to one of five code-generation strategies and converted into a synthesized part-wise Blender Python programs that are validated through an execution-guided repair-and-refine loop. The resulting programs are compiled into simulation-ready assets, and exported as SDF for physics simulation. A persistent scene-state registry links object requests, executable programs, rendered geometry, and simulation assets, turning scene assembly into a traceable and locally editable world-building process. We evaluate SceneCode across scene-level synthesis, object-level asset quality, human judgment, and downstream robot interaction. Results show that executable world programs improve prompt-faithful indoor scene generation and produce assets with cleaner mesh structure, and simulator-loadable articulation metadata. Project page: https://scene-code.github.io/.

</details>


### [81] [Towards Multi-Model LLM Schedulers: Empirical Insights into Offloading and Preemption](https://arxiv.org/abs/2605.19593)
*Mert Yildiz,Pietro Spadaccino,Alexey Rolich,Francesca Cuomo,Andrea Baiocchi*

Main category: cs.AI

TL;DR: The paper empirically studies how multiple LLMs behave under partial CPU-GPU offloading and preemption on heterogeneous hardware, revealing non-linear, model-dependent performance penalties and deriving key considerations for next-generation multi-model LLM schedulers.


<details>
  <summary>Details</summary>
Motivation: Modern LLM services often need to host many different models (sizes, architectures, specializations) on shared heterogeneous hardware. Existing systems and schedulers mainly focus on maximizing throughput for a single model, ignoring complex interactions that arise when multiple models share constrained GPU memory and require CPU-GPU offloading and preemption. There is a lack of detailed empirical understanding of how these mechanisms affect performance across models and platforms, which is crucial for designing effective multi-model schedulers.

Method: The authors conduct an empirical study of various LLMs across different hardware platforms, systematically varying the degree of GPU residency (layer offloading to CPU) and preemption patterns. They measure decode throughput, overheads of preemption (including model state reload and KV cache movement), and how these metrics change with model size, architecture, sequence length, and interconnect bandwidth. They then analyze the resulting performance trends and sensitivities.

Result: The study finds that (1) partial offloading causes strongly non-linear and model-specific degradation in decode throughput; smaller models can be disproportionately sensitive to reduced GPU residency, (2) preemption introduces substantial overhead dominated by reloading model weights rather than moving the KV cache, and this overhead varies widely across models and hardware platforms, and (3) longer sequence lengths and lower interconnect bandwidth further exacerbate data movement costs and execution inefficiencies in hybrid CPU-GPU execution.

Conclusion: Future LLM schedulers for multi-model, heterogeneous environments must explicitly account for: each model’s sensitivity to offloading, workload properties (e.g., sequence length distributions), and the detailed cost structure of preemption and data transfers. Incorporating these empirically observed behaviors can guide the design of more efficient next-generation LLM serving systems that manage hybrid CPU-GPU execution and shared GPU memory effectively.

Abstract: Modern deployments of Large Language Models (LLMs) increasingly require serving multiple models with diverse architectures, sizes, and specialization on shared, heterogeneous hardware. This setting introduces new challenges for resource allocation, dispatching, and scheduling, particularly under GPU memory constraints where partial CPU-GPU offloading and preemption become necessary. While existing systems primarily optimize throughput for a single model, comparatively little work addresses multi-model scheduling under these conditions. In this paper, we present an empirical study of how different LLMs behave across hardware platforms, focusing on the performance implications of layer offloading and preemption. We show that offloading leads to strongly non-linear and model-dependent degradation in decode throughput, with smaller models exhibiting sharper sensitivity to reduced GPU residency. We further demonstrate that preemption incurs substantial overhead, largely dominated by model state reload rather than key-value cache transfer, and that this cost varies significantly across models and hardware platforms. Additionally, we highlight the role of sequence length and interconnect bandwidth in amplifying data movement and execution inefficiencies. Based on these findings, we identify a set of key features that future schedulers must consider, including model-specific offloading sensitivity, workload characteristics, and the cost structure of preemption and data transfer. These insights provide guidance for the design of next-generation LLM serving systems capable of efficiently managing heterogeneous, multi-model workloads with hybrid CPU-GPU execution.

</details>


### [82] [Transforming Constraint Programs to Input for Local Search](https://arxiv.org/abs/2605.19671)
*Jo Devriendt,Patrick De Causmaecker,Marc Denecker*

Main category: cs.AI

TL;DR: The paper proposes an automatic way to generate local search neighborhoods for constraint optimization problems by exploiting symmetry properties, and validates the approach on six classical problems.


<details>
  <summary>Details</summary>
Motivation: Local search for combinatorial optimization usually needs manual encoding of constraints and neighborhoods, which is labor-intensive and problem-specific. The authors want to reduce or remove this manual effort by deriving local search neighborhoods directly from a high-level constraint specification.

Method: They analyze the symmetry properties of constraint optimization problems and show how these symmetries correspond to local search neighborhoods. Within the IDP system (a knowledge-base/constraint solving environment), they implement a procedure that, given a constraint specification, automatically constructs appropriate neighborhoods for local search based on detected symmetries. They then apply this automatic neighborhood generation to six standard optimization benchmarks.

Result: For six classical optimization problems, the automatically generated neighborhoods are effective enough to produce good solutions, demonstrating that the symmetry-based method is viable in practice. The experiments show that the neighborhoods derived from constraint specifications can be competitive with manually designed ones, or at least adequate, though the abstract does not provide exact metrics.

Conclusion: There is a meaningful and exploitable relationship between symmetries in constraint optimization problems and local search neighborhoods. By leveraging this relationship, it is possible to automatically generate useful neighborhoods directly from a constraint specification, as implemented in the IDP system. The empirical study across several classical problems indicates that this is a practical and promising approach to automating local search design.

Abstract: Applying local search algorithms to combinatorial optimization problems is not an easy feat. Typically, human intervention is required to compile the constraints to input data for some metaheuristic algorithm. In this paper, we establish a link between symmetry properties of constraint optimization problems and local search neighborhoods, and we use this link to automatically generate neighborhoods from a constraint specification in the context of the IDP system. We evaluate the obtained neighborhoods for six classical optimization problems. The resulting observations support the viability of this technique.

</details>


### [83] [EngiAI: A Multi-Agent Framework and Benchmark Suite for LLM-Driven Engineering Design](https://arxiv.org/abs/2605.19743)
*Gioele Molinari,Florian Felten,Soheyl Massoudi,Mark Fuge*

Main category: cs.AI

TL;DR: The paper proposes EngiBench, a benchmark suite and EngiAI, a reference multi-agent system, to evaluate LLM-based engineering design agents across workflow, retrieval, and HPC orchestration tasks.


<details>
  <summary>Details</summary>
Motivation: Existing evaluation frameworks for LLM agents focus mostly on single-agent or simple tool-use scenarios and do not capture the complexities of engineering design pipelines that involve multiple agents, simulation tools, retrieval systems, and manufacturing preparation. There is a need for standardized tasks to measure how well LLM-based systems can coordinate these components end-to-end.

Method: The authors design a benchmark with three parts: (1) a workflow benchmark using seven distinct prompt styles that stress different cognitive skills like tool use, semantic disambiguation, conditional branching, and working memory; (2) a RAG benchmark with a gating mechanism that separately scores the contribution of retrieved information to parameter selection; and (3) an HPC benchmark that measures the ability of LLM agents to orchestrate full ML training pipelines on a SLURM cluster. They implement EngiAI, a multi-agent system built on LangGraph with seven specialized agents coordinated by a supervisor to run topology optimization, document retrieval, HPC job orchestration, and 3D printer control.

Result: Across four LLM backends tested on two engineering problems (Beams2D and Photonics2D), proprietary models reach 96–97% average task completion on Beams2D, while open-source 4B-parameter models reach 55–78%, showing a performance gap but also improvement across generations. Conditional branching tasks are notably difficult, reducing completion rates to 20–53% on Photonics2D. In the RAG benchmark, gating demonstrates that retrieval is essential: scores are near 1.0 with retrieval and near zero without it. In the HPC benchmark, one model successfully completes the entire pipeline in all runs, while another manages only 50%, indicating brittleness over long, multi-step workflows.

Conclusion: The benchmark and EngiAI reference system provide a targeted way to evaluate LLM-based multi-agent systems for engineering workflows, revealing differences between model families and highlighting specific weaknesses such as conditional branching and long-horizon instruction following. The results validate the benchmark design—especially the RAG gating—and suggest that while proprietary models currently excel, open-source models are rapidly improving, and more work is needed to enhance reliability on complex, multi-step engineering tasks.

Abstract: Large Language Model (LLM) agents are increasingly applied to engineering design tasks, yet existing evaluation frameworks do not adequately address multi-agent systems that combine simulation, retrieval, and manufacturing preparation. We introduce a benchmark suite with three evaluation dimensions: (1) a workflow benchmark with seven prompt styles targeting distinct cognitive demands-including direct tool use, semantic disambiguation, conditional branching, and working-memory tasks; (2) a Retrieval-Augmented Generation (RAG) benchmark with gated scoring isolating retrieval contributions to parameter selection; and (3) an High Performance Computing (HPC) benchmark evaluating end-to-end ML training orchestration on a SLURM cluster. Alongside the benchmark we present EngiAI, a Multi-Agent System (MAS) reference implementation built on LangGraph that operationalizes the benchmark by coordinating seven specialized agents through a supervisor architecture, unifying topology optimization, document retrieval, HPC job orchestration, and 3D printer control. Across four LLM backends and two EngiBench problems, proprietary models achieve 96-97% average task completion on Beams2D, while open-source 4B-parameter models reach 55-78%, with clear generational improvement. Conditional branching proves most challenging, with task completion dropping to 20-53% for the conditional style on Photonics2D. RAG gating confirms near-perfect retrieval-augmented scores ($\approx 1.0$) versus near-zero without retrieval, validating the evaluation design. On HPC orchestration, one model completes all pipeline steps in 100% of runs while another drops to 50%, revealing that multi-step instruction following degrades over long-running workflows.

</details>


### [84] [Memory-Augmented Reinforcement Learning Agent for CAD Generation](https://arxiv.org/abs/2605.19748)
*Yin Xiaolong,Liu Yu,Shen Jiahang,Lu Xingyu,Ni Jingzhe,Fan Fengxiao,Sang Fan*

Main category: cs.AI

TL;DR: The paper proposes a memory-augmented reinforcement learning framework for agents that automatically generate complex CAD models, significantly improving success rate and geometric consistency.


<details>
  <summary>Details</summary>
Motivation: Existing LLM-based CAD generation methods struggle with complex models that have long operation sequences, varied operation types, and strict geometric constraints, mainly due to broken reasoning chains and the absence of robust error-correction mechanisms. There is a need for a framework that can robustly plan, execute, and verify CAD generation while learning from experience without requiring large-scale labeled data.

Method: The authors encapsulate the geometric kernel into a structured, callable toolchain and build a closed-loop process including design intent understanding, global planning, execution, and multi-dimensional verification. They introduce a dual-track memory module with a case library (past examples) and a skill library (reusable skills), along with a dynamic utility retrieval algorithm to select helpful memories. Reinforcement learning is used both for improving retrieval—avoiding geometrically invalid but semantically similar examples—and for policy optimization, enabling the agent to refine its behavior over time.

Result: Experimental results demonstrate that the proposed framework substantially increases both the generation success rate and geometric consistency for complex CAD models compared to prior LLM-based methods.

Conclusion: A memory-augmented, RL-based CAD generation agent with structured toolchains and dual memory can better handle complex, constraint-heavy CAD tasks, enabling online self-correction and continual improvement without needing extensive new annotations.

Abstract: Automatic generation of computer-aided design (CAD) models is a core technology for enabling intelligence in advanced manufacturing. Existing generation methods based on large language models (LLMs) often fall short when handling complex CAD models characterized by long operation sequences, diverse operation types, and strong geometric constraints, primarily because reasoning chains break and effective error-correction mechanisms are lacking. To address this problem, this paper proposes a memory-augmented reinforcement learning framework for CAD generation agents. The framework encapsulates the underlying geometric kernel into a structured toolchain callable by the agent and builds a closed-loop mechanism of design intent understanding, global planning, execution, and multi-dimensional verification. It also designs a dual-track memory module consisting of a case library and a skill library, and proposes a dynamic utility retrieval algorithm. By introducing reinforcement learning into retrieval and policy optimization, the agent can effectively avoid retrieval traps in which examples are semantically similar but geometrically infeasible, enabling online self-correction and continual evolution without additional large-scale annotated data. Experiments show that the proposed method significantly improves both the success rate and geometric consistency on complex CAD model generation tasks.

</details>


### [85] [CogScale: Scalable Benchmark for Sequence Processing](https://arxiv.org/abs/2605.19758)
*Yannis Bendi-Ouis,Romain de Coudenhove,Xavier Hinaut*

Main category: cs.AI

TL;DR: CogScale is a lightweight synthetic benchmark suite to quickly test how well different neural architectures handle memory and reasoning over sequences at various scales, without needing huge datasets or models.


<details>
  <summary>Details</summary>
Motivation: Evaluating new neural architectures on long-range memory and sequential reasoning typically needs massive training runs, which are expensive and slow down research iteration. There is a need for a small, standardized, and scalable set of tasks that specifically probe cognitive and memory abilities so researchers can screen architectures efficiently before large-scale training.

Method: The authors design CogScale, a collection of 14 synthetic tasks that each target particular cognitive or memory skills (e.g., basic retention vs. more complex reasoning) and can be parametrically scaled in difficulty and length. They then benchmark seven architectures—GRU, LSTM, xLSTM, Echo State Network, Mamba, Transformer Decoder, and Transformer Encoder-Decoder—under strict parameter budgets of 1k, 10k, and 100k parameters. Performance is measured across multiple difficulty levels and scales, using CogScale as a standardized evaluation protocol.

Result: Under small parameter budgets and simpler tasks, classical RNNs and Echo State Networks perform very well at basic information retention. However, as tasks require more complex reasoning and the difficulty and scale increase, these models degrade, and only architectures with attention mechanisms (Transformers) and modern state-space models (e.g., Mamba) consistently sustain high performance across scales.

Conclusion: CogScale successfully distinguishes how different architectures handle scaling in memory and reasoning, showing that while traditional recurrent models are efficient for simple retention under tight parameter constraints, attention-based and state-space models are better suited for complex, large-scale sequential reasoning. The benchmark offers a practical, lightweight tool for rapidly validating new architectural ideas before investing in costly large-scale experiments.

Abstract: The ability to maintain and manipulate information over time is a fundamental aspect of living beings and Artificial Intelligence. While modern models have achieved remarkable success in tasks like natural language processing, evaluating the capacity of novel architectures to process sequential information remains computationally expensive and time-consuming. Testing a new architecture often requires scaling up to massive datasets and models, leading to vast computational costs and slow iteration cycles. In this paper, we propose CogScale, a benchmark of 14 scalable synthetic tasks designed to isolate and evaluate specific cognitive and memory abilities at different parametrizable scales. By providing a standardized, lightweight framework, CogScale allows researchers to rapidly validate architectural innovations before committing to large-scale training. To establish a solid baseline, we evaluate seven distinct architectures: Gated Recurrent Unit (GRU), Long Short-Term Memory (LSTM), xLSTM, Echo State Network (ESN), Mamba, Transformer Decoder, and Transformer Encoder-Decoder. These evaluations are conducted under strict parameter budgets (1k, 10k, and 100k) and across different difficulty levels and scales. Our results show that while classical RNNs and Echo State Networks excel at basic retention within strict parameter budgets, only attention mechanisms and modern state-space models consistently maintain high performance as reasoning complexity and task difficulty scale.

</details>


### [86] [What Really Improves Mathematical Reasoning: Structured Reasoning Signals Beyond Pure Code](https://arxiv.org/abs/2605.19762)
*Yuze Zhao,Junpeng Fang,Lu Yu,Zhenya Huang,Kai Zhang,Qing Cui,Qi Liu,Jun Zhou,Enhong Chen*

Main category: cs.AI

TL;DR: The paper empirically studies how code in pretraining data actually affects reasoning, using controlled 10T-token experiments with separated domains.


<details>
  <summary>Details</summary>
Motivation: Although code is widely used in LM pretraining and is believed to improve reasoning, it is unclear whether code itself causes broad reasoning gains or if other properties of the data are responsible. The authors want to disentangle the impact of executable code from other structured reasoning signals and understand cross-domain trade-offs in training data composition.

Method: They perform controlled pretraining experiments of language models on a 10-trillion-token corpus carefully separated into domains (pure code, code–natural language, math–text mixtures, etc.). They hold various factors constant (e.g., math token budget) and vary the composition and structure of code and math data. They then evaluate on programming and mathematical reasoning benchmarks and analyze routing/expert activation patterns inside the models to see how different data compositions affect capability transfer and interference.

Result: 1) When they use only standalone executable code and control for code–NL mixtures, code clearly boosts programming skills but does not generally improve reasoning and can hurt knowledge-intensive and complex math tasks. 2) The reasoning benefits often associated with code-heavy training are mostly due to cross-domain structured reasoning traces like code–text and math–text, not bare code alone. 3) Densifying structured math-domain samples under a fixed math token budget significantly improves hard math reasoning without much sacrifice in coding performance. 4) Routing analyses show that expert-activation patterns track these data composition effects, revealing both competitive and synergistic interactions between domains.

Conclusion: Executable code by itself is not a universal reasoning enhancer; its main benefit is for programming, and it can compete with other domains such as complex math. Structured, cross-domain reasoning traces (e.g., math- or code-linked text) and carefully designed data composition are key to improving reasoning while balancing cross-domain trade-offs. This points to data-centric strategies that emphasize cognitive scaffolds rather than simply scaling code tokens.

Abstract: Code has become a standard component of modern foundation language model (LM) training, yet its role beyond programming remains unclear. We revisit the claim that code improves reasoning through controlled pretraining experiments on a 10T-token corpus with fine-grained domain separation. Our findings are threefold. First, when code is restricted to standalone executable programs and Code-NL data are controlled for, code substantially improves programming ability but does not act as a general reasoning enhancer; instead, it competes with knowledge-intensive tasks, especially complex mathematical reasoning. Second, the reasoning gains often attributed to code are better explained by cross-domain structured reasoning traces, such as code-text and math-text mixtures, rather than by executable code alone. Third, increasing the density of structured math-domain samples within a fixed math budget yields substantial gains on difficult mathematical reasoning while largely preserving programming performance, suggesting that cognitive scaffolds offer a targeted way to mitigate cross-domain trade-offs. Finally, routing analyses show that data-composition effects are reflected in expert-activation patterns, providing mechanism-level evidence for competitive and synergistic interactions across domains. Our results clarify which data characteristics transfer across capability dimensions and point to more precise data-centric optimization strategies.

</details>


### [87] [GroupAffect-4: A Multimodal Dataset of Four-Person Collaborative Interaction](https://arxiv.org/abs/2605.19765)
*Meisam Jamshidi Seikavandi,Alice Modica,Anna Obara,Shan Ahmed Shaffi,Fabricio Batista Narcizo,Tanya Ignatenko,Ted Vucurevich,Karim Haddad,Daniel Barratt,Daniel Overholt,Jesper Bunsow Boldt,Paolo Burelli,Andrew Burke Dittberner*

Main category: cs.AI

TL;DR: GroupAffect-4 is a new, richly annotated multimodal dataset for studying affect in small co-located groups, with benchmarks and tools for multi-level analysis.


<details>
  <summary>Details</summary>
Motivation: Prior corpora in affective computing and social signal processing focus on individuals or meetings but rarely model affect as a coupled process spanning individual, interpersonal, and group levels. Moreover, relevant signals—physiology, gaze, audio, self-report, task metrics, and personality—are usually split across different datasets, making integrated analysis difficult. The authors want a single, coherent resource to study affect dynamics in small co-located groups across varied collaborative tasks.

Method: The authors built GroupAffect-4, a corpus of 40 participants organized in 10 four-person groups. Each group performs four different collaborative tasks (information pooling, negotiation, idea generation, and a public-goods game). Each participant wears a wrist physiology sensor, eye-tracking glasses, and a close-talk microphone. Sessions include continuous self-reported affect, post-task questionnaires, task outcomes, and Big-Five personality assessments. All modalities are synchronized to a shared clock. Data coverage and task validity are quantified, and fifteen benchmark prediction targets are defined across three levels (within-person state, between-person traits, and group dynamics). Baseline models are evaluated with a leave-one-group-out setup.

Result: The resulting dataset has high coverage: more than 91% of expected physiology windows and 98% of eye-tracking windows are available. An affective manipulation check on the negotiation task confirms that the tasks elicit meaningful affective variation, supporting ecological validity. The authors define 15 benchmarkable targets and provide baseline feasibility results using leave-one-group-out evaluation to demonstrate what kinds of predictions are possible given the data. The dataset is organized with a BIDS-like structure, Croissant metadata, datasheets, session-level quality reports, and open processing pipelines.

Conclusion: GroupAffect-4 offers a comprehensive, well-structured, and publicly available corpus for modeling affect at individual, interpersonal, and group levels in co-located small groups. Its multimodal nature, validated tasks, benchmark targets, and shared codebase make it a strong foundation for future work on group affect dynamics and for standardized evaluation in this area.

Abstract: Existing affective-computing, social-signal-processing, and meeting corpora capture important parts of human interaction, but they rarely support analysis of affect in co-located groups as a coupled individual, interpersonal, and group-level process. The required signals (per-participant physiology, eye movement, audio, self-report, task outcomes, and personality) are usually fragmented across separate dataset traditions. We introduce GroupAffect-4, a multimodal corpus of 40 participants in 10 four-person groups, each completing four ecologically varied collaborative tasks spanning information pooling, negotiation, idea generation, and a public-goods game. Each participant is instrumented with a wrist-worn physiology sensor, eye-tracking glasses, and a close-talk microphone; sessions include continuous affect self-reports, post-task questionnaires, task outcomes, and Big-Five personality scores, all time-aligned to a shared clock. The dataset covers over 91% of expected physiology windows and 98% of eye-tracking windows, with strong task validity confirmed by a clear affective manipulation check across the negotiation block. We define fifteen benchmarkable targets spanning three analysis levels -- within-person state, between-person traits, and group dynamics -- and report leave-one-group-out feasibility baselines establishing the dataset's evaluative scope. GroupAffect-4 is released with a BIDS-inspired structure, Croissant metadata, a datasheet, per-session quality reports, and open processing scripts. Code and processing scripts are available at https://github.com/meisamjam/GroupAffect-4; the dataset is publicly archived at https://zenodo.org/records/20037847.

</details>


### [88] [Minimax Optimal Variance-Aware Regret Bounds for Multinomial Logistic MDPs](https://arxiv.org/abs/2605.19768)
*Pierre Boudart,Pierre Gaillard,Alessandro Rudi*

Main category: cs.AI

TL;DR: They analyze reinforcement learning in episodic MDPs whose transition dynamics follow a multinomial logistic (MNL) model, and derive tight, problem-dependent regret bounds that refine existing worst-case results.


<details>
  <summary>Details</summary>
Motivation: Existing RL algorithms for MDPs with MNL mixture transitions achieve a worst-case regret of order \tilde{O}(d H^2 sqrt{T}), but this ignores problem-dependent structure that can make learning easier in many practical settings. Logistic bandit work has shown that incorporating variance-like problem parameters can yield sharper bounds, suggesting similar refinements might be possible for MNL mixture MDPs. The goal is to obtain regret bounds that adapt to such structure while also being minimax optimal.

Method: They introduce a problem-dependent constant, \bar{σ}_T ≤ 1/2, representing the normalized average variance of the optimal downstream value function along the learner’s trajectory. They design a new RL algorithm for episodic MNL mixture MDPs whose regret scales with this quantity, using ideas inspired by self-concordant analysis and variance-sensitive confidence constructions from logistic bandits. They then develop matching lower-bound constructions that show no algorithm can do substantially better (up to logs) in terms of this parameter.

Result: The proposed algorithm achieves regret \tilde{O}(d H^2 \bar{σ}_T sqrt{T}). This recovers the previous \tilde{O}(d H^2 sqrt{T}) worst-case bound when \bar{σ}_T is constant, but yields strictly smaller regret for structured MDPs where \bar{σ}_T is small. As a concrete example, for KL-constrained robust MDPs, they show \bar{σ}_T = O(1/H), leading to an improved regret scaling that is better by a factor of H in its dependence on the horizon. They also prove a matching lower bound of Ω(d H^2 \bar{σ}_T sqrt{T}), demonstrating that their upper bound is tight up to logarithmic factors.

Conclusion: They fully characterize the regret complexity of reinforcement learning in MNL mixture MDPs via a problem-dependent variance parameter \bar{σ}_T. Their algorithm is minimax optimal up to logs, adapts to favorable structure in the transition dynamics, and can significantly reduce horizon dependence in settings such as KL-robust MDPs. This bridges the gap between worst-case guarantees and problem-dependent performance for this class of structured MDPs.

Abstract: We study reinforcement learning for episodic Markov Decision Processes (MDPs) whose transitions are modelled by a multinomial logistic (MNL) model. Existing algorithms for MNL mixture MDPs yield a regret of $\smash{\tilde{O}(dH^2\sqrt{T})}$ (Li et al., 2024), where $d$ is the feature dimension, $H$ the episode length, and $T$ the number of episodes. Inspired by the logistic bandit literature (Abeille et al., 2021; Faury et al., 2022; Boudart et al., 2026), we introduce a problem-dependent constant $\barσ\_T \leq 1/2$, measuring the normalised average variance of the optimal downstream value function along the learner's trajectory. We propose an algorithm achieving a regret of $\smash{\tilde{O}(dH^2\barσ\_T\sqrt{T})}$, which recovers the existing bound in the worst case and improves upon it for structured MDPs. For instance, for KL-constrained robust MDPs, $\barσ\_T = O(H^{-1})$, reducing the horizon dependence by a factor $H$. We further establish a matching $\smash{Ω(dH^2\barσ\_T\sqrt{T})}$ lower bound, proving minimax optimality (up to logarithmic factors) and fully characterising the regret complexity of MNL mixture MDPs for the first time.

</details>


### [89] [OpenComputer: Verifiable Software Worlds for Computer-Use Agents](https://arxiv.org/abs/2605.19769)
*Jinbiao Wei,Qianran Ma,Yilun Zhao,Xiao Zhou,Kangqi Ni,Guo Gan,Arman Cohan*

Main category: cs.AI

TL;DR: OpenComputer is a verifier-grounded benchmark and framework for testing and improving computer-use agents on realistic desktop tasks across many applications.


<details>
  <summary>Details</summary>
Motivation: Existing computer-use benchmarks and evaluation methods, especially LLM-as-judge, are unreliable for tasks that require precise understanding of application state. There is a need for a scalable, trustworthy, and fine-grained way to construct and evaluate realistic desktop tasks for agents that operate real software.

Method: The authors build OpenComputer, which combines: (1) app-specific state verifiers that expose structured inspection endpoints on real desktop apps; (2) a self-improving verification layer that learns from execution feedback to become more reliable; (3) a task-generation pipeline that automatically creates realistic, machine-checkable desktop tasks; and (4) an evaluation harness that logs full interaction trajectories and computes auditable, partial-credit scores.

Result: OpenComputer currently supports 33 common desktop applications and 1,000 finalized tasks across many real-world categories. Experiments show that the hard-coded, programmatic verifiers match human evaluation more closely than LLM-based judges, especially for tasks where subtle app state matters. State-of-the-art agents show difficulty completing tasks end-to-end, and open-source models perform much worse than on prior benchmarks like OSWorld-Verified.

Conclusion: Verifier-grounded evaluation using real applications reveals that current computer-use agents are far from robust automation of desktop tasks, and that programmatic verifiers provide more reliable and fine-grained evaluation than LLM-as-judge. OpenComputer exposes substantial performance gaps and offers a scalable platform for developing and assessing more capable agents.

Abstract: We present OpenComputer, a verifier-grounded framework for constructing verifiable software worlds for computer-use agents. OpenComputer integrates four components: (1) app-specific state verifiers that expose structured inspection endpoints over real applications, (2) a self-evolving verification layer that improves verifier reliability using execution-grounded feedback, (3) a task-generation pipeline that synthesizes realistic and machine-checkable desktop tasks, and (4) an evaluation harness that records full trajectories and computes auditable partial-credit rewards. In its current form, OpenComputer covers 33 desktop applications and 1,000 finalized tasks spanning browsers, office tools, creative software, development environments, file managers, and communication applications. Experiments show that OpenComputer's hard-coded verifiers align more closely with human adjudication than LLM-as-judge evaluation, especially when success depends on fine-grained application state. Frontier agents struggle with end-to-end completion despite partial progress, and open-source models exhibit sharp drops from their OSWorld-Verified scores, exposing a persistent gap in robust computer automation.

</details>


### [90] [Distribution-Free Uncertainty Quantification for Continuous AI Agent Evaluation](https://arxiv.org/abs/2605.19779)
*Yuxuan Gao,Megan Wang,Yi Ling Yu*

Main category: cs.AI

TL;DR: The paper develops and applies conformal prediction tools to continuously evaluate AI agents, giving valid uncertainty intervals and abstention rules for scores, rankings, and leaderboards in a distribution-free way.


<details>
  <summary>Details</summary>
Motivation: Existing evaluations of AI agents (e.g., chatbots, recommendation systems) often rely on point estimates of quality or static benchmarks, which fail to quantify uncertainty, adapt to distribution shift (like model updates), or scale reliably to multi-agent pipelines and leaderboards. There is a need for rigorous, distribution-free uncertainty quantification and error control in real-time agent monitoring and comparison, including guarantees on coverage, ranking reliability, and multiple comparisons.

Method: The authors adapt split conformal prediction and adaptive conformal inference (ACI) to the setting of continuous AI agent evaluation, forecasting quality scores at future horizons (e.g., 24 hours). They construct conformal prediction intervals and use ACI to dynamically adjust interval widths as the data distribution shifts after agent releases. They derive compositional uncertainty bounds for multi-stage, multi-agent pipelines, analyzing performance under varying inter-stage correlations. They design a conformal abstention rule for pairwise rankings that controls the false-ranking rate, and introduce false discovery rate (FDR)-corrected abstention for large-scale leaderboard comparisons. Empirically, they evaluate 50 agents using 18 hourly real-time signals, studying conditional coverage per agent and relationships between sentiment divergence and ranking stability, as well as performing a circularity-controlled validation against benchmarks.

Result: Conformal prediction intervals display strong calibration, with calibration error below 0.02 for all nominal coverage levels at a 24-hour forecast horizon. ACI responds appropriately to distribution shifts induced by new agent releases, temporarily widening intervals by 35% and later reconverging as more data accrues. The compositional bounds are validated in simulations across inter-stage correlations from -0.5 to 0.9. The conformal abstention rule achieves controlled false-ranking rates, and the FDR-corrected approach is suitable for leaderboard-scale multiple testing. In real-world evaluation of 50 agents from 18 hourly signals, per-agent conditional coverage clusters tightly around the nominal 80% level, and cross-source sentiment divergence is strongly associated with ranking instability (correlation r = 0.64, p < 0.01). A circularity-controlled validation shows that the proposed framework captures additional signal beyond standard benchmarks (Spearman rho_s = 0.52, p < 0.01, n = 35).

Conclusion: Conformal prediction and adaptive conformal inference can be successfully extended to continuous, real-time AI agent evaluation, yielding well-calibrated uncertainty intervals and principled abstention mechanisms for rankings and leaderboards without distributional assumptions. The framework adapts to system changes such as new agent releases, scales to multi-stage pipelines, and adds value beyond existing benchmarks. The released code and data facilitate adoption of rigorous uncertainty quantification and error control in practical AI monitoring and evaluation pipelines.

Abstract: We adapt split conformal prediction and adaptive conformal inference (ACI) to continuous AI agent evaluation, providing distribution-free coverage guarantees for forecasted quality scores. Conformal intervals achieve calibration error below 0.02 across all nominal levels at the 24h horizon, while ACI correctly widens intervals by 35% following agent releases then reconverges. We further develop compositional uncertainty bounds for multi-agent pipelines (validated via simulation across inter-stage correlations rho in [-0.5, 0.9]), a conformal abstention rule for pairwise rankings with controlled false-ranking rate, and FDR-corrected abstention for leaderboard-scale multiple testing. Evaluating 50 agents via 18 real-time signals collected hourly, we show that per-agent conditional coverage is well-concentrated around the nominal level (mean 80.4%, 90% of agents within [72%, 90%]), and that cross-source sentiment divergence predicts ranking instability (r=0.64, p<0.01). A circularity-controlled validation confirms the framework captures signal beyond benchmarks (rho_s=0.52, p<0.01, n=35). Code and data are released under CC BY 4.0.

</details>


### [91] [From SGD to Muon: Adaptive Optimization via Schatten-p Norms](https://arxiv.org/abs/2605.19781)
*Thomas Massena,Corentin Friedrich,Mathieu Serrurier*

Main category: cs.AI

TL;DR: They propose a data-driven optimizer that dynamically selects the best matrix-wise geometry (within an LMO framework) per layer, interpolating between SGD-, Adam-, and Muon-like behaviors, and show it is competitive or better than strong baselines with minimal overhead.


<details>
  <summary>Details</summary>
Motivation: Existing optimizers such as Muon, Adam, and SGD implicitly fix a geometry (via constraints or preconditioning) for parameter updates. These geometries are chosen manually or empirically and are static during training, which may not be optimal for every layer or training phase. There is a need for a principled and efficient way to adapt the update geometry to the problem and data at runtime.

Method: They frame matrix-wise update constraints under the Linear Minimization Oracle (LMO) theory and derive a closed-form, data-driven criterion that selects a proxy-optimal LMO geometry per layer based on observed gradient and activation statistics. The criterion uses a single-step random feature regression surrogate model to approximate the effect of geometries. The design space spans from SGD-style to Muon-style updates, and adding parameter-wise preconditioning allows the method to subsume SGD, Muon, Adam, and MuAdam as special cases. They implement efficient computational strategies so that the adaptive geometry selection incurs only about 3% runtime overhead relative to optimized baselines.

Result: The proposed adaptive LMO-based optimizer achieves performance that either surpasses or matches the better of Muon and AdamW in three distinct training scenarios, while adding only roughly 3% runtime overhead. It successfully discovers effective geometries from data instead of relying on fixed, hand-chosen ones.

Conclusion: LMO geometry for deep learning optimizers can be effectively and efficiently adapted online using gradient and activation statistics. This adaptive approach opens a pathway to optimizers that automatically choose and interpolate between known update geometries (SGD, Adam, Muon, etc.), leading to competitive or improved performance without substantial computational cost, challenging the convention of static optimizer geometries.

Abstract: Modern optimizers, like Muon, impose matrix-wise geometry constraints on their updates. These matrix-wise constraints can be unified under Linear Minimization Oracle (LMO) theory. However, all current methods impose fixed LMO geometries for the update rules, chosen by-design or empirically, which are not necessarily optimal according to the problem's geometry. We introduce a novel efficient datadriven criterion for dynamically choosing proxy-optimal update LMO geometries on individual Deep Neural Network layers. Derived in closed form from gradient and activation statistics using a single-step random feature regression surrogate model, our criterion navigates a design space interpolating from SGD to Muon updates. Moreover, integrating parameter-wise preconditioning allows our framework to recover SGD, Muon, Adam, and MuAdam as specific extrema. To make this adaptive approach scalable, we pair it with efficient computational strategies, achieving only a $\sim$ 3% runtime overhead on highly optimized baselines. As a proof of concept, we show that this data-driven optimizer beats or remains competitive with the performance of the best performing optimizer between Muon and AdamW across three different training scenarios. Ultimately, this work provides evidence that LMO geometry can be successfully and efficiently adapted from runtime data, opening a new pathway for optimizer design beyond static geometries.

</details>


### [92] [Prior Knowledge or Search? A Study of LLM Agents in Hardware-Aware Code Optimization](https://arxiv.org/abs/2605.19782)
*Dmitry Redko,Albert Fazlyev,Konstantin Sozykin,Maria Ivanova,Evgeny Burnaev,Egor Shvetsov*

Main category: cs.AI

TL;DR: The paper investigates how large language model (LLM) agents actually perform in optimization/discovery loops, focusing on code/kernel optimization, and finds that they rely mainly on pretrained priors, behaving greedily and often ignoring feedback or structural hints.


<details>
  <summary>Details</summary>
Motivation: LLM-based optimization and discovery systems are being widely deployed using propose-evaluate-revise loops, but it is unclear which parts of these complex agentic setups matter, how effectively LLMs use feedback, and under what conditions exploration fails. The authors want a controlled understanding of LLM behavior in such loops rather than relying on anecdotal or system-level benchmarks.

Method: The authors run three controlled experiments on kernel/code optimization tasks: (1) a pure black-box optimization setting where the LLM iteratively proposes solutions given only scalar feedback; (2) a zero-shot kernel generation task where models must generate kernels for different input sizes under varying prompts and temperatures; and (3) a feedback-loop kernel optimization setting where the LLM iteratively refines CUDA and TVM IR kernels based on performance feedback. They carefully vary factors like presence of explicit size information, kernel sizes (common vs uncommon), representation language (CUDA vs TVM IR), and temperature to isolate behavior patterns.

Result: (1) In black-box optimization, LLMs behave like greedy optimizers, exploiting immediate feedback rather than exploring broadly. (2) In zero-shot kernel generation, explicit input-size instructions have no observable effect; the models converge to the same kernel parameters across sizes and temperatures, and performance collapses on uncommon kernel sizes regardless of programming language. (3) In feedback-loop optimization, performance of CUDA code improves steadily with iterations, while TVM IR performance degrades over time, suggesting that working in a lower-density, less familiar representation harms optimization.

Conclusion: For code/kernel optimization tasks, LLMs primarily leverage their pretrained priors instead of meaningfully using iterative feedback or benefitting from complex agentic structures. Their exploration is greedy, they can ignore seemingly critical conditioning information (like input sizes), and they struggle with low-density or unfamiliar languages and uncommon problem settings. This calls into question the effectiveness of current propose-evaluate-revise agent designs and suggests that better mechanisms are needed to induce genuine exploration and feedback utilization.

Abstract: LLM discovery and optimization systems are increasingly applied across domains, implementing a common propose-evaluate-revise loop. Such optimization or discovery progresses via context conditioning on received feedback from an environment. However, as modern LLM agents are increasingly complex in their structure, it is difficult to evaluate which components contribute the most, and when and how this exploration may fail. We answer these questions through three controlled experiments. Our findings: (1) In pure black-box optimization, LLMs act as greedy optimizers. (2) In zero-shot kernel generation, providing explicit input-size information has no measurable effect, models converge to the same kernel parameters regardless of size or temperature, as though the size instruction were invisible. Moreover, when tasked to perform kernel optimization for uncommon kernel sizes, performance sharply degrades regardless of the language used. (3) In feedback-loop kernel optimization, CUDA improves monotonically under iterative feedback, while TVM IR actively degrades, which demonstrates that kernel optimization degrades when models operate with low-density language. Our results conclude that LLMs in code optimization tasks highly depend on pretrained priors rather than provided feedback or agentic structure.

</details>


### [93] [Streamlined Constraint Reasoning via CNN Pattern Recognition on Enumerated Solutions](https://arxiv.org/abs/2605.19895)
*Patrick Spracklen*

Main category: cs.AI

TL;DR: They use CNNs trained on solution vs perturbed non-solution examples, then an LLM to translate the CNN's learned structure into MiniZinc streamliner constraints, dramatically speeding up hardened constraint programming problems.


<details>
  <summary>Details</summary>
Motivation: Existing automated streamliner synthesis either searches a hand-designed grammar or directly prompts an LLM on the model text, which may miss deep structural regularities in actual solutions. There is a need to ground streamliner generation in empirical solution structure, especially for already-hardened benchmark problems where conventional techniques have been exhausted.

Method: 1) Enumerate feasible solutions for a CP model and generate perturbed near-miss non-solutions. 2) Train a convolutional neural network contrastively to distinguish true solutions from perturbations, thereby encoding structural regularities of valid solutions. 3) Use an LLM to synthesize candidate MiniZinc streamliner constraints, guided by the discriminative patterns learned by the CNN rather than only by the text of the CP model. 4) Evaluate and select effective streamliners on hardened benchmark instances.

Result: On hardened Vessel Loading, the approach yields a 98.8% portfolio time reduction, with the best single streamliner giving a 932x geometric-mean speedup. On hardened Social Golfers, it achieves a 98.6% portfolio time reduction and a 356x best-single speedup. On Black Hole, it gives an 89.4% portfolio reduction and a 1103x best-single speedup. The discovered streamliners correspond to intuitive structural constraints: class-based packing for Vessel Loading, stronger symmetry-breaking canonicalisations for Social Golfers, and layout-coordinate bounds for Black Hole.

Conclusion: Grounding LLM-based streamliner synthesis in CNN-learned patterns from actual solutions provides powerful, automatically discovered structural constraints that significantly accelerate solving of already-hardened CP benchmarks. This two-stage neural-symbolic pipeline can uncover non-obvious but effective streamliners, suggesting a promising direction for automated solver guidance beyond hand-crafted hardening.

Abstract: Constraint programming practitioners accelerate hard problems through a layered set of techniques applied in order of risk. Standard hardening (symmetry-breaking and implied constraints) is applied first and preserves satisfiability. Streamliner constraints, which restrict search to a structural sub-family of solutions, do not preserve satisfiability and are reserved as a final lever. Existing automated streamliner-synthesis approaches either search a constraint grammar or prompt a Large Language Model directly on the problem model. We propose a different approach: enumerate feasible solutions, train a Convolutional Neural Network contrastively against perturbed non-solutions to detect structural patterns, and translate the CNN's discriminative signal into candidate MiniZinc streamliners through LLM-driven synthesis. The CNN grounds the LLM's constraint generation in observed solution structure rather than model text alone. We evaluate on hardened benchmark models where streamliner discovery is the residual performance lever. Our pipeline achieves 98.8% portfolio time reduction on hardened Vessel Loading, 98.6% on hardened Social Golfers, and 89.4% on Black Hole, with best-single streamliners reaching geometric-mean speedups of 932x, 356x, and 1103x respectively. Discovered streamliners include class-based packing constraints on Vessel Loading, beyond-hardening canonicalisations on Social Golfers, and layout-coordinate bounds on Black Hole.

</details>


### [94] [Probabilistic Tiny Recursive Model](https://arxiv.org/abs/2605.19943)
*Amin Sghaier,Ali Parviz,Alexia Jolicoeur-Martineau*

Main category: cs.AI

TL;DR: The paper introduces Probabilistic Tiny Recursive Models (PTRM), which add stochastic exploration to tiny recursive reasoning models, greatly improving puzzle-solving accuracy without retraining or high compute cost.


<details>
  <summary>Details</summary>
Motivation: Deterministic Tiny Recursive Models can get stuck in suboptimal solutions during deep recursion, and current workarounds rely on task-specific input perturbations and voting, which are not general and require manual design. The authors want a task-agnostic, efficient way to scale test-time compute and escape bad local optima.

Method: Starting from Tiny Recursive Models, the authors inject Gaussian noise into the latent state at each deep recursion step to create multiple stochastic trajectories. These trajectories explore different solution basins in parallel. The existing Q-value head, originally used for early stopping, is repurposed to score and select the best final answer among trajectories. The method works at test time only, without retraining or task-specific augmentations.

Result: PTRM substantially improves performance on reasoning benchmarks. On Sudoku-Extreme, accuracy increases from 87.4% to 98.75%. On Pencil Puzzle Bench, accuracy rises from 62.6% to 91.2%, nearly doubling the accuracy of strong frontier LLMs (91.2% vs. 55.1%) while using only 7M parameters and less than 0.0001x their computational cost.

Conclusion: Stochastic exploration via Gaussian noise and trajectory selection with an existing Q head provides an effective, task-agnostic test-time compute scaling mechanism for Tiny Recursive Models. PTRM significantly boosts accuracy on complex reasoning tasks, outperforming much larger LLMs at negligible additional cost and without retraining or bespoke data augmentations.

Abstract: Tiny Recursive Models (TRM) solve complex reasoning tasks with a fraction of the parameters of modern large language models (LLMs) by iteratively refining a latent state and final answer. While powerful, their deterministic recursion can lead to convergence at suboptimal solutions, without escape mechanism. A common workaround relies on task-specific input perturbations at test time combined with answer aggregation via voting. We introduce Probabilistic TRM (PTRM), a task-agnostic framework for test-time compute scaling that addresses this limitation through stochastic exploration. PTRM injects Gaussian noise at each deep recursion step, enabling parallel trajectories to explore diverse solution basins, and selects among them using the model's existing Q head (used for early stopping in the original TRM). Without requiring retraining or task-specific augmentations, PTRM enables substantial accuracy gains across benchmarks, including Sudoku-Extreme (87.4% to 98.75%) and on various puzzles from Pencil Puzzle Bench (62.6% to 91.2%). On the latter, PTRM achieves nearly double the accuracy of frontier LLMs (91.2% vs. 55.1%) at less than 0.0001x the cost, using only 7M parameters.

</details>


### [95] [GeoX: Mastering Geospatial Reasoning Through Self-Play and Verifiable Rewards](https://arxiv.org/abs/2605.20006)
*Kyeongjin Ahn,Seungeon Lee,Krishna P. Gummadi,Meeyoung Cha*

Main category: cs.AI

TL;DR: GeoX is a self-play framework that learns geospatial reasoning from satellite/aerial images via executable spatial programs and reinforcement learning, avoiding large human-labeled datasets.


<details>
  <summary>Details</summary>
Motivation: Geospatial reasoning over satellite or aerial imagery is difficult because it requires complex spatial logic and the question space is combinatorial, making human annotation extremely costly. There is a need for a scalable way to train models for such reasoning without relying on millions of manually curated examples.

Method: The authors introduce GeoX, a single-policy multimodal self-play system. Given an input image, the policy generates spatial problems expressed as executable programs and also attempts to solve them under three reasoning paradigms: abduction, deduction, and induction. These programs are defined over spatial primitives and leverage an external image understanding tool. A separate verifier executes the generated programs, producing verifiable outcomes that are transformed into reward signals. These rewards jointly train the problem-proposing and problem-solving behaviors of the model via reinforcement learning, thereby improving its spatial logic capabilities without human labels.

Result: GeoX improves the geospatial reasoning performance of underlying vision-language models by up to 5.5 points on average compared to their original performance, and it matches or surpasses conventional baselines that rely on training with millions of curated, human-annotated data points.

Conclusion: Self-play with executable spatial programs and verifiable rewards is an effective strategy for learning geospatial reasoning from images without large human-labeled datasets. GeoX demonstrates that a unified multimodal policy trained in this way can achieve competitive or superior results to heavily supervised baselines and can also be used to construct a geospatial understanding benchmark via the problems accumulated during self-play.

Abstract: Geospatial reasoning requires solving image-grounded problems over the complex spatial structure of a scene. However, developing this capability is hindered by the cost of annotating a vast and combinatorial question space. We propose GeoX, a self-play framework that acquires spatial logic through executable programs that yield verifiable rewards, without relying on large-scale human-curated data Given a satellite or aerial image, our framework employs a single multimodal policy that proposes spatial problems as executable programs and solves them under three reasoning modes-abduction, deduction, and induction-over spatial primitives and an image understanding tool. A verifier executes each program to covert a reward signal that jointly optimizes the two roles via reinforcement learning. GeoX consistently improves its base VLMs by up to 5.5 points on average, matching or exceeding conventional baselines trained on millions of curated data. Along-side the proposed method, we release a benchmark for geospatial understanding accumulated through self-play.

</details>


### [96] [When Skills Don't Help: A Negative Result on Procedural Knowledge for Tool-Grounded Agents in Offensive Cybersecurity](https://arxiv.org/abs/2605.20023)
*Samuel Jacob Chacko,James Hugglestone,Chashi Mahiul Islam,Xiuwen Liu*

Main category: cs.AI

TL;DR: The paper reanalyzes an LLM-based cybersecurity Capture-the-Flag agent and finds that adding Agent Skills (packaged procedural knowledge) offers little or even negative benefit when the environment already provides rich, structured feedback.


<details>
  <summary>Details</summary>
Motivation: Although Agent Skills are reported to significantly improve task performance across benchmarks, results vary widely and sometimes performance worsens. The mechanisms determining when Skills help versus when they are redundant or harmful are unclear, especially in offensive cybersecurity tasks, motivating a deeper analysis of their true marginal value.

Method: The authors reinterpret four documentation richness conditions (55, 1,478, 1,976, and 4,147 lines) from a 180-run controlled MCP-grounded Capture-the-Flag agent study as a No-Skills, Experiential-Skills, Curated-Skills, and Comprehensive-Skills ablation. They compare task pass rates and statistical effect sizes (chi-square tests, Cochran–Armitage trend test, Cohen’s h) across these conditions and examine how structured environment feedback interacts with Skills usage.

Result: Across the four conditions, the performance gap between no-Skills and full-Skills is only 8.9 percentage points and is not statistically significant (p = 0.71, χ²; p = 0.25, Cochran–Armitage), with most pairwise effect sizes below the small-effect threshold. In some scenarios, such as a timing side-channel setting, adding curated Skills actually degrades agent performance.

Conclusion: The key moderating factor for the usefulness of Agent Skills is environment-feedback bandwidth: when tools return strict, schema-validated, low-latency observations, the environment itself supplies corrective procedural guidance, greatly reducing or even reversing the marginal benefit of Skills. The authors propose a falsifiable hypothesis around this mechanism, discuss implications for designing compound AI systems, and plan to release their reanalysis pipeline for replication.

Abstract: Agent Skills, structured packages of procedural knowledge loaded into an LLM agent at inference time, are widely reported to improve task pass rates by an average of 16.2~percentage points across diverse domains. Yet the same benchmarks show wide variance, with 16 of 84 tasks suffering negative deltas when Skills are introduced. The community has not yet articulated a clean mechanism for \emph{when} Skills help and when they are merely redundant overhead. We re-analyze a recently published 180-run controlled study of an MCP-grounded autonomous Capture-the-Flag (CTF) agent under four documentation conditions of increasing richness (55, 1{,}478, 1{,}976, and 4{,}147 lines), and show that these conditions correspond almost exactly to a No-Skills, Experiential-Skills, Curated-Skills, and Comprehensive-Skills ablation. In offensive cybersecurity, a domain not deeply covered by existing Skills benchmarks, the marginal benefit of Skills collapses. The spread between the no-Skills and full-Skills conditions is only 8.9~pp ($p = 0.71$, $χ^2$; $p = 0.25$, Cochran--Armitage trend test; five of six pairwise Cohen's $h$ values fall below the $0.2$ small-effect threshold). We argue that the missing variable is \emph{environment-feedback bandwidth}. When an agent's tool layer returns strict, schema-validated, low-latency observations, the environment itself supplies the procedural correction signal that Skills are normally needed to provide. As a result, the marginal benefit of curated Skills diminishes substantially, and, in some cases (e.g., our timing side-channel setting), actively degrades performance. We articulate a falsifiable hypothesis, sketch its design implications for compound AI systems, and will release the reanalysis pipeline to support replication.

</details>


### [97] [AutoResearchClaw: Self-Reinforcing Autonomous Research with Human-AI Collaboration](https://arxiv.org/abs/2605.20025)
*Jiaqi Liu,Shi Qiu,Mairui Li,Bingzhou Li,Haonian Ji,Siwei Han,Xinyu Ye,Peng Xia,Zihan Dong,Congyu Zhang,Letian Zhang,Guiming Chen,Haoqin Tu,Xinyu Yang,Lu Feng,Xujiang Zhao,Haifeng Chen,Jiawei Zhou,Xiao Wang,Weitong Zhang,Hongtu Zhu,Yun Li,Jieru Mei,Hongliang Fei,Jiaheng Zhang,Linjie Li,Linjun Zhang,Yuyin Zhou,Sheng Wang,Caiming Xiong,James Zou,Zeyu Zheng,Cihang Xie,Mingyu Ding,Huaxiu Yao*

Main category: cs.AI

TL;DR: The paper introduces AutoResearchClaw, a multi-agent autonomous research system that models research as an iterative, failure-aware process with human-in-the-loop collaboration and cross-run learning, achieving substantial gains over prior AI research agents on a dedicated benchmark.


<details>
  <summary>Details</summary>
Motivation: Existing autonomous research systems treat research as a linear pipeline with single-agent reasoning, fragile execution that stops on failure, and no memory across runs, which does not reflect real-world scientific practice where hypotheses are iteratively refined, failures are informative, and experience accumulates. There is a need for an AI framework that better captures this iterative, multi-perspective, and collaborative nature of scientific discovery.

Method: The authors design AutoResearchClaw, a multi-agent autonomous research pipeline incorporating: (1) structured multi-agent debate for generating and analyzing hypotheses and results; (2) a self-healing execution engine with a Pivot/Refine decision loop that turns experimental failures into information and new directions; (3) verifiable result reporting mechanisms to avoid fabricated metrics and hallucinated citations; (4) human-in-the-loop collaboration with seven graded modes, from full autonomy to step-by-step oversight; and (5) cross-run evolution mechanisms that store and reuse lessons from past failures to prevent repeat mistakes.

Result: On ARC-Bench, a 25-topic benchmark focused on the experiment stage of research, AutoResearchClaw achieves a 54.7% performance improvement over the AI Scientist v2 baseline. Through ablation on the seven human-intervention modes, the authors find that targeted human involvement at key decision points yields better performance than both fully automated operation and exhaustive human oversight at every step.

Conclusion: AutoResearchClaw more faithfully models the iterative, failure-aware, and collaborative nature of scientific research than prior autonomous systems, and its design leads to substantially better performance on an experiment-stage benchmark. The results suggest that multi-agent debate, self-healing execution, verifiable reporting, cross-run learning, and strategically targeted human collaboration can turn autonomous research systems into amplifiers of human scientific judgment rather than replacements for it.

Abstract: Automating scientific discovery requires more than generating papers from ideas. Real research is iterative: hypotheses are challenged from multiple perspectives, experiments fail and inform the next attempt, and lessons accumulate across cycles. Existing autonomous research systems often model this process as a linear pipeline: they rely on single-agent reasoning, stop when execution fails, and do not carry experience across runs. We present AutoResearchClaw, a multi-agent autonomous research pipeline built on five mechanisms: structured multi-agent debate for hypothesis generation and result analysis, a self-healing executor with a \textsc{Pivot}/\textsc{Refine} decision loop that transforms failures into information, verifiable result reporting that prevents fabricated numbers and hallucinated citations, human-in-the-loop collaboration with seven intervention modes spanning full autonomy to step-by-step oversight, and cross-run evolution that converts past mistakes into future safeguards. On ARC-Bench, a 25-topic experiment-stage benchmark, AutoResearchClaw outperforms AI Scientist v2 by 54.7%. A human-in-the-loop ablation across seven intervention modes reveals that precise, targeted collaboration at high-leverage decision points consistently outperforms both full autonomy and exhaustive step-by-step oversight. We position AutoResearchClaw as a research amplifier that augments rather than replaces human scientific judgment. Code is available at https://github.com/aiming-lab/AutoResearchClaw.

</details>


### [98] [Using Aristotle API for AI-Assisted Theorem Proving in Lean 4: A Formalisation Case Study of the Grasshopper Problem](https://arxiv.org/abs/2605.20120)
*Gabriel Rongyang Lau*

Main category: cs.AI

TL;DR: Case study of an AI-generated Lean proof for an IMO problem, showing which parts are formally verified and highlighting a key missing global counting argument.


<details>
  <summary>Details</summary>
Motivation: To understand and precisely characterize what has actually been proved when an AI system generates a seemingly complete formal development for a difficult math problem, and to expose limitations where local proof search works but global combinatorial reasoning is missing.

Method: Perform a detailed Lean 4 formalization case study of an Aristotle API proof attempt for the Grasshopper problem (IMO 2009/6), inspect the generated Lean artifact, identify which lemmas are fully verified, what the main theorem statement is, and which part remains as an unresolved `sorry`, and relate this to the intended mathematical proof structure.

Result: The AI-generated Lean development successfully formalizes a generalized version of the Grasshopper theorem and proves four nontrivial helper lemmas about partial sums, effects of adjacent swaps, the form of modified sums, and a maximality-implies-membership fact. However, the main theorem is closed with a single remaining `sorry`, corresponding to an unresolved global counting argument; the Lean code does not factor this into a separate, proved counting lemma.

Conclusion: AI-assisted theorem proving can produce substantial, correct local reasoning in Lean for complex olympiad problems, but may still fail at the global combinatorial bookkeeping needed to complete the proof. The paper offers a reproducible Lean artifact and a fine-grained analysis of which parts are genuinely verified, illustrating a core limitation of current AI formalization systems.

Abstract: AI-assisted theorem proving can now generate substantial Lean developments for olympiad-level mathematics, but the evidential status of such developments depends on which declarations are actually verified. This paper reports a Lean 4 formalization case study of an Aristotle API proof attempt for the Grasshopper problem, originally posed as IMO 2009 Problem 6. The generated artifact states a generalized Lean version of the theorem, contains four verified helper lemmas for local components of a maximality and adjacent-swap exchange strategy, and leaves the main theorem grasshopper closed directly by one unresolved sorry. The verified components establish that the final partial sum equals the total sum, that an adjacent transposition can affect only the relevant intermediate partial sum, that the changed partial sum has the expected form, and that maximality at a position admitting an adjacent successor swap forces a corresponding forbidden-set membership fact. The Aristotle output summary identifies the intended remaining mathematical step as the global counting step needed to show that these membership facts produce at least n distinct forbidden values, contradicting the cardinality assumption |M| < n; the Lean source itself does not reduce the main theorem to a separately encoded counting lemma. This case study gives an inspectable example of a central limitation in AI-assisted formalization, namely that local proof search can succeed while the global combinatorial bookkeeping required for a theorem remains unresolved. The paper contributes a reproducible Lean artifact and a precise analysis of its verified and unverified proof content.

</details>


### [99] [Not Every Rubric Teaches Equally: Policy-Aware Rubric Rewards for RLVR](https://arxiv.org/abs/2605.20164)
*Utkarsh Tyagi,Xingang Guo,MohammadHossein Rezaei,Daniel George,Anas Mahmoud,Jackson Lee,Bing Liu,Yunzhong He*

Main category: cs.AI

TL;DR: The paper proposes POW3R, a reinforcement learning framework that dynamically reweights rubric-based reward criteria based on policy behavior, improving learning efficiency and final performance while preserving the original human rubric objective.


<details>
  <summary>Details</summary>
Motivation: In many RLHF and post-training settings, model outputs must satisfy several qualitative criteria simultaneously (e.g., helpfulness, safety, style). Rubric-based rewards grade each criterion and aggregate them, but standard static aggregations assume that human-assigned importance weights should also determine the optimization signal strength. In practice, some criteria are already saturated or unreachable, and others that vary meaningfully between rollouts may have low human weights, making learning inefficient and misdirected. The motivation is to decouple what should matter in the final evaluation from what is currently most informative for optimization, enabling more effective use of rubric rewards.

Method: The authors introduce POW3R, a policy-aware rubric reward framework. It keeps the original human rubric—criterion definitions and their importance weights—as the evaluation objective, but dynamically adapts criterion-level reward weights during training. POW3R leverages rollout-level contrast: it analyzes which rubric criteria currently differentiate the policy’s sampled outputs and upweights those criteria as optimization signals, while downweighting saturated or unreachable ones. This produces a modified scalar reward for GRPO (a reinforcement learning algorithm) that is more informative for policy updates, without changing the underlying human rubric target.

Result: On three base policies and two datasets (covering both multimodal and text-only tasks), POW3R is compared to vanilla GRPO with static rubric rewards. Across 30 base-policy/metric combinations, POW3R outperforms the baseline in 24, yielding higher mean rubric reward and higher strict completion rate (the proportion of prompts whose responses satisfy all required rubric criteria). It also converges to the same performance plateau using 2.5–4× fewer training steps, indicating greater sample efficiency and better use of rubric feedback.

Conclusion: Rubric-based rewards should distinguish between two roles of criteria: what should ultimately matter in judging final answers versus what is currently most useful for training the policy. By making rubric rewards policy-aware and contrastive at the criterion level, POW3R improves both efficiency and effectiveness of RL with rubric rewards, while preserving the human-defined objective. This suggests that dynamic, behavior-aware weighting of rubric criteria is crucial for scalable, multi-criterion RLHF-style training.

Abstract: Reinforcement learning with verifiable rewards has made post-training highly effective when correctness can be checked automatically. However, many important model behaviors require satisfying several qualitative criteria at once. Rubric-based rewards address this setting by grading prompt-specific criteria and aggregating them into a scalar reward. Yet standard static aggregations conflate a criterion's human-assigned importance with its current usefulness as an optimization signal. We show that this assumption breaks down in rubric RL: many important criteria are already saturated or currently unreachable, while criteria that distinguish rollouts are not necessarily those with the largest human weights. We introduce POW3R, a policy-aware rubric reward framework that preserves human weights and category balance as the rubric objective while adapting criterion-level reward weights during training. POW3R uses rollout-level contrast to emphasize criteria that currently separate the policy's outputs, making the GRPO reward more informative without changing the underlying evaluation target. Across three base policies on two datasets spanning multimodal and text-only settings, POW3R wins $24$ of $30$ base-policy/metric comparisons, improving both mean rubric reward and strict completion (the fraction of prompts whose response satisfies every required rubric criterion) over vanilla GRPO with rubric rewards, and reaches the same plateau in $2.5$--$4\times$ fewer training steps. Rubric rewards should therefore distinguish what should matter in the final answer from what can teach the current policy.

</details>


### [100] [HaorFloodAlert: Deseasonalized ML Ensemble for 72-Hour Flood Prediction in Bangladesh Haor Wetlands](https://arxiv.org/abs/2605.20167)
*Salma Hoque Talukdar Koli,Fahima Haque Talukder Jely,Md. Samiul Alim,Md. Zakir Hossen*

Main category: cs.AI

TL;DR: A machine-learning based early warning system (HaorFloodAlert) is developed to predict flash floods in Bangladesh’s haor wetlands up to 72 hours in advance, using a deseasonalized ensemble model and SAR-derived upstream river conditions.


<details>
  <summary>Details</summary>
Motivation: Flash floods in haor wetlands in Bangladesh arrive with little to no warning, devastating the annual boro rice crop. Existing flood early warning systems are designed mainly for riverine floods and do not capture the unique, flat-terrain backwater dynamics of haor basins. There is a need for a specialized, operationally useful forecasting system that gives farmers and authorities actionable lead time and realistic crop damage estimates.

Method: The authors design HaorFloodAlert, a deseasonalized machine learning ensemble for 72-hour probabilistic flood forecasting in the Sunamganj Haor region (~8,000 km2). They carefully remove temperature’s seasonal confounding effect, after finding it boosted accuracy simply by correlating with warm-season flooding. The model combines a Random Forest and XGBoost ensemble (weighted 0.5625 and 0.4375) trained on hydrometeorological predictors and 77 flood events observed by Sentinel-1 SAR. They also construct an upstream flood proxy using Sentinel-1 SAR data around Silchar on the Barak River, applying Otsu-thresholded SAR change detection to derive a backwater-relevant signal that yields ~36 hours of lead time. Model performance is evaluated via leave-one-out cross-validation. Additionally, they design a three-tier alert pipeline and integrate a BRRI-calibrated boro rice damage estimator for impact-based forecasts.

Result: The upstream Sentinel-1 SAR proxy for the Barak River achieves 84–91% spatial match in change detection validation, indicating reliable detection of relevant floodplain changes. The operational ensemble model (RF 0.5625 + XGBoost 0.4375) reaches 89.6% leave-one-out cross-validation accuracy, 87.5% recall, and an AUC-ROC of 0.943 on 77 observed Sentinel-1 flood events. Removing temperature’s seasonal leakage reduces artificially inflated performance, improving the model’s robustness and generalizability for true early warning. The system can provide about 72 hours of flood probability forecasts and ~36 hours of lead time from the upstream SAR signal.

Conclusion: HaorFloodAlert demonstrates that a deseasonalized machine learning ensemble, coupled with SAR-based upstream proxies, can deliver skillful, short-term flash flood forecasts tailored to haor wetland dynamics. The approach overcomes a key pitfall—spurious seasonal predictors like temperature—by explicitly controlling for them, and it shows that backwater-driven flash floods can be anticipated with high accuracy. With its three-tier alert structure and rice damage estimation calibrated to BRRI data, the system is positioned for operational use to help protect Bangladesh’s haor-region boro rice harvest and support more resilient agricultural planning.

Abstract: Flash floods in Bangladesh's haor wetlands show up with almost no warning. They wreck the annual boro rice harvest. Current setups, built for riverine floods, miss backwater dynamics entirely. These basins are flat. Water does not behave like it does on the Brahmaputra.
  We built HaorFloodAlert, a deseasonalized machine learning ensemble that forecasts 72-hour flood probability for the Sunamganj Haor (approximately 8,000 km2). Temperature was acting as a seasonal cheat code - it inflated accuracy by 6.9 pp just because floods happen in warm months. We caught that. We also built an upstream Barak River Sentinel-1 SAR proxy from Silchar, Assam, giving about 36 hours of lead time. Otsu-thresholded SAR change detection validates at 84-91 percent spatial match.
  The operational ensemble (RF 0.5625 + XGBoost 0.4375) hits 89.6 percent LOOCV accuracy, 87.5 percent recall, and 0.943 AUC-ROC on 77 real Sentinel-1 events. A three-tier alert pipeline and a BRRI-calibrated boro rice damage estimator are included.

</details>


### [101] [PEEK: Context Map as an Orientation Cache for Long-Context LLM Agents](https://arxiv.org/abs/2605.19932)
*Zhuohan Gu,Qizheng Zhang,Omar Khattab,Samuel Madden*

Main category: cs.AI

TL;DR: PEEK introduces a persistent, compact context map that captures reusable orientation knowledge about large, recurring external contexts, enabling LLM agents to reason more accurately and cheaply over long-term workloads.


<details>
  <summary>Details</summary>
Motivation: LLM agents often work repeatedly over the same large external contexts (e.g., codebases, corpora), but existing methods either replay past trajectories, re-read raw context, or store high-level strategies. None captures and maintains compact, reusable knowledge about what the context contains, how it is structured, and which elements are historically useful. This leads to unnecessary recomputation, higher cost, and weaker performance on long-context and recurring tasks. The paper aims to address this gap by creating a mechanism for agents to retain and refine orientation knowledge about a fixed external context across invocations.

Method: The authors propose PEEK, a system that maintains a context map: a small, constant-sized artifact appended to the agent's prompt that summarizes key orientation knowledge about the external context. PEEK uses a programmable cache policy with three main modules. (1) Distiller: extracts transferable, context-level knowledge from inference-time signals such as intermediate reasoning, retrieved items, or successful solution traces. (2) Cartographer: converts distilled knowledge into structured map edits, updating or adding entries in the context map in a machine-readable format (e.g., entities, schemas, locations, and organization of the context). (3) Evictor: maintains the map within a fixed token budget using a priority-based eviction policy, removing or compressing lower-utility information. This framework is model- and agent-architecture agnostic and can be applied across different LLMs and tools.

Result: On benchmarks for long-context reasoning and information aggregation, PEEK outperforms strong baselines by 6.3–34.0% while requiring 93–145 fewer iterations and yielding 1.7–5.8× lower cost than ACE, a state-of-the-art prompt-learning framework. For context-learning tasks, PEEK improves solving rate and rubric accuracy by 6.0–14.0% and 7.8–12.1% respectively, at 1.4× lower cost than ACE. The improvements hold across multiple LLMs and agent architectures, including a production-grade coding agent based on OpenAI Codex, demonstrating robustness and generality.

Conclusion: Maintaining a compact, persistent context map of reusable orientation knowledge substantially improves how LLM agents interact with large, recurring external contexts. By systematically distilling, structuring, and managing this knowledge within a fixed token budget, PEEK enables more accurate, efficient, and cost-effective long-context reasoning and context learning. The results suggest that explicit orientation knowledge, rather than just trajectory replay or raw-context access, is a key missing component in current LLM agent designs.

Abstract: Large language model (LLM) agents increasingly operate over long and recurring external contexts, like document corpora and code repositories. Across invocations, existing approaches preserve either the agent's trajectory, passive access to raw material, or task-level strategies. None of them preserves what we argue is most needed for repeated same-context workloads: reusable orientation knowledge (e.g., what the context contains, how it is organized, and which entities, constants, and schemas have historically been useful) about the recurring context itself. We introduce PEEK, a system that caches and maintains this orientation knowledge as a context map: a small, constant-sized artifact in the agent's prompt that gives it a persistent peek into the external context. The map is maintained by a programmable cache policy with three modules: a Distiller that extracts transferable knowledge from inference-time signals, a Cartographer that translates it into structured edits, and a priority-based Evictor that enforces a fixed token budget. On long-context reasoning and information aggregation, PEEK improves over strong baselines by 6.3-34.0% while using 93-145 fewer iterations and incurring 1.7-5.8x lower cost than the state-of-the-art prompt-learning framework, ACE. On context learning, PEEK improves solving rate and rubric accuracy by 6.0-14.0% and 7.8-12.1%, respectively, at 1.4x lower cost than ACE. These gains generalize across LMs and agent architectures, including OpenAI Codex, a production-grade coding agent. Together, these results show that a context map helps long-context LLM agents interact with recurring external contexts more accurately and efficiently.

</details>


### [102] [A Methodology for Selecting and Composing Runtime Architecture Patterns for Production LLM Agents](https://arxiv.org/abs/2605.20173)
*Vasundra Srinivasan*

Main category: cs.AI

TL;DR: The paper defines the 'stochastic-deterministic boundary' (SDB) as the key contract that governs how probabilistic LLM outputs trigger deterministic system actions, and presents patterns, methods, and diagnostics for designing reliable LLM agent runtimes around this boundary.


<details>
  <summary>Details</summary>
Motivation: LLM-based production agents mix stochastic model behavior with deterministic software, but current systems lack a clear, principled abstraction for the boundary between them. This makes reliability, pattern selection, and debugging difficult as models evolve. The authors aim to formalize this boundary and provide design guidance for robust agent runtimes.

Method: The authors introduce the SDB abstraction as a four-part contract (proposer, verifier, commit step, reject signal) and use it to structure a taxonomy of six runtime patterns for different agent types. They analyze each pattern using analogies to distributed-systems architectures, characterize how stochastic workers change known behaviors, and develop a five-step methodology for pattern selection plus a diagnostic mapping from failures to pattern weaknesses. They also define the replay divergence failure mode and a reliability decomposition that separates model variance from architectural effects. Finally, they apply their methodology to five workloads and build a reference implementation for a contract-renewal agent.

Result: The paper yields: (1) a formalization of the stochastic-deterministic boundary in LLM agents; (2) a catalog of six SDB-based runtime patterns with distributed-systems lineage; (3) a five-step method for choosing patterns and a diagnostic approach that ties production failures to pattern flaws; (4) identification of replay divergence as a key failure mode; (5) a conceptual reliability decomposition highlighting the role of architecture; and (6) a runnable reference system for a 90-day contract-renewal agent demonstrating the approach in practice.

Conclusion: The authors conclude that the SDB is the fundamental primitive for production LLM agent runtimes and that as models become more stable, architectural choices and SDB design dominate long-term reliability. Their patterns, methodology, and diagnostics give practitioners a structured way to design, select, and debug agent runtimes, and to anticipate failure modes like replay divergence in evolving LLM-based systems.

Abstract: Production LLM agents combine stochastic model outputs with deterministic software systems, yet the boundary between the two is rarely treated as a first-class architectural object. This paper names that boundary the stochastic-deterministic boundary (SDB): a four-part contract among a proposer, verifier, commit step, and reject signal that specifies how an LLM output becomes a system action. We argue that the SDB is the load-bearing primitive of production agent runtimes.
  Around this primitive, we organize agent runtime design into three concerns: Coordination, State, and Control. We present a catalog of six runtime patterns that compose the SDB differently across conversational, autonomous, and long-horizon agents: hierarchical delegation, scatter-gather plus saga, event-driven sequencing, shared state machine, supervisor plus gate, and human in the loop. For each pattern, we trace its lineage to distributed-systems concepts and identify what changes when the worker is stochastic.
  The paper contributes a five-step methodology for selecting runtime patterns, a diagnostic procedure that maps production failures to pattern weaknesses, and a failure mode called replay divergence, in which LLM-based consumers of a deterministic event log produce different downstream outputs under model-version or prompt changes. A stylized reliability decomposition separates per-call model variance from architectural momentum, motivating the claim that as model variance decreases, pattern choice and SDB strength become increasingly important levers for long-run reliability. We apply the methodology to five workloads and provide one runnable reference implementation for a 90-day contract-renewal agent.

</details>
